---
artifact_type: task
created_at: 2026-07-15T11:00:26.1112886-07:00
dependencies:
    - 050.013-T
id: 050.014-T
labels:
    - phase-1
    - tests
    - refactor
    - rust
parent_id: 050-F
priority: medium
references:
    - docs/exec-plans/2026-07-14-consumption-first-graphtor-plan.md
    - docs/decisions/2026-07-14-consumption-first-graphtor-deliberation.md
status: done
title: 'P1-RF5: External integration-test variant-safe pre-refactor'
updated_at: 2026-07-15T16:32:44.4431218-07:00
---

P1-RF5 (Source-variant compatibility pre-refactor 5 of 5; EXTERNAL test-only decomposition). Refactor the irrefutable `let Source::Local(...)` bindings in the EXTERNAL integration tests so `cargo test` stays green when the additive `type: database` variant lands (P1-T6 / 050.006-T): adding `Source::Database` turns each irrefutable test binding into a refutable pattern (E0005) and breaks the test build otherwise. Sites (verified complete via a full `tests/` scan): tests/config_test.rs:31 and tests/pipeline_format_test.rs:223,243 — the ONLY external test files with breaking irrefutable/non-exhaustive `Source::Local` consumers. Every OTHER `tests/*` occurrence merely CONSTRUCTS `Source::Local(LocalSource { .. })`, which an additive variant does NOT break, so no other external test file needs a pre-refactor. Route each binding through `Source::as_local()` (added in P1-RF1 / 050.010-T); while only `Local` exists behaviour is identical, so this task is independently green. NO new variant is added here (that is P1-T6). Files: tests/config_test.rs, tests/pipeline_format_test.rs (2 test files). Depends on 050.013-T (P1-RF4). Test-first.

<!-- BEGIN:acceptance-criteria -->
`tests/config_test.rs` (~L31) destructures the served config's first source through `Source::as_local()` instead of `let Source::Local(local) = &config.sources[0];`, asserting the SAME local fields; the test passes identically for a local-only config.
`tests/pipeline_format_test.rs` (~L223 and ~L243) route BOTH irrefutable bindings through `as_local()` with identical assertions.
The remaining `tests/*` `Source::Local(LocalSource { .. })` CONSTRUCTORS are left UNCHANGED (an additive variant does not break construction); no other external test file needs a pre-refactor (verified by a complete `tests/` scan).
`cargo test --all-targets` compiles and passes with NO `Source::Database` variant yet (independently green); after this task ZERO external irrefutable `let Source::Local(...)` bindings remain across `src/` AND `tests/`.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
`tests/config_test.rs:31`: replace `let Source::Local(local) = &config.sources[0];` with an `as_local()`-routed binding (e.g. `let Some(local) = config.sources[0].as_local() else { panic!() };`), preserving the existing field assertions.
`tests/pipeline_format_test.rs:223,243`: same `as_local()` routing for both `let Source::Local(local) = &config.sources[0];` sites.
This is the FINAL pre-refactor (tail of the P1-RF1..P1-RF5 chain). Combined with the src-side test-consumer coverage folded into P1-RF1 (src/config/source.rs colocated tests L162/L188/L197) and P1-RF4 (src/main.rs colocated test L4323), the ENTIRE `Source::Local` consumer surface — production AND test, internal AND external — becomes variant-safe, so P1-T6 (050.006-T) adds `Database(DatabaseSource)` with only `src/config/source.rs` match-arm changes while `cargo build` / `clippy --pedantic` / `cargo test` all stay green. Pure structural test refactor; gates P1-T6. Executed by Ship; Stage plans only.
<!-- END:implementation-notes -->