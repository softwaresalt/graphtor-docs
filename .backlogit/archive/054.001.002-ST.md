---
artifact_type: subtask
created_at: 2026-08-17T06:47:23.3261088Z
dependencies:
    - 054.001.001-ST
id: 054.001.002-ST
labels:
    - dark-mode
    - read-only-serve
    - code
    - pr90
    - post-cap-residual
parent_id: 054.001-T
references:
    - docs/exec-plans/2026-08-16-readonly-serve-guarantee-hardening-plan.md
status: done
title: Implement honest read-only contract surfaces incl. qualified startup log
updated_at: 2026-08-17T08:18:03.0793008Z
---

Post-cap residual decomposition (finding C/E) — IMPLEMENTATION half; DEPENDS ON 054.001.001-ST (its red test must exist and fail first). Implement the honest wording across all in-code contract surfaces enumerated by parent 054.001-T WITHOUT changing guard runtime behavior: is_engine_enforced_readonly rustdoc, open_engine_readonly rustdoc (store.rs:197), open_sqlite_readonly rustdoc (:138), EngineReadonlyGuard field/type docs, the 'filesystem lock active' startup log (store.rs:209-212), and any read-only wording in the serve entry point (main.rs open_serve_databases). Keep is_engine_enforced_readonly() == self.engine_readonly_guard.is_some() (do NOT overload to access_mode==ReadOnly; open_sqlite_readonly is ReadOnly with guard None and must keep reporting false). This turns the 054.001.001-ST red test GREEN.

Width isolation: production code + rustdoc (src/db/store.rs, and main.rs only if the sweep finds overstated wording there). The red test is 054.001.001-ST; the design-doc rewrite is 054.002-T.

Acceptance:
1) The 054.001.001-ST startup-log test is now GREEN.
2) is_engine_enforced_readonly() still returns guard.is_some() (NOT overloaded); existing characterization tests stay green (open_engine_readonly true; open_sqlite_readonly false; AccessMode::ReadOnly refuses mutate; single-owner byte-identical read-cycle unchanged; no cross-process closure asserted).
3) All enumerated surfaces corrected; a grep sweep finds no remaining unconditional OS-level read-only claim.
4) Call-site list of is_engine_enforced_readonly() attached confirming no caller depended on a changed meaning.
5) No edit to EngineReadonlyGuard::lock/Drop.
6) cargo test + cargo clippy --all-targets -- -D warnings -D clippy::pedantic green.

<!-- BEGIN:implementation-notes -->
Characterization-first for existing honest invariants + make the 054.001.001-ST red test green. Depends on 054.001.001-ST. Carries the bulk of parent 054.001-T's acceptance criteria (surface sweep + call-site audit + no-overload invariant); the red test moved to 054.001.001-ST and the release-observability evidence to 054.001.003-ST. Reuse existing open_engine_readonly_* test scaffolding at src/db/store.rs:823+.
<!-- END:implementation-notes -->