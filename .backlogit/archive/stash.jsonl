{"id":"94D655D7","priority":"medium","kind":"task","text":"Upgrade cozo/git2 deps to clear audit advisories: RUSTSEC-2026-0041 lz4_flex via cozo chain, RUSTSEC-2026-0008 git2 0.19 unsound Buf deref","created_at":"2026-04-29T23:19:11.5926081Z","removed_at":"2026-04-30T03:14:54.0519377Z","removal_reason":"harvested","harvested_artifact_id":"007.006-T"}
{"id":"AE889CAD","priority":"high","kind":"task","text":"[SPIKE] Research and implement guardrails to prevent direct-to-main pushes by agents. Incident: commits cbbfb81 and 3fd285a were pushed directly to main bypassing branch protection after a PR merge. Research scope: (1) git pre-push hooks that block pushes to main locally, (2) GitHub branch protection hardening (require PR, disable admin bypass for agents), (3) agent-level instruction/checklist enforcement in copilot-instructions.md, (4) CI workflow to detect and alert on direct main commits. Deliverable: a recommendation with the lowest-friction approach that works for both human and agent contributors, plus implementation.","created_at":"2026-04-30T23:39:45.9659882Z","removed_at":"2026-04-30T23:43:31.5979719Z","removal_reason":"harvested","harvested_artifact_id":"013.007-T"}
{"id":"8474839B","priority":"high","kind":"feature","text":"PDF document ingestion: add ability to parse and chunk PDF files of any size into the pipeline. Requires: PDF text extraction crate (evaluate pdf-extract, lopdf, pdfium), page/section-aware chunking strategy, handling of tables/images/figures (text extraction vs OCR), large-file streaming to avoid memory blowout, integration as a new parser backend alongside Markdown. Needs a spike to evaluate crate choices, extraction quality, and chunking strategy for structured documents.","created_at":"2026-05-01T04:46:44.5046919Z","removed_at":"2026-05-01T05:00:35.8653736Z","removal_reason":"removed"}
{"id":"FF99F3D3","priority":"low","kind":"bug","text":"Fix stale doc comment in src/chunk/mod.rs:5 — still references \"LanceDB vectors to Kùzu graph nodes\" but should reference CozoDB unified store","created_at":"2026-05-01T04:37:05.1982583Z","removed_at":"2026-05-01T05:00:35.9238906Z","removal_reason":"removed"}
{"id":"2F9B9F1D","priority":"high","kind":"feature","text":"Persist embeddings in CozoDB HNSW index and implement semantic vector search (search_similar). Currently vectors are computed during sync but discarded. This is the primary value-add — enabling similarity-based documentation discovery beyond keyword matching. Planned internally as 009-F.","created_at":"2026-05-01T04:46:44.355499Z","removed_at":"2026-05-01T16:03:53.3671432Z","removal_reason":"harvested","harvested_artifact_id":"016-F"}
{"id":"506E9EBA","priority":"high","kind":"feature","text":"Expand MCP tool surface: add list_sources (enumerate indexed sources), get_chunk_by_id (retrieve chunk by SHA-256 ID), get_document (all chunks for a path), search_semantic (vector search, depends on HNSW), get_status (DB stats/health). Currently only search_local_docs and traverse_doc_links exist.","created_at":"2026-05-01T04:46:44.3826371Z","removed_at":"2026-05-01T16:03:55.1054775Z","removal_reason":"harvested","harvested_artifact_id":"017-F"}
{"id":"C114CFEA","priority":"high","kind":"feature","text":"Wire incremental sync (sync_source) into the CLI sync command. The sync module exists and is tested (git diff + mtime diff + surgical re-ingestion) but cmd_sync uses the full pipeline::run instead. Add --full flag for existing behavior, make incremental the default.","created_at":"2026-05-01T04:46:44.4167244Z","removed_at":"2026-05-01T16:03:55.3333256Z","removal_reason":"harvested","harvested_artifact_id":"018-F"}
{"id":"E4B157F9","priority":"high","kind":"feature","text":"Web crawler acquisition source: add type: url source to sources.yaml for crawling live documentation sites (e.g. learn.microsoft.com/azure/cosmos-db/). Requires: HTTP client (reqwest), HTML-to-Markdown converter, recursive link follower with configurable depth/domain limits, rate limiting, robots.txt compliance, deduplication. This is a new acquisition backend alongside git and local — follows the same pipeline interface (returns list of Markdown files for parse stage). Needs a spike first to evaluate crate choices and crawl strategy.","created_at":"2026-05-01T04:46:44.4830511Z","removed_at":"2026-05-01T16:03:55.6111054Z","removal_reason":"harvested","harvested_artifact_id":"019-F"}
{"id":"F7BE3795","priority":"high","kind":"feature","text":"Word document (.docx) ingestion: add ability to parse and chunk Word documents of any size into the pipeline. Requires: OOXML parser crate (evaluate docx-rs, office-crypto, or zip + xml manual parsing), paragraph/heading-aware chunking, table extraction, embedded image handling, large-file streaming, integration as a new parser backend alongside Markdown and PDF. Needs a spike to evaluate crate choices, extraction fidelity, and chunking strategy for Word's nested structure.","created_at":"2026-05-01T04:48:39.6124965Z","removed_at":"2026-05-01T16:03:55.8555948Z","removal_reason":"harvested","harvested_artifact_id":"020-F"}
{"id":"FCE968DB","priority":"high","kind":"task","text":"E2E smoke test: configure sources.yaml with azure-cosmos-db-docs repo, run graphtor-docs sync, verify chunks loaded with graphtor-docs status, start MCP server with graphtor-docs serve, test search_local_docs and traverse_doc_links against real Cosmos DB documentation content.","created_at":"2026-05-01T04:46:44.4554459Z","removed_at":"2026-05-01T16:04:07.662052Z","removal_reason":"harvested","harvested_artifact_id":"018.001-T"}
{"id":"C7F2D1C0","priority":"critical","kind":"task","text":"Agent harness learning: Stage agent MUST NOT call backlogit_ship_shipment, backlogit_claim_shipment, backlogit_archive_item, or backlogit_delete_item. Stage only assembles shipments and gates readiness (verifies tasks are decomposed, plans reviewed, tests defined). Ship agent owns: claiming the shipment, feature branch creation, build loop, PR lifecycle, CI validation, review handling, operator-approved merge, and THEN backlogit_ship_shipment. Stage's terminal action is confirming readiness and reporting to the operator for Ship orchestration.","created_at":"2026-05-02T04:29:01.7532985Z","removed_at":"2026-05-02T06:09:46.2852414Z","removal_reason":"removed"}
{"id":"A753EA81","priority":"high","kind":"feature","text":"Mature the documentation source registry (sources.yaml) to support mixed-format ingestion per source: each Git, URL, or local source entry should declare which document types it contains (PDF, DOCX, Markdown) so the pipeline dispatches the correct parser per file. Add schema validation, default format lists, and per-source format override configuration. This enables a single Git repo source to ingest its Markdown, PDF, and DOCX files in one pass.","created_at":"2026-05-02T16:45:51.8655378Z","removed_at":"2026-05-02T16:46:37.9603503Z","removal_reason":"harvested","harvested_artifact_id":"021-F"}
{"id":"81C47B70","priority":"high","kind":"task","text":"Fully test PDF, DOCX, and Git repo documentation ingestion: create comprehensive integration tests covering each acquisition source type end-to-end (acquire → parse → chunk → embed → load). Validate correct handling of edge cases (large files, malformed input, empty documents, nested structures) for each format. Ensure deterministic chunk IDs and idempotent re-ingestion across all three source types.","created_at":"2026-05-02T16:45:51.1829887Z","removed_at":"2026-05-02T16:46:43.6340549Z","removal_reason":"harvested","harvested_artifact_id":"021.001-T"}
{"id":"17B07B96","priority":"high","kind":"feature","text":"Streaming PDF parser with heading-aware chunk accumulation: Replace current whole-file PDF parsing with page-streaming via lopdf (random page access). Stream pages one at a time, detect heading boundaries via font-size heuristics, accumulate text into section buffers, and emit chunks at semantic boundaries rather than arbitrary page breaks. This bounds memory usage (one section at a time vs. entire PDF), produces semantically meaningful chunks aligned to document structure, avoids near-duplicate chunks from overlapping page windows, and naturally fits the existing doc_chunks schema (headings, content, position). Requires evaluating lopdf vs pdf-extract for font metadata access. Current pdf-extract approach loads entire PDF and produces flat text with no heading detection — debug-mode processing of a 109MB PDF takes 45+ minutes.","created_at":"2026-05-03T05:57:09.2825139Z","removed_at":"2026-05-03T06:01:14.7303748Z","removal_reason":"harvested","harvested_artifact_id":"022-F"}
{"id":"FBD823AB","priority":"high","kind":"feature","text":"PDFium-render dual backend for large PDF ingestion: Replace pdf-extract with pdfium-render for PDFs ≥20 MiB. PDFium opens documents lazily (instant for any size) vs pdf-extract's lopdf which eagerly parses all xref entries serially (\u003e20 min for 104 MB). Implementation: PdfiumBackend struct with try_parse() fallback pattern, DLL discovery via env var → exe dir → system path. Already benchmarked: pdf-extract DNF on Cosmos DB PDF, pdfium-render expected \u003c10s. Dependency: pdfium-render 0.9.1 (thread_safe, pdfium_latest features). Feature branch exists: feat/pdfium-large-pdf-backend with partial implementation.","created_at":"2026-05-04T05:54:53.5895569Z","deliberation_id":"001-DL","removed_at":"2026-05-04T06:32:32.0374139Z","removal_reason":"removed"}
{"id":"493CA939","priority":"medium","kind":"feature","text":"Comprehensive documentation for all aspects of graphtor-docs: architecture overview, data pipeline stages (acquire/parse/embed/load), MCP tool reference, configuration guide (sources.yaml schema, all source types), CLI command reference, developer setup guide, embedding model details (all-MiniLM-L6-v2 via Candle), CozoDB schema and query patterns, incremental sync design, and troubleshooting guide. Should cover both user-facing (how to configure and run) and developer-facing (how to contribute and extend) perspectives. Output: docs/ directory with well-structured markdown files.","created_at":"2026-05-03T02:19:05.3981109Z","removed_at":"2026-05-04T22:35:11.3666716Z","removal_reason":"removed"}
{"id":"56CA4B2E","priority":"medium","kind":"feature","text":"Refactor PdfExtractBackend Pass 2 to use output_doc_page in a page-by-page loop instead of output_doc (full traversal), enabling heading-aware chunking for PDFs of any size. Currently the 20 MiB LARGE_PDF_THRESHOLD guard forces all large PDFs into page-based chunking with heading_hierarchy: [\"Page N\"]. The histogram (Pass 1) is already bounded at 30 pages and is not the bottleneck — Pass 2 is. Switching Pass 2 to output_doc_page makes the threshold guard unnecessary and restores section-based chunk IDs for large PDFs. Analysis: docs/research/pdf-pass2-page-by-page-refactor-2026-05-04.md","created_at":"2026-05-05T05:08:25.7543814Z","removed_at":"2026-05-05T05:51:47.5274726Z","removal_reason":"harvested","harvested_artifact_id":"027-F"}
{"id":"750E47A6","priority":"high","kind":"feature","text":"Rebrand MCP tool names to be source-agnostic: rename `search_ms_docs_semantic` → `search_semantic`, ensure tool descriptions don't imply MS-only scope. The underlying engine already handles any Markdown source — the naming is the only barrier to multi-workspace adoption.","created_at":"2026-05-05T22:58:36.22789Z","removed_at":"2026-05-05T23:35:18.6846116Z","removal_reason":"harvested","harvested_artifact_id":"028.001-T"}
{"id":"3BE45F02","priority":"high","kind":"feature","text":"Add pre-sync commands to start.ps1: run `graphtor-docs sync` and `backlogit sync` before launching copilot. Ensures both the doc index and backlog index are fresh at session start without relying on the agent or operator to remember. Complements the auto-sync-on-serve feature but works immediately without code changes to graphtor itself.","created_at":"2026-05-05T23:02:46.0190941Z","removed_at":"2026-05-05T23:35:18.9689335Z","removal_reason":"harvested","harvested_artifact_id":"028.002-T"}
{"id":"43F49359","priority":"high","kind":"feature","text":"Workspace auto-discovery mode: graphtor automatically detects and indexes .md files in the current repo without requiring manual sources.yaml configuration. Enables zero-config adoption in any workspace — just run `graphtor-docs install \u0026amp;\u0026amp; graphtor-docs sync` and it indexes the local repo's docs.","created_at":"2026-05-05T22:58:36.0712666Z","removed_at":"2026-05-05T23:35:34.6899797Z","removal_reason":"harvested","harvested_artifact_id":"029.001-T"}
{"id":"E7B5A92C","priority":"high","kind":"feature","text":"Auto-sync on MCP server start: when `graphtor-docs serve` launches the MCP server, it should detect stale or unsynced content (via commit hash / mtime comparison) and perform an incremental sync before accepting tool calls. Eliminates the manual `sync` step — the agent gets fresh data the moment it connects. Should be non-blocking (serve immediately, sync in background) with a status tool indicating sync progress.","created_at":"2026-05-05T22:59:41.9650945Z","removed_at":"2026-05-05T23:35:35.0255514Z","removal_reason":"harvested","harvested_artifact_id":"029.002-T"}
{"id":"F522A2CC","priority":"medium","kind":"feature","text":"Add composite MCP tool `research_topic` that wraps the common search→traverse→retrieve workflow into a single tool call. Keeps individual tools intact but provides a one-shot convenience for the 80% case: (1) search_local_docs or search_semantic based on query, (2) auto-traverse top_k results 1-2 hops, (3) deduplicate, (4) return enriched results with full content + related context. Reduces 4-6 round-trips to 1 for the typical agent research pattern.","created_at":"2026-05-05T22:55:44.2806173Z","removed_at":"2026-05-05T23:35:35.4261038Z","removal_reason":"harvested","harvested_artifact_id":"029.003-T"}
{"id":"AEAA9E54","priority":"high","kind":"feature","text":"Add `--json` flag to every CLI command that outputs results in JSON-RPC 2.0 format (matching MCP server response structure) so agents can consume CLI output identically to MCP tool responses. Also add a `graphtor-docs manifest` command that emits a JSON-RPC 2.0 tool manifest listing all CLI commands with names, descriptions, and parameter schemas — mirroring what the MCP server returns via `tools/list` on initialization. This enables CLI-first agent workflows without requiring the MCP STDIO server, and ensures parity between CLI and MCP surfaces.","created_at":"2026-05-06T01:21:39.0590108Z","removed_at":"2026-05-06T18:25:48.5973597Z","removal_reason":"harvested","harvested_artifact_id":"030-F"}
{"id":"0E1FC522","priority":"medium","kind":"task","text":"Update project documentation (README.md, AGENTS.md, docs/) to incorporate latest feature additions: --json global flag for JSON-RPC 2.0 CLI output, manifest subcommand, zero-config workspace adoption (init/install/doctor/upgrade/uninstall), and docs rebranding from LocalDocRAG to graphtor-docs. Ensure usage examples, architecture references, and CLI command listings reflect the current tool surface.","created_at":"2026-05-06T19:51:21.2896068Z","removed_at":"2026-05-07T01:18:04.8621148Z","removal_reason":"harvested","harvested_artifact_id":"031-F"}
{"id":"7DF2B95F","priority":"medium","kind":"feature","text":"Replace brute-force O(n) vector search with CozoDB native HNSW index for sub-logarithmic approximate nearest-neighbor lookup. Current implementation loads all embeddings from doc_vectors as JSON strings, deserializes, computes cosine similarity in Rust, and sorts — O(n) per query. HNSW would provide O(log n) search, eliminate JSON serde overhead, and enable scaling to the full 703-repo MS Docs corpus (500k+ chunks). Requires schema v3 migration, native vector column type, and re-ingestion pass.","created_at":"2026-05-07T19:24:24.6934249Z","removed_at":"2026-05-07T19:37:14.3237292Z","removal_reason":"harvested","harvested_artifact_id":"032-F"}
{"id":"831A0320","priority":"medium","kind":"task","text":"Installation of graphtor seems to install the MCP server configuration in .github/copilot/mcp.json, which is absolutely not correct as it is not recognized by VS Code, Copilot CLI, or any other IDE or CLI.","created_at":"2026-05-19T05:03:24.02823Z","archived_at":"2026-05-20T19:57:49.5303827Z","reason":"archived"}
{"id":"5E8F56AF","priority":"medium","kind":"task","text":"telemetry output for backlogit metrics reporting","created_at":"2026-05-20T23:17:57.4813057Z","archived_at":"2026-05-20T23:21:32.1905649Z","reason":"archived"}
{"id":"65564D24","priority":"medium","kind":"task","text":"pre-warm indexing/sync with operator-friendly progress reporting","created_at":"2026-05-20T23:17:58.3391409Z","archived_at":"2026-05-20T23:21:33.0757394Z","reason":"archived"}
{"id":"3FE2DDFB","priority":"medium","kind":"feature","text":"graphtor needs a way to be called in a pre-warm manner to fully index/sync the workspace database before starting copilot. Currently, calling sync outputs a lot of graph related syntax to the terminal, which doesn't mean much to the operator. Instead, graphtor needs to assess total amount of content it needs to ingest and show either a progress bar in the terminal or a stream of output showing the file being ingested and the percentage to total completion at the individual file level and at the total volume of content to ingest level.","created_at":"2026-05-20T15:02:58.4802396Z","archived_at":"2026-05-21T23:38:39.8894219Z","reason":"archived"}
{"id":"0D214027","priority":"medium","kind":"task","text":"Add telemetry to output to be used by backlogit for metrics reporting.","created_at":"2026-05-20T15:00:15.129337Z","archived_at":"2026-05-21T23:38:44.3449521Z","reason":"archived"}
{"id":"03D96C20","priority":"medium","kind":"feature","text":"multi-database file support","created_at":"2026-05-20T23:17:59.1277673Z","archived_at":"2026-05-22T06:49:46.0174557Z","reason":"archived"}
{"id":"B751FA6D","priority":"medium","kind":"task","text":"As part of multi-database file support, allow sources.yaml to specify the target database filename for a source group so content can be routed into a specific database file such as powerbi.db or rust-docs.db.","created_at":"2026-05-22T06:33:40.3764034Z","archived_at":"2026-05-22T06:49:46.5697283Z","reason":"archived"}
{"id":"1F123CF3","priority":"medium","kind":"task","text":"Stash this: multi-database file support: Consider this: you want to be able to support multiple database files within Graphtor-docs so that you can have one for Rust, another one for C Sharp, and another one for Power BI to house all of your documentation that you need. You want to support that one content source or type and then be able to reuse that across multiple projects so that you don't have to re-ingest the data for every project using those sources.","created_at":"2026-05-20T15:09:03.492951Z","archived_at":"2026-05-22T06:50:03.251568Z","reason":"archived"}
{"id":"E6E6477A","priority":"high","kind":"feature","text":"Multi-database runtime hardening: production-ready concurrent access to multiple database files (graph.db, powerbi.db, etc.) without panics from CozoDB/SQLite locking. Includes read-only parallel access for search/status operations, per-database locking strategy, and graceful error recovery instead of panics when locks are encountered.","created_at":"2026-05-23T14:43:16.0383597Z","archived_at":"2026-05-23T14:50:08.1008905Z","reason":"archived"}
{"id":"4BEEF41A","priority":"high","kind":"feature","text":"Source registry normalization and duplicate-intake preflight: Support multiple named source files (*.sources.yaml pattern e.g. graph.sources.yaml, powerbi.sources.yaml). Enforce consistent schema across all source files including explicit database field. Before ingestion, parse all source files and detect duplicate file/path intake across databases, alerting the operator to allow override or adjustment.","created_at":"2026-05-23T14:43:08.5678387Z","archived_at":"2026-05-23T14:50:08.9433891Z","reason":"archived"}
{"id":"25F91517","priority":"high","kind":"bug","text":"Serve safety: when an existing .db is used without .graphtor/config/sources.yaml, auto-generate a local stub config (sources: []) instead of falling back to workspace auto-discovery/background sync so imported databases can be served read-only without ingesting local markdown.","created_at":"2026-05-26T17:23:32.5443004Z","removed_at":"2026-05-27T17:30:06.8688511Z","removal_reason":"harvested","harvested_artifact_id":"041-F"}
{"id":"3848CFD7","priority":"critical","kind":"feature","text":"Run a full release-version test using the current .graphtor/config sources.yaml files, diagnose sync embedding-model lookup failures, and add useful sync progress indication to the sync command.","created_at":"2026-05-29T00:54:02.4078538Z","archived_at":"2026-05-29T01:30:18.180415Z","reason":"archived"}
{"id":"964597B1","priority":"medium","kind":"task","text":"Triage cargo audit unmaintained transitive dependencies after 042-S closure: RUSTSEC-2025-0056 adler, RUSTSEC-2025-0141 bincode, RUSTSEC-2025-0057 fxhash, RUSTSEC-2025-0119 number_prefix, and RUSTSEC-2024-0436 paste via cozo/tokenizers/indicatif","created_at":"2026-06-14T22:23:25.0639161Z","deliberation_id":"002-DL","archived_at":"2026-06-19T01:23:13.6630813Z","reason":"archived"}
{"id":"011D6491","priority":"high","kind":"feature","text":"Cross-source cross-product link resolution in graph traversal.\n\nPROBLEM: doc_edges { src_chunk_id, target_path =\u003e link_text, anchor } (src/db/edges.rs) stores link targets as raw strings. find_related_chunks (src/db/traverse.rs) resolves each target_path only against doc_chunks where source_id = seed.source_id AND path == target_path — deliberately source-scoped. So cross-product links (absolute Learn URL paths like /fabric/admin, and any link whose target lives in a different ingested source) never resolve; a multi-source DB is fragmented at source boundaries for graph traversal. (Vector search in src/db/search.rs is already cross-source; this is graph-edge-only.)\n\nGOAL: Let traversal follow an edge whose target is a document in a different source when a reliable, globally-unique key identifies that target — without reintroducing the relative-path ambiguity the current source-scoping prevents.\n\nCROSS-SOURCE KEY (contract with docline): Introduce a canonical_url identifier per chunk/doc, populated from a new docline frontmatter field canonical_url (the Learn URL a doc publishes under, e.g. /fabric/admin/foo, forward-slash, no host). Index it as a new relation, e.g. doc_url_index { canonical_url: String =\u003e source_id: String, chunk_id: String } (or a canonical_url column on doc_chunks plus a lookup). canonical_url MUST be globally unique across sources.\n\nTWO-TIER RESOLUTION (in chunks_at_path / a new resolver): Tier 1 (unchanged): intra-source exact match — source_id = seed.source_id AND path == target_path. Keeps within-repo relative links fast and preserves the source-pollution guard. Tier 2 (new): if target_path is absolute (starts_with('/')) or Tier 1 returns nothing, look up doc_url_index by canonical_url == target_path — not source-scoped — resolving to (target_source_id, target_chunk_id).\n\nTRAVERSAL CHANGES (find_related_chunks): after Tier-1, for unresolved/absolute targets call the new global chunk_by_canonical_url(target_path); enqueue the resolved chunk with its own source_id so subsequent hops are scoped correctly to the doc's home source. Respect max_depth. Unresolved target stays a dangling edge (no crash, no false match).\n\nEDGE TYPING (recommended): classify each edge as intra_source vs cross_product (absolute target, or resolved to a different source_id) so consumers can weight/filter cross-repo hops.\n\nACCEPTANCE: (1) Two sources (e.g. powerbi + fabric) in one DB: a /fabric/admin/foo link from a powerbi chunk resolves via find_related_chunks to the fabric chunk (different source_id) when a matching canonical_url exists. (2) Intra-source relative links resolve exactly as before — no regression; identical relative source_paths across sources are NOT cross-linked. (3) Cross-product links with no matching canonical_url remain dangling (graceful). (4) Traversal hops across sources up to max_depth; anchors on cross-product edges are preserved.\n\nOUT OF SCOPE: Deriving canonical_url (docline owns that). Changing vector search. Rewriting stored link text.\n\nTOUCH POINTS: src/db/schema.rs (new relation/column + migration), src/db/edges.rs (index population), src/db/traverse.rs (chunks_at_path, find_related_chunks), src/parse/frontmatter.rs (read canonical_url if keying off frontmatter), src/db/chunks.rs (carry canonical_url).","created_at":"2026-07-04T07:16:43.9607746Z","archived_at":"2026-07-04T23:47:15.6855109Z","reason":"harvested","harvested_artifact_id":"043-F"}
{"id":"055D3479","priority":"medium","kind":"task","text":"Chore: reduce CI build actions to only run on code changes, to conserve GitHub Actions usage limits.\n\nIDEA: The CI workflow (.github/workflows/ci.yml) currently runs the full build/fmt/clippy/test/audit pipeline on every PR/push, including chore/docs-only PRs where compilation is unnecessary. Skip the heavy build actions where they add no value.\n\nAPPROACHES TO CONSIDER:\n- paths-ignore: on the workflow triggers to skip runs when only non-code paths change (e.g. docs/**, **/*.md, .github/agents/**, .github/instructions/**, .github/prompts/**).\n- PR-title-based conditional gating on jobs/steps, e.g.:\n    if: |\n      !startsWith(github.event.pull_request.title, 'chore:') \u0026\u0026\n      !startsWith(github.event.pull_request.title, 'docs:')\n  to skip build actions for chore:/docs: PRs.\n- Or a combination of path filters + title-based skip.\n\nGOAL: Only run heavy build/clippy/test/audit actions when code actually changes, reducing GitHub Actions minutes/usage.\n\nCAVEATS TO ADDRESS DURING DESIGN:\n- Required status checks + branch protection: if the build check is a REQUIRED check, skipping the job (job never runs) can BLOCK merge because the required check never reports a conclusion. Options: use workflow-level paths/paths-ignore filters (GitHub treats path-skipped required checks as passing) rather than per-job if:; or add a companion job that reports success on skip; or make the check non-required. Design must not deadlock merges of chore/docs PRs.\n- push to main should still run full CI (or at least on release-relevant changes).\n- Keep the audit gate running whenever Cargo.lock / Cargo.toml change (advisory gate) even for chore/docs PRs.\n- Title-based gating is fragile (PR title can change after checks run) and spoofable; path-based filtering is more robust — weigh both.\n\nTOUCH POINTS: .github/workflows/ci.yml (triggers, job-level if / paths-ignore).","created_at":"2026-07-04T20:40:55.2719324Z","archived_at":"2026-07-05T00:56:15.2660616Z","reason":"harvested","harvested_artifact_id":"044-F"}
{"id":"CEA0C26F","priority":"medium","kind":"task","text":"Console UX: when the embedding model directory cannot be found/loaded, print the diagnostic to the console in TTY red so the user clearly understands the model was missing (and that sync therefore degraded to graph-only with no vector embeddings).\n\nContext / root cause: src/embed/resolver.rs emits the \"[embed] local embedding model unavailable\" / \"[embed] embedding model unavailable\" diagnostics via plain uncolored eprintln! (emit_local_dir_diagnostic ~L215-231, emit_resolution_diagnostic ~L193-210). On load failure resolve_embedding_model() degrades to Ok(None) and sync continues WITHOUT embeddings (doc_chunks.embedding stays null), which silently produces a graph/keyword-only index. A real user hit this and did not realize embeddings were skipped.\n\nDesired behavior:\n- Render the missing/unloadable embedding-model diagnostic in bold red on a TTY (auto-detected, honoring NO_COLOR), consistent with the new src/cli/errfmt.rs anstream/anstyle color approach (AutoStream strips ANSI on non-TTY). Note: the diagnostic lives in the graphtor-core lib (embed module), while errfmt is in the bin crate — either move a small shared color helper into the lib or add anstream/anstyle usage in the embed module.\n- Also make the degraded outcome prominent at end of sync (e.g., a colored WARN summary line like \"embeddings skipped — semantic search unavailable; N chunks stored without vectors\") so the skip is not easily missed among logs.\n\nAcceptance: on a TTY with a missing GRAPHTOR_EMBED_MODEL_DIR (or absent model files), sync prints a clearly red warning that embeddings were skipped; piped/non-TTY output is plain (NO_COLOR respected); existing tests still pass. Related to the error-messaging color work (errfmt) shipped for the fatal-error path.","created_at":"2026-07-10T01:49:15.655141Z","archived_at":"2026-07-10T23:49:56.6790128Z","reason":"harvested","harvested_artifact_id":"049.002-T"}
{"id":"79B5A7BC","priority":"medium","kind":"feature","text":"Read-only, zero-config graphtor context server for agent workspaces (docline generates content -\u003e graphtor builds the db -\u003e agents consume it read-only), with the graphtor dev workspace as the generation+serve exception.\n\nPRODUCT FRAMING (authoritative intent, per operator):\ngraphtor is not primarily a sync engine inside an agent environment. Pipeline: docline authors the standardized Markdown source; graphtor `sync` is a GENERATION-side step that builds a .db from that content (run where the docline content lives); the generated .db is consumed in AGENT workspaces as a READ-ONLY source of context. In a consumer workspace there is no source content, so graphtor should never sync there (sync there is only ever accidental). Modes are separated by ROLE:\n  - Generation (authoring/build workspace, has docline output/sources): `sync` content -\u003e .db (write).\n  - Consumption (agent workspace, has only .db files): `serve` read-only context (no sync).\n\nWORKSPACE-MODE EXCEPTION (critical): read-only consumption is the DEFAULT for ALL workspaces EXCEPT the graphtor tool's own development workspace (this repo, e.g. C:\\Source\\GitHub\\graphtor). That one workspace is BOTH a generator AND a server — it must retain the full sync/write path (it builds/tests databases like powerbi.db, postgresql.db from its own sources.yaml). Every OTHER workspace is effectively a read-only server.\nMode-detection recommendation: derive mode from CONTENT, not a hardcoded path — a workspace is in generation mode only when it has RESOLVABLE ingestion sources with real content (sources.yaml with local sources whose paths exist). Otherwise it is read-only consumption, EVEN IF a stale/empty sources.yaml exists (a consumer workspace must never accidentally re-enable sync). The graphtor dev repo qualifies for generation because it has real sources; consumer workspaces (bare .db files, no real sources) get read-only auto-discovery. Consider an explicit override flag only as a safety escape hatch, but default behaviour must be content-derived and safe (fail read-only).\n\nDESIGN IMPLICATION: read-only serve is the default posture; the write/sync posture is enabled only by presence of real generation sources (the dev workspace being the primary such case). An agent/consumer workspace's `serve` needs no sources.yaml and no GRAPHTOR_DB_PATH; it exposes whatever .db files live in `.graphtor/`, read-only.\n\nCurrent behaviour (verified):\n- `.mcp.json` from install is ALREADY minimal — managed_server_value (src/workspace/mcp_config.rs:273) writes only {command, args:[\"serve\"], transport:\"stdio\"}, NO env. GRAPHTOR_DB_PATH/GRAPHTOR_SOURCES are operator-added by hand.\n- No env -\u003e `serve` defaults db_path=cwd/.graphtor/graph.db, config=cwd/.graphtor/config/sources.yaml.\n- `serve` discovers dbs via discover_db_files(db_path, source_config) (src/main.rs:2408): iterates config.sources OR a single explicit db_path; does NOT scan `.graphtor/` for dropped .db files.\n- `serve` opens each db read-write (rw_stores) AND read-only (ro_stores) via open_serve_databases (src/main.rs:2344) and spawns background incremental sync (WRITE) when sources.yaml has sources (src/main.rs:2447) — the accidental-resync risk.\n\nDesired UX:\n- Drop a .db into the root of `.graphtor/` -\u003e `serve` auto-detects and exposes it read-only for query/search/semantic. No sources.yaml, no GRAPHTOR_DB_PATH.\n- Source-backed dbs (a `local` source with an existing path) keep read-write + background sync (generation side); dbs with NO matching real source are served READ-ONLY and NEVER background-synced (no rescan, no v4 prune).\n\nDesign directions to deliberate:\nA) serve auto-discovery of `*.db` in top level of `.graphtor/`; classify has-real-ingestion-source (rw + sync) vs no-source (read-only, no sync). Read-only default; sync only when real sources resolve.\nB) Explicit read-only db entry in sources.yaml (new `type: database` kind, or top-level `databases:` list, or `read_only: true`) for external/aliased dbs.\nC) Combination: auto-discovery zero-config default + explicit entries for named/aliased/external dbs.\nDecide: consumption vs generation is CONTENT-DERIVED (resolvable real sources) by default; optional explicit override flag as escape hatch only.\n\nCross-cutting: serve db discovery + role classification; background-sync gating (never sync a no-real-source db, even with a stale sources.yaml); DataStore read-only open (open_sqlite_readonly exists); v4 pre-sync gate still applies to served read-only dbs (refuse/report pre-v4); MCP tool surface + list-sources across multiple auto-discovered dbs (synthesize source metadata from stored doc_sources); status parity; docs (.mcp.json, .graphtor layout, docline-\u003egraphtor-\u003eagent pipeline + dev-workspace exception, in product-specs/design-docs); security (only .graphtor root, path/containment, skip lock/index/tmp + .graphtor/models + generated artifacts); backward compat (default graph.db + existing sources-driven multi-db serve preserved for the generation/dev workspace).\n\nAcceptance (high level): in a consumer workspace with a minimal .mcp.json (just `serve`) and .db files in `.graphtor/`, the MCP tools + `status` query them read-only and `serve` never writes/resyncs; in the graphtor dev workspace (real sources), sync still works and it can both generate and serve; a stale/empty sources.yaml in a consumer workspace does NOT enable sync; pre-v4 dbs are gated; docs capture the pipeline + dev-workspace exception. Deliberation first, then phased implementation.","created_at":"2026-07-14T17:22:47.4753372Z","archived_at":"2026-07-15T01:54:05.1395656Z","reason":"harvested","harvested_artifact_id":"050-F"}
{"id":"B333B9B8","priority":"medium","kind":"feature","text":"Consumption-first `install`: default to a minimal serve-only footprint; make ingestion scaffolding opt-in; document ingestion setup separately.\n\nMotivation / product framing: graphtor in a consumer (agent) workspace is a READ-ONLY context server, not a sync engine (see stash 79B5A7BC). Installing into a new workspace should therefore NOT scaffold ingestion machinery by default. Only the graphtor dev/authoring workspace (with real docline sources) needs the generation/sync scaffold.\n\nCurrent behaviour (verified):\n- `install()` (src/workspace/install.rs:34-45) creates ALL of `.graphtor/{bin,data,cache,config,logs}` from GRAPHTOR_SUBDIRS (src/workspace/paths.rs: GRAPHTOR_SUBDIRS = [\"bin\",\"data\",\"cache\",\"config\",\"logs\"]) and copies the running binary into `.graphtor/bin/`.\n- cmd_install (src/main.rs:3002) then calls init_sources_yaml (src/main.rs:3018 -\u003e src/workspace/init.rs) writing a template `.graphtor/config/sources.yaml`, and generate_mcp_config (src/main.rs:3028) writing/merging `.mcp.json`.\n- Post-install message (src/main.rs:3080) tells the user to \"edit .graphtor/config/sources.yaml to add documentation sources\" — ingestion-oriented.\n- Note: bin/data/cache/config/logs all support ingestion/generation; serve only needs the .graphtor root (to hold dropped .db files) + a resolvable binary + .mcp.json.\n\nDesired behaviour:\n- `install` default (consumption-first): create only what serve needs — the `.graphtor/` root (where operators drop .db files) and a minimal `.mcp.json` (command graphtor-docs, args [\"serve\"]). Do NOT write sources.yaml. Do NOT create config/bin/cache/data/logs. Post-install message should explain \"drop a .db into .graphtor/ to serve it read-only\" and link to the ingestion-setup docs.\n- Ingestion/generation scaffold becomes OPT-IN: e.g. `graphtor-docs install --with-ingestion` (or the existing `graphtor-docs init` / a dedicated setup command) creates config + sources.yaml + data/cache/logs (+ bin/binary copy if used) for authoring/build workspaces. The graphtor dev workspace uses this path.\n- Binary resolution for serve: consumption workspaces typically use a globally-installed `graphtor-docs` on PATH (so no `.graphtor/bin/` copy needed); .mcp.json should reference the PATH command by default, and only use `.graphtor/bin/graphtor-docs` when the ingestion/bin scaffold was created. Decide the resolution/precedence.\n\nDocumentation: add a SEPARATE docs section covering how to set up ingestion/sync of docline-produced source content (generation-side): creating the ingestion scaffold, authoring sources.yaml, running sync, and how the generated .db is then consumed read-only in agent workspaces. Keep the default install docs focused on the consumption/serve path.\n\nCross-cutting / considerations: install/uninstall/upgrade parity (uninstall must clean up whichever footprint was created; upgrade of a consumption install has no bin copy to replace); doctor expectations (doctor currently assumes config/data etc. — must tolerate the minimal consumption layout); idempotency; backward compat for existing full installs; the graphtor dev workspace must retain the full generation scaffold; .gitignore/.graphtor artifact expectations. Depends on / pairs with 79B5A7BC (serve read-only auto-discovery of .graphtor/*.db) — a minimal consumption install only makes sense once serve auto-discovers dropped dbs; sequence 79B5A7BC first or ship together.\n\nAcceptance (high level): `graphtor-docs install` in a fresh consumer workspace creates only `.graphtor/` + a minimal serve `.mcp.json`, no sources.yaml and no config/bin/cache/data/logs; dropping a .db into `.graphtor/` makes it queryable read-only (via 79B5A7BC); an explicit opt-in flag/command scaffolds the ingestion/generation layout; uninstall cleans up correctly; docs have a distinct ingestion/sync setup section; the graphtor dev workspace keeps full generation capability.","created_at":"2026-07-14T22:28:28.2477999Z","archived_at":"2026-07-15T01:54:14.0011528Z","reason":"harvested","harvested_artifact_id":"051-F"}
{"id":"2D49BDDF","priority":"high","kind":"task","text":"cmd_upgrade lacks the is_reparse_point containment guard that install/uninstall/serve now have. In src/main.rs cmd_upgrade acquires WorkspaceLock::acquire(workspace_dir, force_unlock) on a workspace resolved by find_workspace_dir, which matches .graphtor via candidate.is_dir() (follows symlinks/junctions). If .graphtor is a reparse point pointing outside the project, upgrade's lock acquire and --force-unlock create/remove/replace graphtor.lock inside the external target (out-of-workspace mutation), the same containment class fixed for install (W7-1) and uninstall (W7-2) on PR 90. Fix: add if graphtor_core::path::is_reparse_point(and ws_dir) bail before the acquire in cmd_upgrade, mirroring the install/uninstall guards, plus a regression test planting a symlinked .graphtor and asserting upgrade fails closed and the linked target lock is untouched. Source: PR 90 wave-7 Security Reviewer finding, confidence 0.72, P3 advisory. Deferred from 045-S because cmd_upgrade is out of the W7-1/W7-2 declared scope; prioritized high for immediate next-shipment attention because it is a security containment gap.","created_at":"2026-07-16T10:28:22.6113211Z","archived_at":"2026-07-16T17:42:36.3906529Z","reason":"harvested","harvested_artifact_id":"052.001-T"}
{"id":"A6C7EDB3","priority":"low","kind":"task","text":"Install-path symlink write symmetry (wave-5 security P3 advisory, out of 045-S uninstall/serve scope). The uninstall and read paths now reject a symlinked or junction .gitignore and validate MCP config candidates via is_reparse_point/validate_path, but the INSTALL write paths still follow a pre-existing symlink: add_gitignore_entry does fs read_to_string plus fs write on .gitignore, and generate_mcp_config/write_json write .mcp.json and can traverse a symlinked .vscode parent. If an attacker plants a symlink at .gitignore or .mcp.json before the developer runs install, install would read or overwrite the external target, escaping the workspace on the write path. Fix: gate the install writers with the same is_reparse_point/validate_path containment guard used on the uninstall and serve side, with regression tests. Source: PR 90 wave-5 Security Reviewer finding, confidence 0.63.","created_at":"2026-07-16T09:20:43.1819105Z","archived_at":"2026-07-16T17:42:37.0493371Z","reason":"harvested","harvested_artifact_id":"052.002-T"}
{"id":"E8F043DD","priority":"medium","kind":"feature","text":"Documentation refresh + brochure-level quick-start. Update all user- and contributor-facing documentation (README, docs/, ARCHITECTURE.md, configuration and usage guides) to accurately match the current functionality, architecture, and design intent as of the 0.3.1 codebase — covering the MCP tool surface, documentation source acquisition (git/local/web), the chunk -\u003e embed -\u003e index -\u003e serve pipeline, serve modes/posture, and recent reparse-point containment/security behavior. Additionally author a succinct \"brochure-level\" quick-start that leads with a clear value proposition (what graphtor-docs is, who it is for, and why it matters: local, private, indexed semantic cross-referencing of documentation served over MCP) and takes a new user from install to first successful query quickly. Keep the reference docs and the quick-start internally consistent and aligned with the actual code and MCP tool names.","created_at":"2026-07-16T19:47:06.4283215Z","archived_at":"2026-07-16T21:12:44.1286985Z","reason":"harvested","harvested_artifact_id":"053-F"}
