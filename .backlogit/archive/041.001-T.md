---
archived_from: .backlogit/queue/041.001-T.md
artifact_type: task
commit: 6b500a1079f7522e8ee269b0f5be4d2fb2dab3ad
created_at: 2026-05-26T17:30:00Z
custom_fields:
    source_stash_id: 25F91517
id: 041.001-T
labels:
    - test
    - config
    - harness-ready
parent_id: 041-F
priority: high
status: archived
title: Write unit tests for ensure_sources_stub helper
updated_at: 2026-05-28T16:06:15.0246857-07:00
---

Write unit tests for the `ensure_sources_stub` function in `src/config/mod.rs`:

1. Temp dir with a `.db` file and no config → stub created, returns `Some(path)`.
2. Temp dir with existing `sources.yaml` → no-op, returns `None`.
3. Temp dir with no `.db` → no-op, returns `None`.
4. Temp dir with `*.sources.yaml` pattern file → no-op, returns `None`.

## Acceptance Criteria

- Tests compile but fail (red phase) before implementation.
- Each scenario is an independent `#[test]` function.
- Uses `tempfile` crate for isolated filesystem.

## Execution Posture

test-first
