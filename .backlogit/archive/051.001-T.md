---
artifact_type: task
created_at: 2026-07-14T18:56:06.5631433-07:00
dependencies:
    - 051.004-T
id: 051.001-T
labels:
    - phase-2
    - install
    - rust
parent_id: 051-F
priority: medium
references:
    - docs/archive/plans/2026-08-24-pre-august-compaction/2026-07-14-consumption-first-graphtor-plan.md
    - docs/decisions/2026-07-14-consumption-first-graphtor-deliberation.md
status: done
title: 'P2-T1: Consumption-first install default'
updated_at: 2026-07-15T18:24:46.9225487-07:00
---

Add a NEW consumption-first install path, `install_minimal()`, that creates ONLY the `.graphtor/` root + a minimal serve `.mcp.json` written via the SHARED MCP config writer (P2-T3 / 051.004-T) - PATH command graphtor-docs, args ['serve'], with the managed provenance marker and atomic write. It writes NO sources.yaml; creates NONE of config/bin/cache/data/logs; and does NOT create/update .gitignore (a consumption-first minimal install has no managed .gitignore side effect; the .graphtor/ root is the operator's db-drop location). CRITICAL backward-compat (PR #88 review comment 3585938838 / thread 8-followon): PRESERVE the existing full-scaffold `install()` (src/workspace/install.rs:34) UNCHANGED - it still creates .graphtor/{bin,data,cache,config,logs} and copies the running binary - so internal callers (upgrade, --with-ingestion full install, init) keep working and the EXISTING upgrade tests stay green after this task. Do NOT repurpose or gut `install()`; ADD a sibling `install_minimal()` instead. cmd_install DEFAULT routes to install_minimal(); `install --with-ingestion` (P2-T2a/T2b) routes to the preserved install(). Represent the minimal 'no copied binary' case via `InstallResult.binary_path: Option<PathBuf>` (full install() returns Some(dest); install_minimal() returns None) or a dedicated minimal result - either way install()'s scaffold behaviour is unchanged. Update cmd_install message + the cmd_uninstall/CLI None (minimal-footprint) handling in main.rs to handle the None/minimal case — this task's in-scope uninstall work is ONLY the cmd_uninstall/CLI None or minimal-footprint plumbing in main.rs; the footprint-safe DELETION set (delete only graphtor-created artifacts, preserve user-dropped *.db) is owned by P2-T5a (051.005-T), NOT this task. Upgrade needs NO change because it still calls the preserved full install()/installed_binary_path. 045-S merges atomically (one merge commit for the whole shipment), so no released intermediate ever exposes a minimal install without its safe uninstall. Files: src/workspace/install.rs, src/main.rs (cmd_install:3002). Depends on P2-T3 (shared writer). Test-first.

<!-- BEGIN:acceptance-criteria -->
fresh default install (install_minimal()) creates ONLY .graphtor/ + a minimal .mcp.json (PATH command, no .exe, managed marker present, written via the shared P2-T3 writer); no sources.yaml; no ingestion subdirs (config/bin/cache/data/logs); .gitignore is NOT created or modified; the minimal 'no binary' case is representable (binary_path None or InstallKind). BACKWARD-COMPAT (must hold): the existing full-scaffold install() is PRESERVED and unchanged - the existing upgrade tests (e.g. upgrade_succeeds_after_install, src/workspace/upgrade.rs:94-101, which calls install() then upgrade()) REMAIN GREEN after this task, because the minimal path is a separate function upgrade does not call and install() still creates .graphtor/bin/.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Minimal .mcp.json generation + marker + atomic write are provided by P2-T3 (051.004-T) and CALLED here - this task asserts the resulting minimal install, it does not re-implement the writer. Backward-compat design (PR #88 review comment 3585938838 / thread 8-followon): the original plan MODIFIED install() to drop bin/, which breaks upgrade.rs (install() then upgrade() copies to a missing .graphtor/bin parent) and left the suite red until P2-T5c. The locked design instead ADDS install_minimal() and PRESERVES install(), so no task leaves the suite broken and the minimal-upgrade handling in P2-T5c (051.010-T) becomes purely ADDITIVE (a new no-op case), not a deferred regression fix. .gitignore decision (thread 8): the current cmd_install always manages .gitignore (src/main.rs:3021-3024); the minimal path SKIPS that side effect while the full path (P2-T2b) retains it. Update cmd_install message + uninstall to handle a None/minimal binary_path; upgrade is unchanged. Uninstall scope boundary: this task only threads the cmd_uninstall/CLI None or minimal-footprint handling in main.rs (recognising the None/minimal binary_path); the actual footprint-safe deletion logic is owned by P2-T5a (051.005-T). Because 045-S merges atomically as a single merge commit, no released intermediate exposes a minimal install without safe uninstall.
<!-- END:implementation-notes -->