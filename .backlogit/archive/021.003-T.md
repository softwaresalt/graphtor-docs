---
archived_from: .backlogit/queue/021.003-T.md
artifact_type: task
created_at: 2026-05-02T10:29:17.9278993-07:00
id: 021.003-T
labels:
    - config
    - validation
parent_id: 021-F
priority: high
references:
    - src/config/validation.rs
    - src/config/source.rs
status: archived
title: Add Format Validation to Config Validation
updated_at: 2026-05-02T11:59:41.4127519-07:00
---

Extend `src/config/validation.rs` to validate the `formats` field on every source entry. Reject unknown format strings (only `md`, `pdf`, `docx` are accepted). Reject empty format lists. Emit clear error messages identifying the offending source ID and invalid value.

<!-- BEGIN:acceptance-criteria -->
1. `validate()` rejects sources with unknown format strings (e.g., `"txt"`, `"html"`)
2. `validate()` rejects sources with an empty `formats` list
3. Valid formats (`md`, `pdf`, `docx`) pass validation
4. Error messages include the source ID and the invalid format value
5. `validate_sources()` in `src/acquire/plan.rs` also checks formats
6. Unit tests cover: valid formats, unknown format, empty list, mixed valid+invalid
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
1. Define `const VALID_FORMATS: &[&str] = &["md", "pdf", "docx"];` in validation.rs
2. Add a `validate_formats()` helper similar to `validate_globs()`
3. Call it from both `validate()` and `validate_sources()`
4. TDD: write failing tests first for unknown format, empty list
5. Files touched: `src/config/validation.rs`, `src/acquire/plan.rs`
6. Run quality gates after each change
<!-- END:implementation-notes -->