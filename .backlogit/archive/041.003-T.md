---
archived_from: .backlogit/queue/041.003-T.md
artifact_type: task
commit: 6b500a1079f7522e8ee269b0f5be4d2fb2dab3ad
created_at: 2026-05-26T17:30:00Z
custom_fields:
    source_stash_id: 25F91517
dependencies:
    - 041.002-T
id: 041.003-T
labels:
    - config
    - implementation
    - harness-ready
parent_id: 041-F
priority: high
status: archived
title: Integrate stub generation into load_source_config call path
updated_at: 2026-05-28T16:06:16.9094974-07:00
---

In `src/main.rs` (or `src/config/mod.rs` depending on where `load_source_config` lives),
insert a call to `ensure_sources_stub` before the `build_workspace_source_config` fallback:

1. Call `ensure_sources_stub(&config_dir, &data_dir)`.
2. If it returns `Some(path)`, parse the stub and return the resulting `SourceConfig`.
3. If it returns `None`, fall through to existing behavior.

Resolve `data_dir` as `.graphtor/data/` relative to cwd (consistent with existing `db_path` resolution).

## Acceptance Criteria

- `serve` on a workspace with imported DB and no config does NOT spawn background sync.
- `serve` logs an info message about auto-generated stub.
- `status --db-path` continues to work without changes.
- `cargo test` passes.

## Execution Posture

implementation
