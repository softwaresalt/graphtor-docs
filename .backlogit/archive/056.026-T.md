---
artifact_type: task
created_at: 2026-08-23T20:44:27.4319238Z
dependencies:
    - 056.019-T
id: 056.026-T
labels:
    - phase:remedy
    - cause:discriminator
    - selection:not-needed
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: done
title: Reconcile generated MCP config type/transport discriminator (unconditional)
updated_at: 2026-09-13T19:51:25.5344494Z
---

Unconditional generator discriminator reconciliation split out of 056.008-T. Whenever exact-CLI evidence (T0 / 056.019-T) shows a docs/generator `type` vs `transport` mismatch, reconcile the generated stdio discriminator in `managed_server_value` to the exact CLI-honored field and align exact legacy-shape recognition. Owns ONLY the `managed_server_value` discriminator field plus exact legacy-shape recognition and focused generation tests. Depends only on the completed evidence classification (056.019-T), not on 056.017/056.024/056.018. Closes not-needed when no mismatch is evidenced. Fresh-install generation only; existing-install delivery is the paired discriminator-delivery task.

<!-- BEGIN:acceptance-criteria -->
- DISPOSITION (2026-09-13, Stage remediation cycle 1; wording corrected cycle 2): CLOSED `not-needed: no type/transport mismatch evidenced`. No controlled exact-CLI evidence proves a functional `type`/`transport` mismatch. Authoritative evidence: docs/decisions/2026-08-29-mcp-serve-discover-preinitialize-evidence.md (E1) proves ONLY that a graphtor server process reached STDIO startup (preflight + embedding-model load complete; `starting MCP STDIO server` logged) before rmcp rejected a pre-`initialize` `server/discover` request and the process exited 2. E1 does NOT record the launching configuration or the discriminator field it carried, so E1 does NOT prove whether `type` or `transport` was honored, and it is NOT affirmative evidence for or against a launch/parse-blocking discriminator mismatch (the current `.mcp.json` shape is likewise not historical proof of what was honored). The evidenced regression cause is H3-A (server-side rmcp 1.5 rejecting a pre-`initialize` `server/discover` request), a server-side handshake failure. 056.019-T (H3-B terminal, archived/done) explicitly disclaims selecting this task absent independent `type`/`transport` mismatch evidence; the T2g plan makes selection conditional on T0 actual-client proof and states a static config-shape difference `does not prove Copilot honors the field`. Mismatch remains NOT PROVEN. Per this task own criterion, disposition = done/not-needed; evidence passed through unchanged. See docs/decisions/2026-09-13-stage-056.026-T-disposition-not-needed.md.
- Depend ONLY on 056.019-T (completed evidence classification); do NOT depend on 056.017-T/056.024-T/056.018-T.
- REACTIVATION CONTRACT (safe; applies ONLY if future exact-CLI evidence proves a mismatch): reconcile the emitted stdio discriminator in `managed_server_value` to the exact CLI-honored field via a SURGICAL edit ONLY in `managed_server_value` and the post-migration MARKED test fixture — NO blanket `transport`->`type` rename. Emit EXACTLY ONE discriminator key (never both `transport` and `type`). Own ONLY that discriminator field + `is_exact_legacy_shape` + focused generation tests; NOT the cwd field (056.008-T) or any mutation/recovery/upgrade delivery.
- REACTIVATION — already-installed refresh flows through the MARKER path, NOT the unmarked-legacy path: entries carrying the `x-graphtor-managed` marker are recognized discriminator-agnostically and auto-refresh from the old discriminator to the reconciled one on regeneration. Do NOT broaden the UNMARKED-legacy `is_exact_legacy_shape` recognizer to accept the new discriminator; it MUST continue to recognize ONLY the historical `transport` graphtor shape.
- REACTIVATION — fail-closed user-entry preservation (data-loss guard): `is_exact_legacy_shape` MUST retain BOTH its exact-key-count==3 invariant AND its narrow LEGACY_COMMAND_SHAPES command guard for every discriminator variant. A user-authored entry that is not an exact graphtor-authored legacy shape — e.g. a `{command:non-graphtor, args, type:stdio}` entry, or a 4-key entry carrying BOTH `transport` and `type` — MUST stay classified Collision (preserved): never migrated/overwritten on install and never pruned on uninstall.
- REACTIVATION — test-first red-then-green three groups: (1) discriminator reconciled to the CLI-honored field, exactly one key, new-field assertion authored to FAIL against the current `transport`-emitting code (red-first); (2) recognition + fail-closed preservation — a MARKED old-discriminator entry auto-refreshes once then is a byte-for-byte no-op on the second run (idempotence), a genuine unmarked pre-marker `transport` graphtor entry still migrates in place, while an unmarked user `type`-shaped/non-graphtor-command entry AND a 4-key both-discriminator entry each stay Collision; (3) containment/marker + exact unowned/non-JSON byte preservation.
- REACTIVATION — Rust 1.75, fmt, clippy pedantic, and targeted generation tests pass. The MSRV criterion is preserved UNCHANGED and NOT weakened; the fixed Rust 2021 / rust-version 1.75 floor is NON-NEGOTIABLE. Because this task is closed not-needed, no code ships and no MSRV gate runs now; the criterion binds any future reactivation.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Split from 056.008-T former unconditional discriminator part. DISPOSITION 2026-09-13 (wording corrected cycle 2): closed `not-needed` — no controlled exact-CLI `type`/`transport` functional mismatch is evidenced (H3-A server-side handshake is the evidenced cause). E1 proves only that a graphtor server process reached STDIO startup before rmcp rejected a pre-`initialize` `server/discover`; it does not record the launching configuration or discriminator field and does not prove whether `type` or `transport` was honored, so mismatch remains NOT PROVEN. Would-be sole owner of the generated `managed_server_value` discriminator field + `is_exact_legacy_shape` in `src/workspace/mcp_config.rs` IF ever reactivated. Safe-contract note (P1 data-loss guard, aligned 2026-09-13): marked (`x-graphtor-managed`) entries refresh discriminator-agnostically via the MARKER path; the UNMARKED-legacy `is_exact_legacy_shape` recognizer stays `transport`-only; user-authored `type`-shaped or 4-key both-discriminator entries stay Collision (never migrated/pruned). This supersedes the earlier unsafe `recognize BOTH pre- and post-reconciliation discriminator` phrasing. Size: S | Complexity: medium (prose; backlogit `task` WIT has no structured size/complexity fields — degraded per Stage sizing gate).
<!-- END:implementation-notes -->