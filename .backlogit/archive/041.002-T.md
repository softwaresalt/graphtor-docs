---
archived_from: .backlogit/queue/041.002-T.md
artifact_type: task
commit: 6b500a1079f7522e8ee269b0f5be4d2fb2dab3ad
created_at: 2026-05-26T17:30:00Z
custom_fields:
    source_stash_id: 25F91517
dependencies:
    - 041.001-T
id: 041.002-T
labels:
    - config
    - implementation
    - harness-ready
parent_id: 041-F
priority: high
status: archived
title: Implement ensure_sources_stub helper in config module
updated_at: 2026-05-28T16:06:15.5916033-07:00
---

Add `pub(crate) fn ensure_sources_stub(config_dir: &Path, data_dir: &Path) -> Result<Option<PathBuf>, GraphtorError>` to `src/config/mod.rs`:

1. Check if any `*.sources.yaml` or `sources.yaml` exists in `config_dir` — return `Ok(None)` if found.
2. Check if `data_dir` contains at least one `*.db` file — return `Ok(None)` if not.
3. Create `config_dir` (`std::fs::create_dir_all`) and write `sources: []\n` to `config_dir/sources.yaml`.
4. Log an info message about stub generation.
5. Return `Ok(Some(path))`.

## Acceptance Criteria

- Unit tests from 041.001-T pass (green phase).
- No `.unwrap()` or `.expect()` in implementation.
- `clippy --all-targets -- -D warnings -D clippy::pedantic` passes.
- Function is `pub(crate)` with `///` doc comment.

## Execution Posture

implementation (green phase)
