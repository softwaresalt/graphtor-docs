---
artifact_type: task
created_at: 2026-07-15T00:47:35.1984022-07:00
dependencies:
    - 051.002-T
    - 051.004-T
id: 051.008-T
labels:
    - phase-2
    - install
    - scaffold
    - rust
parent_id: 051-F
priority: medium
references:
    - docs/archive/plans/2026-08-24-pre-august-compaction/2026-07-14-consumption-first-graphtor-plan.md
    - docs/decisions/2026-07-14-consumption-first-graphtor-deliberation.md
status: done
title: 'P2-T2b: Opt-in full-ingestion scaffold + managed marker'
updated_at: 2026-07-15T18:49:33.7682574-07:00
---

When --with-ingestion is set (routed by P2-T2a / 051.002-T), complete the generation footprint via the FULL-PATH-ONLY install orchestration. Invoke the PRESERVED full-scaffold install() (src/workspace/install.rs:34 - already creates .graphtor/{bin,data,cache,config,logs} + copies the binary; unchanged by P2-T1) AND own the full-path post-install orchestration currently in `cmd_install` (src/main.rs:3017-3028): write the template sources.yaml (init_sources_yaml), manage the .gitignore block (unless --no-gitignore), and write .mcp.json via the shared MCP config writer (051.004-T / P2-T3) so the managed server entry uses the pinned, cwd-independent ABSOLUTE `<canonical_project_root>/.graphtor/bin/graphtor-docs[.exe]` command (computed from the canonical project root at install time; the relative `.graphtor/bin/...` string is reserved ONLY for legacy exact-match recognition, never the going-forward managed value) AND carries the managed-entry provenance marker. These sources.yaml / .gitignore / .mcp.json calls MUST be guarded to the --with-ingestion path so the consumption-first DEFAULT (install_minimal(), P2-T1) never runs them. P2-T2a stays routing-only (flag + branch selection, no scaffold behaviour). The graphtor dev workspace uses this full path. Files: src/workspace/install.rs, src/main.rs (cmd_install full-path orchestration; still < 3 files). Depends on P2-T2a (flag) + P2-T3 (shared writer). Test-first.

<!-- BEGIN:acceptance-criteria -->
--with-ingestion produces the full layout (via the preserved install()) + template sources.yaml + copied binary; the written .mcp.json server entry uses the pinned, cwd-independent ABSOLUTE `<canonical_project_root>/.graphtor/bin/graphtor-docs[.exe]` command (the relative `.graphtor/bin/graphtor-docs` string is reserved ONLY for legacy exact-match recognition) AND contains the managed-entry provenance marker (written through the shared P2-T3 writer); the managed .gitignore block is written (unless --no-gitignore).
The full-path sources.yaml / .gitignore / .mcp.json orchestration (currently UNCONDITIONAL at cmd_install src/main.rs:3017-3028) runs ONLY on the --with-ingestion path.
default (no flag) routes to install_minimal() (P2-T1) and creates NONE of these: no sources.yaml, no managed .gitignore, and only the minimal serve .mcp.json.
P2-T2a (051.002-T) implements no scaffold behaviour (routing only); this task owns every full-path orchestration call.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Scaffold + full-path orchestration; CLI flag/plumbing is P2-T2a (051.002-T, routing only). The existing sources.yaml (init_sources_yaml), managed .gitignore, and MCP-writer calls all live in `cmd_install` (src/main.rs:3017-3028) and today run UNCONDITIONALLY; this task relocates/guards them into the --with-ingestion branch so the minimal default (install_minimal(), P2-T1) skips them (PR #88 comments 3588876203 / 3588876245). Adding src/main.rs keeps the task at 2 files (install.rs + main.rs), under the < 3 rule, and resolves the boundary conflict where neither T2a (routing-only) nor a one-file T2b could own the cmd_install orchestration. Builds on the PRESERVED full install() (P2-T1 keeps install() intact per the backward-compat design) rather than re-creating the scaffold. Binary-resolution ladder + provenance marker + atomic write are owned by P2-T3 (051.004-T) and consumed here so uninstall (P2-T5b) can identify the managed entry. INV: marker parity across the minimal (install_minimal()) and full (install()) install paths.
<!-- END:implementation-notes -->