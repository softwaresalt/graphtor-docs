---
artifact_type: subtask
created_at: 2026-08-17T06:47:07.2466828Z
id: 054.001.001-ST
labels:
    - dark-mode
    - read-only-serve
    - test
    - pr90
    - red-first
    - post-cap-residual
parent_id: 054.001-T
references:
    - docs/exec-plans/2026-08-16-readonly-serve-guarantee-hardening-plan.md
status: done
title: Add RED-first contract test for qualified read-only startup-log wording
updated_at: 2026-08-17T07:54:53.7635446Z
---

Post-cap residual decomposition (finding C): existing characterization tests pin CURRENT behavior and are green, so they are NOT red for the NEW qualified startup-log wording — Constitution Principle II requires a genuinely failing test for NEW behavior. This is the TEST half and MUST land red BEFORE the implementation subtask (054.001.002-ST).

Add a NEW test asserting the CORRECTED, qualified read-only startup-log line emitted by open_engine_readonly (src/db/store.rs:209-212, currently 'opened engine-enforced read-only SQLite DataStore (filesystem lock active)'). The new wording must state the guarantee precisely: robust while a single owning DataStore holds the guard; best-effort whenever the same file is independently guarded more than once (same- or cross-process = F6). Because this wording does not yet exist, the test MUST be observed FAILING (red) against current code before 054.001.002-ST implements it.

Width isolation: TEST code only (a capturing seam for the log line — e.g. a scoped tracing-subscriber capture — colocated in the src/db/store.rs test module). Do NOT change the production log text or any rustdoc here; that is 054.001.002-ST.

Acceptance:
1) A NEW test asserts the exact NEW qualified startup-log wording for open_engine_readonly (single-owner robust / concurrent- or cross-process best-effort / F6).
2) The test is observed FAILING (red) against the current 'filesystem lock active' wording, and that red observation is recorded (Principle II).
3) The test is DISTINCT from the existing characterization tests (which stay green pinning current honest invariants) — not a rename of an existing assertion.
4) No production wording or rustdoc changed in this subtask (test-only).
5) The new test is red now (goes green under 054.001.002-ST); cargo clippy --all-targets -- -D warnings -D clippy::pedantic green on the test code.

<!-- BEGIN:implementation-notes -->
RED-FIRST (test-first) per Constitution Principle II. Log-line assertions need a capture seam — prefer a scoped tracing-subscriber capture (e.g. tracing-test or a custom Layer) over parsing stdout. The wording asserted here MUST match the corrected in-code rustdoc/log that 054.001.002-ST writes and the design-doc wording 054.002-T writes, so agree the exact string first. Sibling of 054.001.002-ST (impl) and 054.001.003-ST (release-observability evidence).
<!-- END:implementation-notes -->