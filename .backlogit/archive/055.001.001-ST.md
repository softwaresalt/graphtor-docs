---
artifact_type: subtask
created_at: 2026-08-17T06:45:14.7890501Z
id: 055.001.001-ST
labels:
    - dark-mode
    - serve-discovery
    - code
    - pr90
    - library-api
    - post-cap-residual
parent_id: 055.001-T
references:
    - docs/exec-plans/2026-08-16-serve-auto-discovery-followups-plan.md
status: done
title: Add graphtor_core::acquire FileFilter additive API; refactor filter_files
updated_at: 2026-08-17T20:03:09.2568295Z
---

Post-cap residual decomposition (finding E: parent 055.001-T exceeds the 2-hour / width heuristic — it spans a library additive API AND a binary classifier refactor). This is the LIBRARY half and MUST land first; the binary streaming subtask (055.001.002-ST) depends on it across the crate boundary.

Expose the shared compiled include/exclude matcher that graphtor_core::acquire::filter_files already uses as an ADDITIVE public API in the graphtor_core LIBRARY crate — e.g. a FileFilter type with new(...) + is_match(&Path) -> bool — carrying /// rustdoc and at least one colocated library-crate unit test. Refactor filter_files to consume FileFilter as the single source of truth for glob include/exclude matching (no forked or duplicated glob compilation). Additive, SemVer-minor, no breaking change to any existing public signature. The graphtor-docs BINARY classifier (source_has_ingestible_content) will reuse this exact matcher in 055.001.002-ST instead of calling filter_files per file.

Width isolation: graphtor_core LIBRARY crate ONLY (public API + filter_files refactor + library unit tests). Do NOT touch src/workspace/serve_discovery.rs — that streaming refactor is 055.001.002-ST.

Acceptance:
1) FileFilter additive public API (new/is_match or equivalent) in graphtor_core::acquire with /// rustdoc and >=1 colocated library-crate unit test.
2) filter_files refactored to consume FileFilter — single source of truth, no duplicated/forked glob compilation.
3) Existing filter_files selection is byte-for-byte unchanged: all pre-existing filter_files tests stay green (characterization).
4) SemVer-minor additive: no existing public signature removed or narrowed; no breaking change.
5) cargo test + cargo clippy --all-targets -- -D warnings -D clippy::pedantic green.

This subtask carries 055.001-T acceptance criterion AC7 (the additive library API) verbatim; the classifier streaming + warning-parity criteria move to sibling 055.001.002-ST.

<!-- BEGIN:implementation-notes -->
RED-FIRST for the NEW FileFilter public API per Constitution Principle II: it has NO prior behavior to characterize, so write the library unit tests, observe them FAIL (red), then implement. Characterization-first applies only to the filter_files refactor (pin existing selection before refactoring). Compile the include/exclude matcher ONCE and reuse it via is_match — do not recompile glob sets per call. Crate boundary: classifier is in the graphtor-docs binary, matcher/filter_files are in the graphtor_core library, which is why the shared predicate must be public + additive.
<!-- END:implementation-notes -->