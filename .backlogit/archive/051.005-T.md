---
artifact_type: task
created_at: 2026-07-14T18:56:08.2047343-07:00
dependencies:
    - 051.001-T
    - 051.008-T
id: 051.005-T
labels:
    - phase-2
    - uninstall
    - security
    - rust
parent_id: 051-F
priority: medium
references:
    - docs/archive/plans/2026-08-24-pre-august-compaction/2026-07-14-consumption-first-graphtor-plan.md
    - docs/decisions/2026-07-14-consumption-first-graphtor-deliberation.md
status: done
title: 'P2-T5a: Footprint-safe uninstall + approval-set enumeration'
updated_at: 2026-07-15T19:32:20.6353893-07:00
---

uninstall() (src/workspace/uninstall.rs:34) removes ONLY graphtor-CREATED filesystem artifacts (the known subdirs of a full install) and MUST NEVER delete user-dropped *.db files in the .graphtor/ root; it MUST NOT follow symlinks out of the root. The footprint rewrite MUST PRESERVE the existing PUBLIC `uninstall(project_root, keep_config)` contract: `keep_config == true` retains `.graphtor/config` (existing behaviour + dedicated test at src/workspace/uninstall.rs:34-77,112-124) - the new graphtor-created-artifact allowlist MUST keep `config/` when `keep_config` is set while still deleting the other managed subdirs and retaining user-dropped `*.db` (PR #88 thread PRRT_kwDORiB5E86RNyVa). Do NOT drop the `keep_config` parameter or its semantics during the rewrite. .gitignore parity (thread 8): a minimal-install uninstall MUST NOT remove a .gitignore it never created; only a full-footprint uninstall touches the managed .gitignore block it wrote. The operator-approval prompt (PA-3) enumerates the exact deletion set before any deletion. Managed .mcp.json entry removal is P2-T5b (051.009-T); minimal/full upgrade parity is P2-T5c (051.010-T). Files: src/workspace/uninstall.rs, src/main.rs (cmd_uninstall PA-3 prompt). Depends on P2-T1 (minimal) + P2-T2b (full scaffold). Test-first.

<!-- BEGIN:acceptance-criteria -->
Three high-level verification scenarios (all substantive checks retained):

(a) footprint-scoped uninstall (minimal + full, keep_config parity) — a minimal install -> uninstall deletes no user-dropped .db and does NOT remove a .gitignore it did not create; a full install -> uninstall with `keep_config == false` removes the full graphtor subdirs and the managed .gitignore block it wrote; a full install -> uninstall with `keep_config == true` removes the managed subdirs BUT PRESERVES `.graphtor/config` (the existing --keep-config contract), while BOTH keep_config values still RETAIN user-dropped `*.db` in the `.graphtor/` root.

(b) safety / data preservation — a user-dropped .db in .graphtor/ SURVIVES uninstall (minimal and full), and no symlink is followed out of .graphtor/.

(c) approval enumeration — the PA-3 approval prompt enumerates the exact deletion set before any deletion.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Split from the original P2-T5 (thread 10: spanned uninstall.rs + mcp_config.rs + main.rs + upgrade.rs). This task = filesystem cleanup + approval enumeration only. PA-3 is a destructive action requiring operator approval at execution (Ship, not Stage). Deletion scoped to graphtor-created artifacts only; the consumption footprint IS the user's db-drop location. The public `--keep-config` flag maps to `uninstall(project_root, keep_config)`; the allowlist-based footprint rewrite MUST branch on `keep_config` to SKIP `config/` deletion when set, preserving the existing dedicated test (uninstall.rs:112-124). Add full-install `keep_config=true` AND `keep_config=false` scenarios alongside the user-db-survival assertion so the new allowlist preserves config when requested while still retaining user databases.
<!-- END:implementation-notes -->