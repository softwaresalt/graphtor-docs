---
artifact_type: task
created_at: 2026-07-16T10:42:36.373466-07:00
custom_fields:
    source_stash_id: 2D49BDDF
    source_stash_kind: task
    source_stash_path: stash.jsonl
    source_stash_priority: high
    source_stash_text: 'cmd_upgrade lacks the is_reparse_point containment guard that install/uninstall/serve now have. In src/main.rs cmd_upgrade acquires WorkspaceLock::acquire(workspace_dir, force_unlock) on a workspace resolved by find_workspace_dir, which matches .graphtor via candidate.is_dir() (follows symlinks/junctions). If .graphtor is a reparse point pointing outside the project, upgrade''s lock acquire and --force-unlock create/remove/replace graphtor.lock inside the external target (out-of-workspace mutation), the same containment class fixed for install (W7-1) and uninstall (W7-2) on PR 90. Fix: add if graphtor_core::path::is_reparse_point(and ws_dir) bail before the acquire in cmd_upgrade, mirroring the install/uninstall guards, plus a regression test planting a symlinked .graphtor and asserting upgrade fails closed and the linked target lock is untouched. Source: PR 90 wave-7 Security Reviewer finding, confidence 0.72, P3 advisory. Deferred from 045-S because cmd_upgrade is out of the W7-1/W7-2 declared scope; prioritized high for immediate next-shipment attention because it is a security containment gap.'
id: 052.001-T
parent_id: 052-F
priority: high
status: done
title: Guard cmd_upgrade against symlinked .graphtor root
updated_at: 2026-07-16T10:42:36.373466-07:00
---

Add is_reparse_point guard in cmd_upgrade (src/main.rs) before WorkspaceLock::acquire, mirroring the install (W7-1)/uninstall (W7-2) guards, plus a regression test asserting upgrade fails closed on a symlinked .graphtor root and the linked target lock is untouched. Implemented in commit a47f9ef (PR #93).