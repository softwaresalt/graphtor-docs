{"id":"A6C7EDB3","priority":"low","kind":"task","text":"Install-path symlink write symmetry (wave-5 security P3 advisory, out of 045-S uninstall/serve scope). The uninstall and read paths now reject a symlinked or junction .gitignore and validate MCP config candidates via is_reparse_point/validate_path, but the INSTALL write paths still follow a pre-existing symlink: add_gitignore_entry does fs read_to_string plus fs write on .gitignore, and generate_mcp_config/write_json write .mcp.json and can traverse a symlinked .vscode parent. If an attacker plants a symlink at .gitignore or .mcp.json before the developer runs install, install would read or overwrite the external target, escaping the workspace on the write path. Fix: gate the install writers with the same is_reparse_point/validate_path containment guard used on the uninstall and serve side, with regression tests. Source: PR 90 wave-5 Security Reviewer finding, confidence 0.63.","created_at":"2026-07-16T09:20:43.1819105Z"}
