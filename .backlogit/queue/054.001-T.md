---
artifact_type: task
created_at: 2026-08-17T03:50:05.4852783Z
id: 054.001-T
labels:
    - dark-mode
    - read-only-serve
    - code
    - pr90
parent_id: 054-F
priority: medium
references:
    - docs/exec-plans/2026-08-16-readonly-serve-guarantee-hardening-plan.md
status: queued
title: Make read-only serve contract honest across all surfaces
updated_at: 2026-08-17T03:50:05.4852783Z
---

Correct the overstated read-only guarantee across all in-code contract surfaces WITHOUT changing guard runtime behavior. Keep is_engine_enforced_readonly() == self.engine_readonly_guard.is_some() (do NOT overload to access_mode==ReadOnly; open_sqlite_readonly is ReadOnly with guard None and must keep reporting false). Repository-wide sweep of overstated read-only claims: is_engine_enforced_readonly rustdoc, open_engine_readonly rustdoc (src/db/store.rs:197), open_sqlite_readonly rustdoc (:138), EngineReadonlyGuard field/type docs, the "filesystem lock active" startup log, and any read-only wording in the serve entry point (main.rs open_serve_databases). State the guarantee precisely: robust while a single owning DataStore holds the guard; best-effort whenever the same file is independently guarded more than once (same- or cross-process = F6). Investigate-first: add a separate app-level is_read_only() (access_mode==ReadOnly) predicate only if a caller demonstrably needs it; else add nothing. Files: src/db/store.rs (+ main.rs only if the sweep finds overstated wording there).

<!-- BEGIN:acceptance-criteria -->
1) is_engine_enforced_readonly() still returns guard.is_some() (NOT overloaded). 2) Colocated characterization tests pass: open_engine_readonly reports true; open_sqlite_readonly reports false; AccessMode::ReadOnly refuses mutate; existing single-owner byte-identical read-cycle unchanged; no cross-process closure asserted. 3) All enumerated surfaces corrected; grep sweep finds no remaining unconditional OS-level read-only claim. 4) Call-site list of is_engine_enforced_readonly() attached confirming no caller depended on a changed meaning. 5) cargo test + clippy pedantic green.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Characterization-first (TDD): pin honest invariants, then correct doc/log text so code and contract agree. No edit to EngineReadonlyGuard::lock/Drop. Reuse existing open_engine_readonly_* test scaffolding at src/db/store.rs:823+.
<!-- END:implementation-notes -->