---
artifact_type: task
created_at: 2026-08-25T22:45:49Z
custom_fields: {}
dependencies:
    - 059.003-T
    - 059.004-T
    - 059.006-T
id: 059.010-T
labels:
    - security
    - tests
    - test-first
    - filesystem
    - store
parent_id: 059-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-store-toctou-nofollow-handle-plan.md
    - docs/decisions/2026-08-24-store-toctou-nofollow-handle-deliberation.md
status: queued
title: 'U10: Windows retained-handle engine non-interference + broader non-name-surrogate reparse regression (tests)'
updated_at: 2026-08-30T03:55:16.2510037Z
---

Add colocated tests for the Windows handle-mode and broader-policy DELTAS split out of U5 (059.005-T). Bounded to at most two test scenarios: (d) validate that the Windows retained handle (FILE_READ_ATTRIBUTES|FILE_WRITE_ATTRIBUTES, full share mode FILE_SHARE_READ|FILE_SHARE_WRITE|FILE_SHARE_DELETE) does NOT block the engine's own db/WAL open -- scoped to that single assertion (Option A retained-handle vs the documented Option C identity-verified re-open fallback decision; record the outcome in the PR); (e) broader-fail-closed-policy regression (Windows): create a legitimate NON-redirecting reparse file whose reparse tag is NOT a name surrogate (a non-name-surrogate reparse point creatable without the symlink privilege where feasible) and assert the guarded open_no_follow path REFUSES it, proving the explicitly broader fail-closed policy adopted in U2 rather than a narrow name-surrogate-only check, so the intentional Windows/Unix breadth asymmetry is pinned by an integration test rather than left implicit. Skip GRACEFULLY (skip, not fail) when the test setup lacks the required privilege/support to create such a reparse file on unprivileged CI; in that case the always-on deterministic predicate unit test in U11 (059.011-T) remains the recorded guarantee and the doc-comment's accepted-breadth statement stands as the recorded behavior. Report executed-vs-skipped. Delta (e) here is OPTIONAL integration coverage layered on top of U11's always-on deterministic predicate test. Reference (do not re-implement) U2's should_refuse_reparse predicate; this task depends on U3, U4, and U6. Engine-open coverage is the DEFERRED accepted engine-open residual (U9, later separate shipment) and is NOT a completion dependency here — delta (d) still asserts that the retained permission handle's share/access mode does not block the engine's own db/WAL open (an unchanged, non-U9-gated non-interference assertion).

<!-- BEGIN:acceptance-criteria -->
- Delta (d) engine-open non-interference assertion passes, confirming the retained handle share/access mode (attribute access plus full share mode) does not block the engine db/WAL open; the Windows handle-mode decision (Option A retained-handle vs Option C identity-verified re-open) is recorded in the PR.
- Delta (e) broader-fail-closed regression: a legitimate non-name-surrogate / non-redirecting reparse file is REFUSED by open_no_follow (OPTIONAL integration coverage layered on U11's deterministic predicate test); it skips gracefully (skip, not fail) when the setup lacks the privilege/support to create the reparse file, and whether it executed or skipped is reported.
- Bounded to exactly these two test scenarios ((d)-(e)). The always-on deterministic guarantee for the broader fail-closed policy is owned by U11 (059.011-T) and only referenced here.
- Engine-open containment is the DEFERRED accepted engine-open residual (U9, later separate shipment), NOT part of this near-term test scope.
- Passes clippy::pedantic -D warnings; no unwrap/expect; safe Rust 1.75; #![forbid(unsafe_code)] preserved.
<!-- END:acceptance-criteria -->
