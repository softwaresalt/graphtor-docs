---
artifact_type: task
created_at: 2026-07-15T09:26:33.37681-07:00
dependencies:
    - 050.012-T
id: 050.013-T
labels:
    - phase-1
    - migration
    - refactor
    - rust
parent_id: 050-F
priority: medium
references:
    - docs/exec-plans/2026-07-14-consumption-first-graphtor-plan.md
    - docs/decisions/2026-07-14-consumption-first-graphtor-deliberation.md
status: queued
title: 'P1-RF4: v4-migration source-helper variant-safe refactor'
updated_at: 2026-07-15T09:26:33.37681-07:00
---

P1-RF4 (Source-variant compatibility pre-refactor 4 of 4). Refactor the two irrefutable `let Source::Local(...)` bindings in the v4-migration source helpers so they are variant-safe BEFORE the `type: database` variant lands, closing the last EXTERNAL irrefutable consumer. Sites: `source_id` (src/main.rs ~L655) and `changed_source_fields` (src/main.rs ~L661). Route through the existing `Source::id()` accessor and `Source::as_local()` (P1-RF1 / 050.010-T); while only `Local` exists behaviour is identical, so this task is independently green. NO new variant is added here (that is P1-T6). Files: src/main.rs (1 file). Depends on 050.012-T (P1-RF3). Test-first.

<!-- BEGIN:acceptance-criteria -->
`source_id` returns the id for a local source identically (delegates to `Source::id()`).
`changed_source_fields` computes the same changed-field set for two local sources.
A non-local (future `Database`) source is handled variant-safely (no panic; defensive empty/default) — fully exercised once P1-T6 adds the variant.
Existing migration tests stay green; NO `Source::Database` variant exists yet (independently green).
After this task, ZERO external `let Source::Local(...)` irrefutable bindings remain (config/acquire/pipeline/sync/main all routed through accessors), so 050.006-T adds `Database(DatabaseSource)` with only src/config/source.rs match-arm updates.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
`source_id` (src/main.rs ~L655): delegate to the existing `Source::id()` accessor (its match is centralized in source.rs) instead of destructuring.
`changed_source_fields` (~L661): replace `let (Source::Local(current), Source::Local(persisted)) = (current, persisted);` with `as_local()` on both operands, returning an empty/defensive changed-field set when either is non-local — a served db is not part of the v4 generation snapshot.
While only `Local` exists behaviour is identical. This is the final variant-safe pre-refactor; it makes the enum-consumer surface fully variant-safe so the additive variant in 050.006-T (P1-T6) cannot break external compilation. Pure structural refactor; gates P1-T6. Executed by Ship; Stage plans only.
<!-- END:implementation-notes -->