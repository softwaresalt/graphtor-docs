---
artifact_type: task
created_at: 2026-08-21T22:49:23.6020419Z
dependencies:
    - 056.008-T
id: 056.009-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Deliver managed launch contract to existing installs (H0a or H3-B1 conditional)
updated_at: 2026-08-22T20:09:00.4624134Z
---



<!-- BEGIN:acceptance-criteria -->
- Run only when 056.008-T is selected. Otherwise move to `done` with `not-needed: 056.008-T not selected`.
- Deliver the defined managed value via `cmd_upgrade`, deriving project root from the located `.graphtor` parent. `graphtor-docs sync`, not upgrade, remains the pre-v4 index rebuild.
- Before any owned-entry mutation that changes bytes, atomically save the original `.mcp.json` bytes under `.graphtor/recovery/` using an exclusive, collision-resistant timestamp-plus-random name. Preserve or tighten source ACL/permissions, report the path, and never overwrite an existing recovery artifact. Backup failure aborts config mutation; no backup for an idempotent no-change run.
- Keep the recovery artifact through T4. Documented cleanup is operator-controlled after the observation window; rollback restores the exact bytes from it.
- Refresh marked/exact-legacy entries. Preserve unowned/non-JSON bytes with an actionable warning/manual reinstall path. Fail closed on reparse/containment errors before replacement where possible; otherwise report explicit partial-upgrade state.
- Observed-red matrix covers marked/legacy refresh, co-resident user bytes, nested invocation, preserved collisions/non-JSON, backup/permission failure, and security failure. Split before code if this exceeds task-width limits.
- Reinstall is fallback/rollback, not automated acceptance. Operator documentation is isolated in 056.013-T.
- Moderate strict-safety action, marker-gated and backup-first.
- fmt + clippy pedantic + tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Conditional existing-install delivery for H0a/H3-B1. Refresh the owned managed entry during `cmd_upgrade` from the canonical project root, creating a secure collision-resistant recovery artifact before mutation. Preserve unowned config; 056.013-T owns documentation.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
`workspace::upgrade::upgrade` is currently binary-only; `cmd_upgrade` orchestrates the owned config refresh without assigning schema rebuild to upgrade. Complete config security preflight and recovery-artifact creation before replacement where possible, and report partial state explicitly.
<!-- END:implementation-notes -->