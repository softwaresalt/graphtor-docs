---
artifact_type: task
created_at: 2026-08-21T22:49:23.6020419Z
dependencies:
    - 056.018-T
    - 056.008-T
id: 056.009-T
labels:
    - phase:remedy
    - cause:managed-config
    - selection:pending
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Deliver managed launch contract to existing installs (H0a or H3-B1 conditional)
updated_at: 2026-08-23T20:52:33.5572902Z
---



<!-- BEGIN:acceptance-criteria -->
- Run only when 056.008-T is selected; otherwise move to `done` with `not-needed: 056.008-T not selected`. This task delivers ONLY the cwd/recovery managed-launch contract; discriminator existing-install delivery is 056.027-T.
- Depend on 056.008-T (cwd value) and 056.018-T (recovery) in the corrected DAG. Consume 056.017-T typed outcomes and compose 056.018-T recovery handles in `cmd_upgrade`; 056.018-T does not edit managed-config APIs. Derive project root from the located `.graphtor` parent and refresh only marked/exact-legacy entries. `graphtor-docs sync`, never upgrade, remains the pre-v4 rebuild.
- Extend text/JSON upgrade output with typed `mcp_config` action, recovery path when bytes changed, and collision/warning state; no message matching.
- Decide Minimal footprint explicitly and return typed not-applicable unless the managed executable contract is valid.
- Write and observe red tests here, then green exactly three groups: nested marked/legacy refresh with recovery metadata, no-change/collision/non-JSON preservation without backup, and recovery/mutation failure with original bytes intact.
- Before T4 on a managed branch, run the delivered upgrade path against the target workspace under the applicable approval, record the post-refresh production config hash, and retain the recovery artifact. Reinstall is fallback only.
- T4 exclusively owns production-entry `/mcp show` acceptance. Rust 1.75 and all gates pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Conditional existing-install orchestration for the cwd/recovery managed-launch contract. Compose typed config actions (056.017-T) and contained recovery handles (056.018-T) in `cmd_upgrade`, refresh the target workspace's marked/legacy managed entry with the pinned `cwd`, and expose text/JSON results before T4. Corrected DAG: depends on both 056.008-T (cwd value) and 056.018-T (recovery). Discriminator existing-install delivery is width-separated to 056.027-T.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
`workspace::upgrade::upgrade` remains binary-only. `cmd_upgrade` is the sole orchestration owner for the cwd/recovery refresh; 056.017-T owns config API signatures, 056.018-T owns recovery handles, 056.027-T owns the width-separated discriminator-only refresh, and 056.013-T owns docs. Corrected DAG: depends on 056.008-T (cwd generation) and 056.018-T (recovery).
<!-- END:implementation-notes -->