---
artifact_type: task
created_at: 2026-08-21T22:49:23.6020419Z
dependencies:
    - 056.008-T
id: 056.009-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: deliver managed launch contract to existing installs (H0a only)'
updated_at: 2026-08-22T00:35:00.0000000Z
---



<!-- BEGIN:acceptance-criteria -->
- CONDITIONAL - H0a-only. If T0 evidences H0b, H1, or a fail-closed gate (H0c) instead of H0a, close as not-needed with a one-line rationale; closing as not-needed satisfies 056.004-T's dependency on it.
- Distinct width from 056.008-T: that task changes the generated managed .mcp.json VALUE; THIS task DELIVERS the refreshed managed entry to ALREADY-INSTALLED workspaces. ~2h single install/upgrade-delivery width.
- Verified (Engram + exact reads): generate_mcp_config runs ONLY from cmd_install (src/main.rs ~3258) and cmd_install_full (~3360); cmd_upgrade (~3480-3538) calls workspace::upgrade::upgrade, which never rewrites .mcp.json. A binary upgrade therefore leaves an already-installed managed entry STALE.
- PRIMARY code acceptance (S1): wire the idempotent, marker-safe generate_mcp_config refresh into cmd_upgrade so `graphtor-docs upgrade` refreshes the managed entry in place. Its four-way decision only touches the marked/exact-legacy managed entry and leaves user-authored entries byte-for-byte unchanged (a case-4 collision returns Config error, never a clobber). This automated refresh, proven by an observed-red migration test, is THE acceptance for this task.
- Reinstall is an explicit MANUAL fallback/rollback ONLY: a documented `graphtor-docs install`/reinstall recipe with a verification step recorded in 056.001-T, for cases where the automatic upgrade refresh is judged unsafe or must be reverted. Reinstall does NOT substitute for the automated red/green migration test and is NOT an alternative that pretends to satisfy the code acceptance.
- Test-first proof / observed-red migration test (this width's code acceptance): an existing marked (or exact legacy-shape) managed entry is UPDATED to the new launch contract after cmd_upgrade runs, AND a co-resident user-authored server entry is preserved byte-for-byte. Observed RED before the cmd_upgrade refresh change, GREEN after.
- Safety (strict-safety, S7): ProposedAction = wire the marker-safe generate_mcp_config refresh into cmd_upgrade; ActionRisk = moderate (rewrites an existing user config file, but marker/exact-legacy gated so user-authored entries are never clobbered); rollback = revert the 056.009 commit and/or regenerate the entry; approval_required = no (non-destructive, idempotent). Recorded in the plan's Risky actions section.
- The bug reporter's already-installed workspace must be repairable; fresh installs alone are insufficient.
- fmt + clippy pedantic (-D warnings) + tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
CONDITIONAL on T0 evidencing H0a. Isolated out of 056.008-T (which changes the generated managed .mcp.json value) so this task owns DELIVERY of the refreshed launch contract to already-installed workspaces. Because generate_mcp_config runs only from install/install_full and cmd_upgrade never rewrites .mcp.json, a binary upgrade leaves the bug reporter's existing managed entry stale. PRIMARY code acceptance (S1): wire the idempotent, marker-safe generate_mcp_config refresh into cmd_upgrade, preserving any user-authored graphtor-docs entry byte-for-byte, proven by an observed-red migration test (existing managed entry updated, co-resident user entry preserved). Reinstall (`graphtor-docs install`) is an explicit MANUAL fallback/rollback recipe recorded in 056.001-T — it does NOT substitute for the automated red/green migration test. Close as not-needed if H0a is not evidenced.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Surfaces: src/main.rs::cmd_upgrade (~3480-3538) -> workspace::upgrade::upgrade (src/workspace/upgrade.rs, no .mcp.json touch today); src/workspace/mcp_config.rs::generate_mcp_config (~138-227) four-way decision: absent -> insert; marked -> refresh in place (Ok(None) if identical); exact legacy pre-marker shape -> migrate+refresh; unmarked other shape -> Config collision, file unchanged. Prefer the automatic cmd_upgrade refresh because it is idempotent and preserves user entries. Do NOT add parent-directory traversal. Depends on 056.008-T (the new contract value must exist before it can be delivered).
<!-- END:implementation-notes -->