---
artifact_type: task
created_at: 2026-08-21T22:49:23.6020419Z
dependencies:
    - 056.008-T
id: 056.009-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: deliver managed launch contract to existing installs (H0a only)'
updated_at: 2026-08-22T07:59:59.7652898Z
---



<!-- BEGIN:acceptance-criteria -->
- Run only when 056.008-T is selected (H0a or H3-B1). If 056.008-T closes not-needed, this task also closes not-needed, including H3-B2 and all non-cwd branches.
- Distinct width: deliver the already-defined managed value to existing installs via `cmd_upgrade`.
- Derive canonical project root from the located `.graphtor` parent, never nested invocation cwd or `.graphtor` itself.
- Refresh marked/exact-legacy entries idempotently. Preserve unowned/non-JSON config bytes and emit an actionable warning/manual reinstall path without failing an otherwise successful binary upgrade. Treat reparse/containment failures as security errors: fail before replacement where possible, otherwise report explicit partial-upgrade state; never silently succeed.
- Observed-red migration matrix: marked/legacy entry refreshes; co-resident user entry stays byte-for-byte; nested-subdirectory invocation updates only project-root `.mcp.json`; unowned collision/non-JSON is preserved with warning; reparse/containment failure is fail-closed and explicit.
- Reinstall is fallback/rollback, not a substitute for automated refresh.
- Moderate strict-safety action, idempotent and marker-gated.
- fmt + clippy pedantic + tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Conditional delivery of the 056.008-T managed launch contract to already-installed workspaces. Wire marker-safe refresh into `cmd_upgrade` using the canonical project root derived from the located `.graphtor` parent. Run for H0a or H3-B1; close whenever 056.008-T closes. Preserve unowned/non-JSON config with an actionable warning, fail closed on reparse/containment violations, and test nested-subdirectory invocation without stray config writes.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
`generate_mcp_config` currently runs only during install; `workspace::upgrade::upgrade` is binary-only. Define refresh ordering so security preflight happens before binary replacement where possible. Marked/legacy entries refresh; unowned config is preserved and does not turn a binary upgrade into an opaque failure; security violations remain hard, explicit failures. Depends on 056.008-T.
<!-- END:implementation-notes -->