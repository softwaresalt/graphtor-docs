---
artifact_type: task
created_at: 2026-08-21T20:45:00.0608768Z
dependencies:
    - 056.002-T
id: 056.007-T
parent_id: 056-F
priority: medium
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: harden stale-lock liveness against pid reuse (H0b only)'
updated_at: 2026-08-22T07:25:00.0000000Z
---



<!-- BEGIN:acceptance-criteria -->
- If H0b is NOT evidenced: close as not-needed with a one-line rationale (Constitution VI) — no code change. Closing as not-needed satisfies 056.004-T's dependency on it.
- If taken: a stale lock whose recorded pid has been reused by an unrelated live process is correctly treated as stale (not a live holder), and a genuinely live holder is still excluded.
- Lock metadata records process start-time alongside pid; the liveness check uses both.
- Identity over age (Copilot review P1): a matching pid + process-start-time identity is treated as LIVE regardless of lock age; the STALE_SECS age check evicts ONLY as a fallback when strong identity is unavailable (a legacy pid-only lock). Age alone MUST NOT evict a verified live holder — today is_stale_with_system (src/lock.rs ~472-481) falls through to the age check even when the recorded pid is alive, so a long-running live server can be evicted purely by age.
- Lock-file format compatibility (both directions): a legacy lock file without a start-time field degrades to the current pid-only liveness check and NEVER parse-errors into GraphtorError::Config (which would become a new pre-serve fail-closed exit). Symmetrically, a lock file carrying an UNKNOWN EXTRA field (as a future binary might add) also parses WITHOUT error (unknown fields ignored — forward-compatible), never a hard fail. Existing atomic write-cleanup and concurrent-release NotFound-retry behavior preserved. Compatibility/parse-fallback tests are added.
- Test-first coverage (item 8 per-surface test-first): observed-red behavior tests are (a) a stale lock whose recorded pid was reused by an unrelated LIVE process is treated as stale/evictable — RED today because pid-only liveness misreads it as a live holder; and (b) a genuinely live long-running holder older than STALE_SECS stays LIVE — RED today because the age branch evicts it even when the pid is alive. Compatibility characterization guards are (c) a legacy start-time-less lock degrades to pid-only liveness without a parse-error and (d) a lock file carrying an UNKNOWN EXTRA field parses WITHOUT error; both are expected GREEN before and after because the existing manual line parser already ignores unknown lines and tolerates missing fields. The task remains test-first through red anchors (a) and (b).
- No --force escape hatch is required for the reused-pid case.
- fmt + clippy pedantic (-D warnings) + tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
CONDITIONAL on T0/T1 evidencing lock contention / stale-lock PID REUSE (H0b) rather than the launch-cwd discovery cause (H0a). Isolated out of T2 (056.003-T) so that task implements exactly one evidenced cause. Harden stale-lock liveness in src/lock.rs by recording process start-time alongside pid so a reused pid is not misread as a live lock holder. A matching pid+start-time identity stays LIVE regardless of lock age; the STALE_SECS age check evicts ONLY as a fallback when strong identity is unavailable (legacy pid-only lock), with legacy compatibility and concurrent-release NotFound behavior preserved. Age alone MUST NOT evict a verified live holder. Prefer start-time+pid over a --force escape hatch. Do NOT weaken exclusion of a genuinely live holder.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Surfaces: src/lock.rs DatabaseLock::acquire / AdvisoryLock::acquire / handle_existing_lock (~166-201) and is_stale_with_system (~472-481; STALE_SECS=3600, LockDetails{pid,timestamp} ~21-24). (The serve/sync entry point is the src/main.rs helper `acquire_database_lock` ~2803-2824, which validates the path then delegates to DatabaseLock::acquire; the liveness change lands in these src/lock.rs primitives, not that wrapper.) Extend the lock-file content and the liveness check to record + compare process start-time: when a matching pid+start-time identity is live, treat the holder as LIVE and DO NOT fall through to the age check; keep the age branch (age >= STALE_SECS) only as a fallback when start-time identity is absent (legacy lock). Preserve the existing hand-written `parse_lock_details` tolerance model: add a recognized start-time line, continue ignoring unrecognized lines, and continue leaving absent fields as `None`; do not introduce serde or a new hard parse-error path. When a legacy start-time-less lock file is parsed and the code degrades to pid-only liveness, emit a debug-level diagnostic (tracing debug!, consistent with repo logging patterns) recording the legacy-format fallback so the degraded path is observable. This is a separate width from the T2 cwd/discovery fix.
<!-- END:implementation-notes -->