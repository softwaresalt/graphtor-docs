---
artifact_type: task
created_at: 2026-08-21T20:13:50.3347087Z
dependencies:
    - 056.021-T
id: 056.001-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Run exact-CLI differential serve probe and order causes
updated_at: 2026-08-23T07:24:58.3970307Z
---



<!-- BEGIN:acceptance-criteria -->
- Record the exact target Copilot executable path, version/build, and `/mcp show graphtor-docs` invocation; this identity must match T4.
- Run inside the 056.021-T isolated `logs/probe/<nonce>` workspace using its temporary in-workspace control and treatment `.mcp.json` and its owned nested ancestor/child fixture. Never mutate, back up, restore, or substitute the user `.mcp.json`; no operator approval receipt is required because the real config is never touched.
- Compose the 056.020-T self-tested transparent byte proxy and read-only observer seam with the 056.022-T process lifecycle; never reimplement pumps or teardown. Both temporary entries are byte-equivalent to the production server entry semantics except the wrapper command; the treatment entry alone adds the candidate working-directory mechanism. Launch both from one controlled foreign directory and record the wrapper plus inner-child cwd/identity.
- Gate 1 (ancestor config-isolation, run FIRST): using the exact target CLI against 056.021-T's nested ancestor/child fixture, prove the nearest child `.mcp.json` shadows and does not merge the sentinel/invalid ancestor `.mcp.json`. Only after this isolation is proven may the control/treatment causal contrast run. If the exact CLI reads or merges the ancestor config, STOP the causal H0 comparison and route to H3-B via 056.019-T (supported isolated-config mechanism); never assume the repository-root `.mcp.json` is unread.
- Treat causal progress as ordered, not mutually exclusive. If treatment honors the canonical project-root cwd and advances to a later gate/handshake stage, activate H0a managed-cwd delivery as a prerequisite even when another H0b/H0c/H1/H3-A cause remains.
- Bound evidence iterations: run exactly one control/treatment contrast per selected correction. After each correction, exactly one terminal outcome holds -- initialize succeeds; one newly evidenced downstream task is selected; H3-B2 blocks the shipment; or the task returns blocked with the captured evidence. No open-ended implementation loop runs inside this task; never discard an already-proven prerequisite.
- If the exact CLI leaves both legs foreign or rejects the original field, select H3-B. Otherwise classify the next cause from child exit/liveness/framing evidence.
- Preserve the unmodified duplex transaction, concurrent stderr, exit versus still-alive state, lock presence, and Generation posture. Keep raw frames in memory through the 056.020-T observer seam for same-run replay; direct replay is confirmation only.
- Apply the deadline through 056.020-T and the exact recorded PID-plus-process-start-time identity/handle teardown contract through 056.022-T -- not a broad entire-process-tree claim -- and the isolated-workspace evidence lifecycle through 056.021-T. Persist only redacted structured summaries and digests. Fail closed when process ownership, exact CLI identity, ancestor config-isolation, or same-build control/treatment parity is unproved.
- Link the ordered downstream tasks and stop when the exact production entry is healthy or H3-B2 blocks the shipment. T4 alone owns restored-production acceptance and must match the recorded CLI identity.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Investigate-first actual-client evidence run using the validated 056.020-T proxy and read-only observer seam plus the 056.022-T process lifecycle, inside the 056.021-T isolated workspace. First prove ancestor config-isolation with the exact CLI against the owned nested fixture; only then record the exact failing Copilot identity and select an ordered sequence of evidenced prerequisites/causes rather than forcing one mutually exclusive branch. No user config mutation or approval receipt is involved because the control and treatment entries live only inside the isolated workspace.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
OS error 232 is H0 evidence only when the actual CLI sees pipes close after the owned inner server exits. T0 produces evidence, not a shipping wrapper. Control and treatment `.mcp.json` are generated inside the 056.021-T isolated workspace; the user config is never read or written. Gate 1 uses the exact CLI to prove the nearest-child config shadows and does not merge the sentinel ancestor config; failure routes to H3-B via 056.019-T rather than a causal H0 comparison. Teardown is the exact recorded PID-plus-start-time identity/handle contract owned by 056.022-T; there is no broad entire-process-tree guarantee. H3-A retains the raw in-memory transaction (via the observer seam) for same-run replay; H3-B is adjudicated by 056.019-T.
<!-- END:implementation-notes -->