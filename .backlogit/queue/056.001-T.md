---
artifact_type: task
created_at: 2026-08-21T20:13:50.3347087Z
dependencies:
    - 056.021-T
id: 056.001-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Run exact-CLI differential serve probe and order causes
updated_at: 2026-08-23T06:40:10.5017983Z
---



<!-- BEGIN:acceptance-criteria -->
- Record the exact target Copilot executable path, version/build, and `/mcp show graphtor-docs` invocation; this identity must match T4.
- Run inside the 056.021-T isolated `logs/probe/<nonce>` workspace using its temporary in-workspace control and treatment `.mcp.json`. Never mutate, back up, restore, or substitute the user `.mcp.json`; no operator approval receipt is required because the real config is never touched.
- Use the 056.020-T self-tested transparent proxy/process harness. Both temporary entries are byte-equivalent to the production server entry semantics except the wrapper command; the treatment entry alone adds the candidate working-directory mechanism. Launch both from one controlled foreign directory and record wrapper plus inner-child cwd/identity.
- Treat causal progress as ordered, not mutually exclusive. If treatment honors the canonical project-root cwd and advances to a later gate/handshake stage, activate H0a managed-cwd delivery as a prerequisite even when another H0b/H0c/H1/H3-A cause remains. Iterate evidence after each selected correction until initialize succeeds or an explicit unsupported-client blocker is proven.
- If the exact CLI leaves both legs foreign or rejects the original field, select H3-B. Otherwise classify the next cause from child exit/liveness/framing evidence; never discard an already-proven prerequisite.
- Preserve the unmodified duplex transaction, concurrent stderr, exit versus still-alive state, lock presence, and Generation posture. Keep raw frames in memory for same-run replay; direct replay is confirmation only.
- Apply the deadline and all-outcome owned process-tree teardown through 056.020-T, and the isolated-workspace evidence lifecycle through 056.021-T. Persist only redacted structured summaries and digests. Fail closed when process ownership, exact CLI identity, or same-build control/treatment parity is unproved.
- Link the ordered downstream tasks and stop when the exact production entry is healthy or H3-B2 blocks the shipment. T4 alone owns restored-production acceptance and must match the recorded CLI identity.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Investigate-first actual-client evidence run using the validated 056.020-T proxy inside the 056.021-T isolated workspace. Record the exact failing Copilot identity and select an ordered sequence of evidenced prerequisites/causes rather than forcing one mutually exclusive branch. No user config mutation or approval receipt is involved because the control and treatment entries live only inside the isolated workspace.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
OS error 232 is H0 evidence only when the actual CLI sees pipes close after the owned inner server exits. T0 produces evidence, not a shipping wrapper. Control and treatment `.mcp.json` are generated inside the 056.021-T isolated workspace; the user config is never read or written. H3-A retains the raw in-memory transaction for same-run replay; H3-B is adjudicated by 056.019-T.
<!-- END:implementation-notes -->