---
artifact_type: task
created_at: 2026-08-21T20:13:50.3347087Z
dependencies:
    - 056.021-T
id: 056.001-T
labels:
    - phase:evidence
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Run exact-CLI differential serve probe and order causes
updated_at: 2026-08-23T20:57:36.1280649Z
---



<!-- BEGIN:acceptance-criteria -->
- Own `tools/mcp-probe/src/exact_cli.rs` and the exact-cli subcommand plus the ONE final subcommand-wiring edit in the thin `tools/mcp-probe/src/main.rs`. The diagnostic `wrapper` subcommand is owned by 056.022-T. Compose the 056.020-T transport, the 056.022-T process guards and wrapper, and the 056.023-T observer/evidence seam; never reimplement the pumps, teardown, wrapper, or evidence capture. Run inside the 056.021-T isolated `logs/probe/<nonce>` workspace using its temporary in-workspace control/treatment `.mcp.json` and owned nested ancestor/child fixture.
- Record the exact target Copilot executable path, version/build, and `/mcp show graphtor-docs` invocation; this identity must match T4. Never mutate, back up, restore, or substitute the user `.mcp.json`; no operator approval receipt is required because the real config is never touched.
- Diagnostic wrapper handoff (parity): the child `.mcp.json` uses the 056.022-T wrapper as `command`; the wrapper args (`--inner-exe`, repeated `--inner-arg`, `--evidence-output`, `--run-nonce`) encode the exact absolute production inner executable plus its original args and are byte-identical between control and treatment, which differ ONLY by the treatment `cwd`. Record and hash the exact inner executable path and version. This wrapper-only handoff is the sole allowed parity difference; add no extra env or user-config mutation.
- Env-inheritance sentinel (for 056.006-T selection): set a unique sentinel environment variable on the exact-CLI process and record, from the 056.022-T wrapper's observation, whether the child inherited it. This bounded probe-owned sentinel observation is written into the redacted evidence and makes 056.006-T's env-inheritance activation criterion provable without altering production config; it never changes the wire and never breaks control/treatment parity.
- Gate 1 (ancestor config-isolation, run FIRST): using the exact target CLI against 056.021-T's nested ancestor/child fixture, prove the nearest child `.mcp.json` shadows and does not merge the sentinel/invalid ancestor. Only after this isolation is proven may the single control/treatment causal contrast run. If the exact CLI reads or merges the ancestor config, STOP the causal H0 comparison, emit typed `H3-B-candidate` evidence, and continue FORWARD to 056.019-T (the sole H3-B terminal); never assume the repository-root `.mcp.json` is unread, and never declare H3-B2 here.
- ONE-SHOT classification: run exactly one control (no `cwd`) / treatment (canonical project-root `cwd`) pair through the shared runner, then emit the current ORDERED cause classification (proven prerequisites first -- e.g. H0a is retained when a cwd correction exposes a later blocker) from child exit / liveness / framing / lock / Generation evidence. This task runs NO implementation loop, never reruns per correction, and never waits for production health.
- Terminal outcome: this task NEVER declares H3-B2. After the single pair it ends in exactly one state -- it emits the initial ordered downstream cause classification and moves to `done`, or (only when evidence capture itself is impossible/blocked for an explicit non-H3-B reason) it returns blocked with the captured evidence. It never reopens or reactivates a downstream task; downstream causal tasks (including 056.019-T for any `H3-B-candidate`) are visited once in the authoritative forward-chain order.
- If the exact CLI leaves both legs foreign or rejects the original field, emit typed `H3-B-candidate` and continue forward to 056.019-T. Otherwise classify the next cause from child exit / liveness / framing evidence. Preserve the unmodified duplex transaction, concurrent stderr, exit versus still-alive state, lock presence, and Generation posture. Raw frames stay in wrapper memory (056.022-T/056.023-T); this runner reads only the redacted structured summary and digests from the wrapper's `--evidence-output` file.
- Apply the deadline through 056.020-T and the direct-`Child`-handle teardown through 056.022-T (no whole-tree claim), inside the 056.021-T isolated workspace. Read and persist only redacted structured summaries and digests. Fail closed when process ownership, exact CLI identity, ancestor config-isolation, or same-inner-executable control/treatment parity is unproved.
- Runtime classification runs via `cargo +1.75.0 run --manifest-path tools/mcp-probe/Cargo.toml -- exact-cli ...`; there is no `self-test` CLI subcommand. T4 alone owns restored-production acceptance and must match the recorded CLI identity.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
One-shot investigate-first exact-client classification. Own `tools/mcp-probe/src/exact_cli.rs` and the single final `main.rs` subcommand wiring (the diagnostic wrapper is 056.022-T); compose the 056.020-T transport, 056.022-T process guards/wrapper, and 056.023-T observer seam inside the 056.021-T isolated workspace, reading only the redacted summary/digests the wrapper writes to `--evidence-output`. First prove ancestor config-isolation with the exact CLI against the owned nested fixture, then run exactly one control/treatment pair and emit the current ordered cause classification, then end `done` with that classification. It NEVER declares H3-B2: a Gate-1 merge or both-legs-foreign result emits typed `H3-B-candidate` and continues forward to 056.019-T. Its only other terminal is blocked when evidence capture itself is impossible for an explicit non-H3-B reason. No hidden implementation loop, no per-correction rerun, no production-health wait, and no reopening of any downstream task. No user config mutation or approval receipt because control/treatment live only inside the isolated workspace.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
OS error 232 is H0 evidence only when the actual CLI sees pipes close after the owned inner server exits. T0 produces evidence and the ordered classification, not a shipping wrapper or an implementation loop. The 056.022-T wrapper handoff encodes the exact absolute production inner executable plus its original args as byte-identical wrapper args across both legs; control/treatment differ only by treatment `cwd`; record and hash the exact inner executable path/version. Gate 1 uses the exact CLI to prove the nearest-child config shadows and does not merge the sentinel ancestor; a merge (or both-legs-foreign) emits typed `H3-B-candidate` and continues forward to 056.019-T rather than declaring H3-B2 or forcing a causal H0 comparison. Teardown is the direct-`Child`-handle contract owned by 056.022-T; there is no whole-tree guarantee. Raw frames never leave wrapper memory; this runner reads only the redacted summary/digests from the wrapper's `--evidence-output`. It also sets a unique sentinel env var on the exact-CLI process and records the 056.022-T wrapper's inherited-or-not observation as redacted evidence for 056.006-T env-inheritance selection (observation only; never alters the wire or control/treatment parity). H3-A reacquisition/validation belongs to 056.011-T and H3-B adjudication to 056.019-T. Runtime classification: `cargo +1.75.0 run --manifest-path tools/mcp-probe/Cargo.toml -- exact-cli ...`.
<!-- END:implementation-notes -->