---
artifact_type: task
created_at: 2026-07-14T18:56:06.5631433-07:00
dependencies:
    - 051.004-T
id: 051.001-T
labels:
    - phase-2
    - install
    - rust
parent_id: 051-F
priority: medium
references:
    - docs/exec-plans/2026-07-14-consumption-first-graphtor-plan.md
    - docs/decisions/2026-07-14-consumption-first-graphtor-deliberation.md
status: queued
title: 'P2-T1: Consumption-first install default'
updated_at: 2026-07-15T00:52:59.9634293-07:00
---

Default install creates only the .graphtor/ root + a minimal serve .mcp.json written via the SHARED MCP config writer (P2-T3 / 051.004-T) - PATH command graphtor-docs, args ['serve'], with the managed provenance marker and atomic write. Do NOT write sources.yaml; do NOT create config/bin/cache/data/logs. Do NOT create or update .gitignore (a consumption-first minimal install has no managed .gitignore side effect; the .graphtor/ root is the operator's db-drop location). Make InstallResult.binary_path an Option<PathBuf> (or add InstallKind) so a consumption install with no copied binary is representable; update callers (cmd_install message, upgrade, uninstall) to handle None. Files: src/workspace/install.rs, src/main.rs (cmd_install:3002). Depends on P2-T3 (shared writer). Test-first.

<!-- BEGIN:acceptance-criteria -->
fresh default install creates ONLY .graphtor/ + a minimal .mcp.json (PATH command, no .exe, managed marker present, written via the shared P2-T3 writer); no sources.yaml; no ingestion subdirs (config/bin/cache/data/logs); .gitignore is NOT created or modified by a minimal install; binary_path is None.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Minimal .mcp.json generation + marker + atomic write are provided by P2-T3 (051.004-T) and CALLED here - this task asserts the resulting minimal install, it does not re-implement the writer. .gitignore decision (thread 8): the current cmd_install always manages .gitignore (src/main.rs:3021-3024); the consumption-first minimal path must SKIP that side effect, while the full path (P2-T2b) retains it. Update cmd_install message, upgrade, uninstall to handle a None binary_path.
<!-- END:implementation-notes -->