---
artifact_type: task
created_at: 2026-05-21T12:01:00.000000-07:00
custom_fields:
    source_stash_id: 3FE2DDFB
    execution_posture: test-first
id: 037.001-T
labels:
    - sync
    - api
    - harness-ready
priority: medium
parent_id: 037-F
references:
    - docs/exec-plans/2026-05-21-prewarm-sync-progress-plan.md
status: done
title: Add optional progress callback parameter to sync_source
updated_at: 2026-05-21T12:01:00.000000-07:00
---

Extend `sync_source` in `src/sync/mod.rs` with an optional progress callback parameter (`Option<&mut dyn FnMut(&Path, usize, usize)>`) invoked at each file reingest boundary. Existing callers pass `None` for backward compatibility.

## Acceptance Criteria

- `sync_source` signature includes `on_progress: Option<&mut dyn FnMut(&Path, usize, usize)>`
- All existing call sites updated to pass `None` and compile cleanly
- Unit test confirms callback invocation count matches fixture file count
- `cargo clippy --all-targets -- -D warnings -D clippy::pedantic` passes
