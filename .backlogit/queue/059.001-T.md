---
artifact_type: task
created_at: 2026-08-25T06:15:54.320146Z
custom_fields: {}
dependencies:
    - 059.007-T
    - 059.014-T
id: 059.001-T
labels:
    - security
    - config
    - dependencies
parent_id: 059-F
priority: high
references:
    - docs/exec-plans/2026-08-24-store-toctou-nofollow-handle-plan.md
status: queued
title: 'U1: Add platform-gated no-follow flag dependencies (config)'
updated_at: 2026-08-30T03:53:08.521112Z
---

<!-- BEGIN:description -->
After U7 records PASS (done) and the operator sign-off 059.014-T is `done`, add the exact proven direct dependencies to Cargo.toml/Cargo.lock: libc under cfg(unix), windows-sys 0.61 with Win32_Storage_FileSystem under cfg(windows), and the cap-std/cap-primitives version demonstrated by the isolated Rust 1.75 feasibility harnesses. U1 must not guess or probe a candidate version in the product manifest. If U7 finds no compatible capability release, this task remains blocked and no cap-std dependency is retained. Engine-open binding is out of near-term scope — U8 (059.008-T) recorded BLOCKED and is an accepted decided input (the accepted engine-open residual), deferred to 059.013-T/U9 on a later separate shipment — so NO engine-open dependency is adopted here. Add Principle VI justification and preserve the existing dependency graph without duplicate versions.
<!-- END:description -->

<!-- BEGIN:acceptance-criteria -->
- U7 records PASS and the operator sign-off 059.014-T is `done` before this task begins; U7's exact proven versions and APIs are adopted without re-running candidate discovery in the product manifest. U8's BLOCKED engine-boundary evidence is an accepted decided input, not a gate, and no engine-open dependency is adopted here.
- libc, windows-sys 0.61/Win32_Storage_FileSystem, and the U7-proven cap-std **and cap-primitives** versions are declared as direct dependencies in the correct target/workspace sections with Principle VI justification (`cap-primitives` is required as a direct dependency because the workspace-root no-follow bootstrap calls `cap_primitives::fs::open_dir_nofollow`; it MUST NOT be left implicit as a transitive dependency of `cap-std`).
- Host build/check and cargo +1.75.0 check --all-targets succeed; a continuous Rust 1.75 CI gate is added or an equivalent existing gate is identified.
- Before/after cargo tree comparisons show `cap-primitives` present as a declared **direct** dependency and no unexpected duplicate libc/windows-sys/cap-std/cap-primitives copy; cargo audit reports no new advisory.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Depends on U7 (root/API/MSRV feasibility, PASS) and the operator sign-off gate 059.014-T. U8's engine-boundary feasibility recorded BLOCKED and is an accepted decided input (the accepted engine-open residual), NOT a prerequisite; engine-open binding is deferred to 059.013-T/U9 on a later separate shipment. Candidate-version discovery and any possibly-failing Rust 1.75 probe belong to U7's isolated harness, so a BLOCKED feasibility result is schedulable and does not strand a partially completed U1. U6 consumes these permission-path dependencies after U1; no engine dependency is part of this near-term path.
<!-- END:implementation-notes -->
