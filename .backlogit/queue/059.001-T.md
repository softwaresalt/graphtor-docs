---
artifact_type: task
created_at: 2026-08-25T06:15:54.320146Z
id: 059.001-T
labels:
    - security
    - config
    - dependencies
parent_id: 059-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-store-toctou-nofollow-handle-plan.md
status: queued
title: 'U1: Add platform-gated no-follow flag dependencies (config)'
updated_at: 2026-08-25T07:10:59.1225913Z
---

In Cargo.toml add libc under [target.'cfg(unix)'.dependencies] (O_NOFOLLOW) and windows-sys under [target.'cfg(windows)'.dependencies], pinned to an already-transitive lock version: windows-sys = { version = "0.61", features = ["Win32_Storage_FileSystem"] }. That feature exposes FILE_FLAG_OPEN_REPARSE_POINT, FILE_FLAG_BACKUP_SEMANTICS, FILE_ATTRIBUTE_REPARSE_POINT, FILE_READ_ATTRIBUTES, FILE_WRITE_ATTRIBUTES, FILE_SHARE_READ|WRITE|DELETE. Add a justification comment (both crates already transitive; Constitution Principle VI). Config-first posture, config domain.

<!-- BEGIN:acceptance-criteria -->
- Both platform deps declared and pinned to an existing Cargo.lock version.
- windows-sys pinned to 0.61 with feature Win32_Storage_FileSystem; libc under cfg(unix).
- Justification comment present (Principle VI).
- Workspace compiles on host (cargo build / cargo check --all-targets).
- Pinned-MSRV verification REQUIRED for the new direct deps: `cargo +1.75.0 check --all-targets` (or `rustup run 1.75.0 cargo check --all-targets`) succeeds, proving libc/windows-sys and their feature set build on the declared MSRV 1.75, not just the host toolchain.
- Duplicate impact verified by an explicit BEFORE/AFTER comparison: capture `cargo tree -d` (and `cargo tree -i windows-sys` / `cargo tree -i libc`) before the edit and after, then diff; no new windows-sys/libc version copy is introduced. A post-edit-only snapshot is insufficient.
- cargo audit reports no new advisory.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Both crates are already transitive in Cargo.lock (windows-sys at 0.61.2). Do not bump to a version that introduces a second copy. No source code changes in this unit.
<!-- END:implementation-notes -->