---
artifact_type: shipment
created_at: 2026-08-29T09:27:43.1639227Z
custom_fields:
    items:
        - 056.029-T
dependencies:
    - 049-S
id: 053-S
priority: high
status: queued
title: Declared-MSRV / rmcp edition-2024 compatibility resolution (pre-052-S)
updated_at: 2026-08-29T09:32:47.7420507Z
---

PHASE 1.6 task-only declared-MSRV release unit. Its sole member is `056.029-T`. It carries a `blocks` dependency on `049-S` and is itself a `blocks` prerequisite of `052-S`, so the H3-A remedy transitively waits for the `049-S` evidence unit and for this unit. When Stage assembles the PHASE 1.5 `056.028-T` evidence-infrastructure shipment after `049-S` closes, it adds that shipment as an explicit `053-S` dependency before claim; that manifest is deliberately not created at plan time (assembly gate: `049-S` closed). Rationale: the PR #108 review established a deterministic incompatibility rather than an open risk -- stable Cargo accepted `edition = "2024"` only from Rust/Cargo 1.85, the workspace declares `rust-version = "1.75"`, and the resolved `rmcp 1.5.0` manifest declares `edition = "2024"` with no `rust-version`, so `cargo +1.75.0` cannot parse it and the former mandatory `052-S` entry gate could only ever fail. This unit resolves the mismatch first by exactly one bounded option -- raise the declared `rust-version` to a truthful floor and align docs/CI, or select an MSRV-compatible rmcp strategy through a bounded decision -- neither preselected nor implemented at plan time, and never silently waived. Width isolation is preserved: this unit owns only the dependency/toolchain declaration and the documentation/CI text asserting it, with any broad source-level migration split into a named follow-up; the transport remedy stays entirely in `052-S`. Like `056.028-T`, its member is not cause-selected, so the `phase:remedy` `selection:` gate does not apply. This unit is terminal only when the declared floor is truthful and the ordinary gates pass at that resolved floor; until then `052-S` and `056.011-T` remain blocked and `056.004-T` (T4) cannot report READY.