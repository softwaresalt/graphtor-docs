---
artifact_type: feature
created_at: 2026-08-25T06:15:32.7194524Z
custom_fields:
    harness_status: pending
id: 059-F
labels:
    - security
    - filesystem
    - toctou
    - store
    - test-first
links:
    - target_id: 052-F
      link_type: related_to
    - target_id: 056.024-T
      link_type: related_to
references:
    - docs/exec-plans/2026-08-24-store-toctou-nofollow-handle-plan.md
    - docs/decisions/2026-08-24-store-toctou-nofollow-handle-deliberation.md
    - docs/decisions/2026-08-29-store-toctou-engine-boundary-redeliberation-deliberation.md
    - docs/compound/best-practices/reparse-point-fail-closed-containment-2026-07-16.md
status: blocked
title: Identity-bound no-follow handle for store.rs read-only permission ops (sibling TOCTOU fix)
updated_at: 2026-08-30T01:07:34.4533816Z
---

Eliminate two sibling check-then-use TOCTOU symlink/reparse swap vulnerabilities on the read-only permission mutation paths in src/db/store.rs by binding every permission operation to an identity-bound, no-follow file handle instead of re-resolving paths. Covers BOTH EngineReadonlyGuard lock/acquisition-rollback/Drop restore (main db index 0 + all sidecars) and clear_stale_readonly_lock write-mode sidecar permission probe/clear. Fail-closed on any link/reparse/unobtainable handle; NO path-based chmod fallback. Preserves exact-permission restoration and sidecar-content invariants. Consumes stash E86A6E56 (clear_stale_readonly_lock, high) and 5905CDEE (EngineReadonlyGuard, medium).

<!-- BEGIN:dod -->
- New test-first scenarios for main-db post-lock swap resistance, sidecar swap-at-clear refusal, and already-read-only clear all pass.
- Existing store::tests for exact-mode, byte-identical content, non-empty-sidecar preservation, dangling-symlink refusal, and stale-lock self-heal pass unchanged. The former empty-sidecar-removal expectation changes explicitly to fail-closed retention unless U3 proves same-identity deletion.
- No path-based set_permissions/set_readonly remains on guarded/candidate entries in lock/Drop/clear_stale_readonly_lock. Transient sidecars are not removed through a separate metadata-check + name-based unlink; when same-identity deletion is unavailable, cleanup fails closed by leaving the sidecar.
- U7 (059.007-T) records PASS for the three bounded root/API/MSRV scenarios, and U8 (059.008-T) records PASS for the three bounded SQLite/Cozo engine-boundary scenarios, before U1 adopts dependencies or production integration begins. Either BLOCKED result keeps Principles III/IV NOT-PASSED.
- Intermediate-directory containment is split across U6's beneath-root permission boundary and U9's actual WAL/SQLite/Cozo engine-open integration. U3-U5 and U10 depend on both units; U2 provides the leaf primitives U6 composes and precedes U6 (U2 depends only on U1); U11 depends on U2.
- Deterministic normal-CI predicate coverage (owned by U11 / 059.011-T): the pure target-independent should_refuse_reparse predicate (module-private constant REPARSE_ATTR = 0x0000_0400) compiles/executes on Linux CI, and a #[cfg(windows)] assertion proves REPARSE_ATTR == windows-sys FILE_ATTRIBUTE_REPARSE_POINT; production Windows code calls the shared predicate.
- Test-scenario split (PR #107 review, 2026-08-25): the former seven-delta U5 (059.005-T) is bounded to three sidecar swap-resistance scenarios (a)-(c); the Windows retained-handle engine non-interference (d) and broader non-name-surrogate reparse regression (e) move to U10 (059.010-T); the deterministic predicate (f) and Windows literal-equality / single-source structural proof (g) move to U11 (059.011-T). Each of U5/U10/U11 defines fewer than four independently countable test scenarios.
- cargo build, clippy::pedantic -D warnings, cargo test, cargo audit all green; no new duplicate crate copy (cargo tree -d); pinned-MSRV `cargo +1.75.0 check --all-targets` green with a continuous Rust 1.75 CI check added during implementation (or an equivalent repository MSRV gate explicitly identified as covering the new deps).
- Windows handle-mode decision (Option A retained-handle vs Option C identity-verified re-open) recorded in the release PR with test evidence.
<!-- END:dod -->

<!-- BEGIN:goals -->
- Open each guarded entry (main db index 0 + -wal/-shm/-journal sidecars) once with no-follow semantics, resolved RELATIVE TO a retained workspace-root capability directory handle (U6/U7), and retain the File handle for the DataStore lifetime.
- Drive every permission op (capture, apply, acquisition rollback, Drop restore; write-mode probe+clear) through the retained handle. Do not unlink transient sidecars after a separate emptiness check; retain them unless deletion is proven against the same file identity.
- Close the intermediate-directory race via U6 and carry containment through the actual engine open via U9, gated on U7/U8 feasibility PASS. Principles III/IV completeness requires U7/U8 PASS plus U6/U9 landed. **(Engine-open portion SUPERSEDED 2026-08-29 — see the "AUTHORITATIVE re-deliberation (2026-08-29)" section below: U8 is the terminally-BLOCKED accepted residual, U9 is deferred to a later separate shipment, and near-term completeness is scoped to the permission-mutation threat; U1 is gated on U7 PASS + sign-off `059.014-T`, not U8.)**
- Remove the per-sidecar is_reparse_point re-check (superseded by no-follow open + explicit Windows reparse-attribute refusal).
- Guarantee cross-platform fail-closed behavior: Unix O_NOFOLLOW (ELOOP) and Windows FILE_FLAG_OPEN_REPARSE_POINT + explicit FILE_ATTRIBUTE_REPARSE_POINT post-open refusal via the shared target-independent should_refuse_reparse predicate (backed by the module-private REPARSE_ATTR constant).
- Preserve #![forbid(unsafe_code)] (any unsafe FFI lives in cap-std/rustix), Rust 1.75 MSRV, and clippy::pedantic -D warnings.
<!-- END:goals -->

<!-- BEGIN:pr107-addendum -->
## PR #107 review addendum (2026-08-25)

Honest status correction from the PR #107 Copilot review remediation:

- **Intermediate-directory threat**: final-component O_NOFOLLOW / FILE_FLAG_OPEN_REPARSE_POINT does NOT prevent a post-`validate_path` swap of an INTERMEDIATE parent directory redirecting the mutation outside the workspace. This is closed by U6 (059.006-T) resolving every component relative to a retained workspace-root directory handle.
- **U7 root/API/MSRV gate (059.007-T)**: three test-first scenarios run before U1 and prove candidate Rust-1.75 compatibility, retained root/API conversion, and the capability refusal matrix.
- **U8 engine-boundary gate (059.008-T)**: three separate test-first scenarios prove a safe capability- or identity-bound SQLite/Cozo open or return BLOCKED.
- **Split integration**: U6 owns the contained opener/permission boundary; U9 (059.009-T) owns actual WAL/engine integration. U2 provides the leaf no-follow/permission primitives U6 composes; U3-U5 and U10 depend on both U6 and U9; U2 precedes U6 and U11 depends on U2.
- **U2-before-U6 dependency rewire (PR #107, 2026-08-25)**: U2 (059.002-T) is a leaf-primitive unit depending only on U1 and is scheduled BEFORE U6. Its former U6/U9 dependencies are removed. U6 (059.006-T) now depends on U2 and COMPOSES U2's open_no_follow / capture_perms / set_readonly_via_handle primitives instead of duplicating them. U9 still follows U6. The current DAG is U7 -> U8 -> U1 -> U2 -> U6 -> U9 -> U3/U4 -> U5/U10, with U11 after U2. No cycles; all feasibility gates preserved.
- **Test-scenario split**: the former seven-delta U5 (059.005-T) is split into three bounded test tasks: U5 keeps sidecar matrix/signal/junction deltas (a)-(c); U10 (059.010-T) owns Windows retained-handle non-interference (d) and broader non-name-surrogate reparse regression (e); U11 (059.011-T) owns the deterministic predicate (f) and Windows literal-equality / single-source structural proof (g).
- **Transient cleanup identity**: root-relative `symlink_metadata` followed by `remove_file` is still a check/use race at the final name. U3 now fails closed by retaining transient sidecars unless a safe same-identity deletion API is proven.
- **Deterministic predicate**: the Windows reparse refusal is a pure target-independent should_refuse_reparse predicate over the module-private constant REPARSE_ATTR (= 0x0000_0400), unit-tested on Linux CI, with a #[cfg(windows)] assertion pinning REPARSE_ATTR to windows-sys FILE_ATTRIBUTE_REPARSE_POINT. This deterministic predicate coverage is owned by U11 (059.011-T).
- **Honest Principles III/IV status**: NOT-PASSED / provisional until U7/U8 record PASS and U6/U9 land.
<!-- END:pr107-addendum -->

<!-- BEGIN:redeliberation-2026-08-29 -->
## AUTHORITATIVE re-deliberation (2026-08-29): U8 BLOCKED — scoped permission-mutation DoD

Supersedes the engine-boundary portions of the DoD/goals/pr107-addendum above. Source:
`docs/decisions/2026-08-29-store-toctou-engine-boundary-redeliberation-deliberation.md`
(Option B chosen) and the exec-plan's "AUTHORITATIVE re-deliberation replan - 2026-08-29".

U7 (059.007-T) PASS; U8 (059.008-T) **BLOCKED** — cozo 0.7 `SqliteStorage` re-opens SQLite by a
bare path on every pool-empty `transact()` for the `DataStore` lifetime, and `DbInstance::new`'s
`path: impl AsRef<Path>` structurally rejects a handle/capability object (type-system fact). No
safe capability-/identity-bound engine open exists without forking cozo (out of scope, Principle VI).

**Amended DoD (engine-boundary portion):**

- The two originally-reported permission-mutation TOCTOUs (`5905CDEE`, `E86A6E56`) are closed by
  identity-bound, no-follow, root-relative handles — leaf (U2) AND intermediate-directory (U6)
  containment of the `chmod` paths. No path-based `chmod`; no permission mutation can be redirected
  outside the workspace. This is feasible per U7.
- **U8 PASS is NO LONGER a completion gate**; its BLOCKED evidence is an accepted decided input.
  **U9 (engine-open binding, 059.009-T) is REMOVED from this DoD** and deferred to Option A
  (upstream cozo, 059.013-T) on a later separate shipment.
- Principles III/IV are **PASSED for the permission-mutation threat only**; they remain
  **NOT-PASSED for the engine-open intermediate-directory redirection**, which is the named,
  signed-off **accepted residual** (see the decision's Accepted-Residual-Risk Record). The original
  fail-closed DoD is **NOT** claimed as satisfied.
- **Implementation (U1 onward) MUST NOT begin until 059.014-T (operator sign-off) is `done`.**
- All other prior DoD obligations (test-first; `#![forbid(unsafe_code)]`; continuous MSRV-1.75 CI;
  clippy pedantic; cargo audit; no duplicate crate copy; exact-permission + sidecar invariants;
  deterministic `should_refuse_reparse` predicate) are retained unchanged for the feasible units.

**Rescoped feasible DAG (backlog rewired 2026-08-29):**
`U7(done) -> U1 -> U2 -> U6 -> U3/U4 -> U5/U10`, with `U11` after U2, and `U1` additionally gated on
`059.014-T` (sign-off). U8 (evidence, terminal), U9 (deferred, deps 059.013-T+U6), and U12
(059.012-T) are excluded from near-term completion and remain for later separate shipments;
U12's obsolete dependency on the terminally-BLOCKED U8 is repointed to the sign-off gate
`059.014-T` so it can become ready at the correct later gate rather than being permanently blocked.

**Shipment posture:** `051-S` stays active/blocked; its manifest is NOT mutated by Stage. The
Ship-side transition (re-scope `051-S` in place, or close it and assemble a fresh implementation
shipment) is planned in the decision and executed only by Ship after sign-off. `049-S` is decoupled
from `051-S` (sequencing-only edge; no code coupling; no schedule gain) so bug `7BF1961D` proceeds
independently; `059-F` security work is NOT abandoned.
<!-- END:redeliberation-2026-08-29 -->
