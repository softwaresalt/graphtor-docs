---
artifact_type: task
created_at: 2026-08-21T20:13:54.4631139Z
id: 056.002-T
labels:
    - phase:evidence
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Add out-of-process serve handshake test driver
updated_at: 2026-08-23T21:25:03.4484198Z
---



<!-- BEGIN:acceptance-criteria -->
- Add reusable out-of-process MCP test support that spawns the real binary, holds `ChildStdin` for the full request/response scope, sends newline-delimited valid `initialize`, enforces a bounded timeout, and cleans up only the owned child. This is an independent, green, T0-agnostic driver: it does not consume T0's cause classification to be built and carries no standing dependency on 056.001-T.
- Drain stderr concurrently with the request/response lifetime through a bounded capture; cancellation-safe shutdown follows response or cleanup. A child that writes beyond one pipe buffer cannot deadlock or be misclassified as H1.
- Exact initialize success requires: incremental newline-delimited JSON-RPC parsing, a response whose `id` exactly matches the request `id`, `jsonrpc: "2.0"`, no `error` member, and a negotiated `result.protocolVersion`. Interleaved server notifications/events are parsed and classified separately and never satisfy initialize; a fragmented or partial frame/event cannot satisfy initialize. Exit, stderr, broken pipe, or timeout are diagnostic only.
- Provide a REUSABLE parity-control harness (green on its own neutral fixtures) that asserts stdout stays protocol-clean during initialize. The ASSERTION that 056.003-T's `mcp_serve_ready` / typed preflight diagnostics (stderr + tracing only) add no stdout bytes and do not alter the initialize wire/protocol is owned by 056.003-T (which consumes this harness) and re-verified as an 049-S shipment-level control after 056.003 lands -- it is NOT a completion gate on this task, which must not depend on 056.003-T.
- Own an explicit read-only production-workspace 'server control' mode that T4 depends on, with an ENFORCEABLE read-only boundary: target only a ReadOnly/auto-discovered serve posture (reject Generation/write-capable targets) or a verified read-only snapshot, disable background sync and any persistence/write path, open databases read-only, and FAIL the control on any attempted workspace write or lock acquisition. In that mode send a valid `initialize` plus one side-effect-free `get_status` and an expected-tools query, and expose the wire-level result (`jsonrpc: "2.0"`, correlated id, no `error`, `result.protocolVersion`, and the expected tool set) as a server control. This is a server-side wire-level control only -- never proof of Copilot UI invocation, never a production mutation, and not a new get_status fingerprint feature.
- This task ends green: driver self-tests and neutral control fixtures pass. It does not commit a branch-specific failing assertion. Each selected curative task uses the driver to write and observe its own red test before production changes, then greens it in the same task.
- H0a generated-entry proof belongs to 056.008-T; H0b desired lock behavior belongs to 056.007-T; H1 deterministic proof belongs to 056.005-T/056.015-T; H3-A before/after exact-client reacquisition (semantic initialize correlation plus redacted transcript digest, not raw-frame replay) belongs to 056.011-T. Operational H0c/H3-B use actual-client evidence.
- Prewarm/pin model state away from non-H1 tests. If no deterministic selected-branch proof can be derived from T0, do NOT reopen or rerun 056.001-T (T0) and do NOT invent a fixture: block T4 and create a NEW bounded Stage follow-up to derive the missing selected-branch red.
- Rust 1.75, fmt, clippy pedantic, and all tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Create the reusable open-stdin, concurrent-stderr, real-process initialize driver -- an independent, green, T0-agnostic driver that also owns a read-only production-workspace 'server control' mode for T4 (wire-level initialize / get_status / expected-tools control, never mutating production and never proving Copilot UI invocation). The task itself remains green; selected curative tasks own their observed-red then green branch assertions.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Keep `ChildStdin` alive until response or cleanup and drain stderr concurrently. The driver owns only the exact child, the reusable transport + parity-control harness, and the read-only production 'server control' mode used by T4 (enforceable read-only boundary: ReadOnly/auto-discovered posture only, background sync/persistence disabled, DB opened read-only, fail on any write/lock; wire-level initialize/get_status/expected-tools). Branch fixtures and assertions stay with their curative tasks; the 056.003-diagnostics stdout-parity ASSERTION is owned by 056.003-T (consuming this harness), re-checked as a 049-S shipment-level control, never a completion gate here. This driver does not consume T0's classification to be built, so it carries no standing dependency on 056.001-T; H3-A before/after reacquisition (not raw replay) is owned by 056.011-T.
<!-- END:implementation-notes -->