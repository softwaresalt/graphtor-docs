---
artifact_type: task
created_at: 2026-08-22T19:57:40.5848162Z
dependencies:
    - 056.012-T
    - 056.027-T
id: 056.013-T
labels:
    - phase:final
parent_id: 056-F
priority: medium
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Document managed MCP launch and compatibility contracts
updated_at: 2026-08-23T20:58:39.7779124Z
---

<!-- BEGIN:description -->
Documentation-only managed launch, upgrade recovery, and H3 operator contract under the EXISTING `### 2. Configure your MCP client` heading in `docs/mcp-tools.md` (managed-launch content beneath it) plus the relevant `### serve`, `### install`, `### upgrade`, and `### uninstall` sections of `docs/cli-reference/graphtor-docs.md`. It always reconciles the docs `type` vs generator `transport` discriminator against the exact CLI-honored field from evidence (the 056.026-T reconciled generator field); it reconciles the Quick Start `.mcp.json` example in `README.md` ONLY when a discriminator change is selected, otherwise verifying that example already matches the CLI-honored field and recording not-needed. It reconciles the generated cwd and recovery-retention/uninstall behavior against the implemented code (056.008-T/056.009-T/056.027-T), records H3-B2 as an explicit unsupported-client blocker, and preserves production-evidence rules.
<!-- END:description -->

<!-- BEGIN:acceptance-criteria -->
- Update only: the managed-launch content beneath the EXISTING `### 2. Configure your MCP client` heading in `docs/mcp-tools.md`; the relevant `### serve`/`### install`/`### upgrade`/`### uninstall` sections of `docs/cli-reference/graphtor-docs.md`; and -- when discriminator evidence selects a change -- the Quick Start `.mcp.json` example in `README.md`. Name the current anchors and do not claim a subsection already exists. No Rust/config/runtime work.
- Always reconcile the `docs/mcp-tools.md` `type` versus generator `transport` discriminator against the exact CLI-honored field from T0 evidence (the 056.026-T reconciled generator field), even when no generator change was selected. Reconcile the README Quick Start `.mcp.json` example ONLY when a discriminator change is selected; when no discriminator change is selected, verify the README example already matches the CLI-honored field and record `not-needed` rather than mutating it. Also reconcile the generated cwd and recovery-retention/uninstall behavior against the implemented code (056.008-T cwd, 056.009-T cwd delivery, 056.027-T discriminator delivery).
- For H0a/H3-B1, document the exact managed working-directory field/mechanism, typed create/update/no-change/collision/path-violation results, upgrade text/JSON action, retained recovery handle, restore procedure, and Minimal not-applicable behavior.
- For H3-A, document only the selected compatible transport change and rollback. For H3-B, distinguish B1's second documented mechanism on the exact CLI from B2 `unsupported-client`, which blocks shipment rather than switching identities.
- State that only T4's restored-production `/mcp show` starts are acceptance; temporary wrappers/capability entries are diagnostic only.
- Omit non-selected branches with rationale; links/lint pass; remain within `docs/mcp-tools.md`, `docs/cli-reference/graphtor-docs.md`, and (for the discriminator Quick Start example) `README.md`.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Depends on 056.012-T and the discriminator existing-install delivery 056.027-T (transitively 056.026-T generation), and consumes final behavior from the managed-config/recovery/H3 tasks. Own the managed-launch content under the existing `### 2. Configure your MCP client` heading, the `### serve`/`### install`/`### upgrade`/`### uninstall` CLI-reference sections, and the README.md Quick Start `.mcp.json` example reconciliation for the discriminator when selected; name current anchors and do not invent a new subsection. Always reconcile the docs and README `type` vs generator `transport` discriminator against the exact CLI-honored field from evidence.
<!-- END:implementation-notes -->