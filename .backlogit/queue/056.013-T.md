---
artifact_type: task
created_at: 2026-08-22T19:57:40.5848162Z
dependencies:
    - 056.012-T
id: 056.013-T
parent_id: 056-F
priority: medium
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Document managed MCP launch and compatibility contracts
updated_at: 2026-08-22T21:40:26.1145931Z
---

<!-- BEGIN:description -->
Documentation-only managed launch, upgrade recovery, and H3 operator contract under the EXISTING `### 2. Configure your MCP client` heading in `docs/mcp-tools.md` (managed-launch content beneath it) plus the relevant `### serve`, `### install`, `### upgrade`, and `### uninstall` sections of `docs/cli-reference/graphtor-docs.md`. It always reconciles the docs `type` vs generator `transport` discriminator against the exact CLI-honored field from evidence, reconciles the generated cwd and recovery-retention/uninstall behavior against the implemented code, records H3-B2 as an explicit unsupported-client blocker, and preserves production-evidence rules.
<!-- END:description -->

<!-- BEGIN:acceptance-criteria -->
- Update only the managed-launch content beneath the EXISTING `### 2. Configure your MCP client` heading in `docs/mcp-tools.md` and the relevant `### serve`/`### install`/`### upgrade`/`### uninstall` sections of `docs/cli-reference/graphtor-docs.md`; name the current anchors and do not claim a subsection already exists. No Rust/config/runtime work.
- Always reconcile the docs `type` versus generator `transport` discriminator against the exact CLI-honored field from T0 evidence, even when no generator change was selected, and reconcile the generated cwd and recovery-retention/uninstall behavior against the implemented code.
- For H0a/H3-B1, document the exact managed working-directory field/mechanism, typed create/update/no-change/collision/path-violation results, upgrade text/JSON action, retained recovery handle, restore procedure, and Minimal not-applicable behavior.
- For H3-A, document only the selected compatible transport change and rollback. For H3-B, distinguish B1's second documented mechanism on the exact CLI from B2 `unsupported-client`, which blocks shipment rather than switching identities.
- State that only T4's restored-production `/mcp show` starts are acceptance; temporary wrappers/capability entries are diagnostic only.
- Omit non-selected branches with rationale; links/lint pass; remain within `docs/mcp-tools.md` and `docs/cli-reference/graphtor-docs.md`.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Depends on 056.012-T and consumes final behavior from the managed-config/recovery/H3 tasks. Own the managed-launch content under the existing `### 2. Configure your MCP client` heading plus the `### serve`/`### install`/`### upgrade`/`### uninstall` CLI-reference sections; name current anchors and do not invent a new subsection or a third documentation surface. Always reconcile the docs `type` vs generator `transport` discriminator against the exact CLI-honored field from evidence.
<!-- END:implementation-notes -->