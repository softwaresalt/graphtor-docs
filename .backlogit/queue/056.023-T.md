---
artifact_type: task
created_at: 2026-08-23T08:22:56.0156328Z
dependencies:
    - 056.022-T
id: 056.023-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Build non-interfering probe observation and evidence capture
updated_at: 2026-08-23T08:22:56.0156328Z
---

<!-- BEGIN:acceptance-criteria -->
- Depend on 056.022-T (forward chain `056.020 -> 056.022 -> 056.023 -> 056.021`). Own only `tools/mcp-probe/src/evidence.rs` (add `observer.rs` only if a second file is genuinely needed) and the copy-only read-only observer seam of the standalone `tools/mcp-probe/` diagnostic crate, using a narrowly justified standalone `serde_json` dependency for JSON-RPC transcript correlation and redaction. Never reimplement the 056.020-T duplex pumps or the 056.022-T process teardown.
- The observer and the transcript correlator execute INSIDE the 056.022-T wrapper process. Define the seam so the 056.020-T pump performs its ordered write-and-flush of forwarded bytes FIRST, then delivers copies/immutable chunks; the pump NEVER awaits observer work and NEVER takes a cross-direction observer lock, so observation cannot reorder, delay, or deadlock forwarding in either direction.
- Deliver through a bounded, non-blocking channel. On saturation or observer failure, atomically mark the summary invalid (evidence incomplete) while forwarding continues byte-for-byte unchanged. The observer receives copies or immutable chunks only and never mutates, reorders, drops, or reframes wire bytes.
- Raw frames NEVER leave wrapper memory. The wrapper writes ONLY a redacted structured summary plus digests, atomically, to its owned `--evidence-output` file; the 056.001-T runner consumes that file and never sees raw frames. Never write raw frame bytes to disk and never replay raw frames cross-process.
- Incrementally reassemble the newline-delimited JSON-RPC stream for OBSERVATION only: correlate the exact `initialize` request id to a `jsonrpc: "2.0"` non-error `result.protocolVersion`, separately record interleaved notifications/events, and never affect wire bytes.
- Self-test exactly this one evidence-observation domain (no process spawn, no workspace/config, no exact CLI): (1) fragmented/partial frames and interleaved events plus a paused/slow observer and a failing observer each prove byte-identical forwarded order, half-close propagation, deadline outcome versus a no-observer control, and correct initialize correlation; (2) redaction of secret-bearing argv/env/message fields in the persisted summary; (3) no raw-frame persistence (only the redacted summary/digests reach the `--evidence-output` file).
- Standalone-crate Rust 1.75 gates pass: `cargo +1.75.0 check|test|build|clippy --manifest-path tools/mcp-probe/Cargo.toml` (clippy with `-D warnings -D clippy::pedantic`). Add no Tokio dependency and no `unsafe` (preserve crate-root `#![forbid(unsafe_code)]`); the only new dependency is the standalone `serde_json`.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Own the copy-only read-only observer seam, JSON-RPC transcript correlation/redaction, and in-wrapper evidence capture (`tools/mcp-probe/src/evidence.rs`, using standalone `serde_json`) for the standalone diagnostic probe crate. The observer and transcript correlator run INSIDE the 056.022-T wrapper process: the 056.020-T pump writes and flushes forwarded bytes first, then delivers copies to the observer through bounded non-blocking delivery; the pump never awaits the observer or takes cross-direction locks. Saturation or observer failure atomically marks the summary invalid while forwarding continues unchanged. Raw frames never leave wrapper memory; the wrapper writes only a redacted structured summary plus digests atomically to its owned `--evidence-output`, which the 056.001-T runner consumes. It incrementally reassembles newline-delimited JSON-RPC for observation only, correlates the exact initialize request id to a `jsonrpc: "2.0"` non-error `result.protocolVersion`, separately records notifications/events, and never affects wire bytes.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Own `tools/mcp-probe/src/evidence.rs` (add `observer.rs` only if a second file is genuinely required) plus focused evidence self-tests inside the standalone `tools/mcp-probe/` crate, using a narrowly justified standalone `serde_json` dependency. Consume the 056.020-T transport's post-write, bounded, non-blocking copy-delivery hook; do not reimplement the pumps. The observer and transcript correlator run inside the 056.022-T wrapper process, receive copies or immutable chunks (never a mutable handle to the forwarded buffer), and write only the redacted summary/digests to the wrapper-owned `--evidence-output`. This task performs no process spawning/teardown (056.022-T), creates no isolated workspace or `.mcp.json` (056.021-T), and runs no real CLI (056.001-T).
<!-- END:implementation-notes -->