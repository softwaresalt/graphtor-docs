---
artifact_type: task
created_at: 2026-08-23T08:22:56.0156328Z
dependencies:
    - 056.022-T
id: 056.023-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Build non-interfering probe observation and evidence capture
updated_at: 2026-08-23T08:22:56.0156328Z
---

<!-- BEGIN:acceptance-criteria -->
- Depend on 056.022-T (forward chain `056.020 -> 056.022 -> 056.023 -> 056.021`). Own only `tools/mcp-probe/src/evidence.rs` (add `observer.rs` only if a second file is genuinely needed) and the copy-only read-only observer seam of the standalone `tools/mcp-probe/` diagnostic crate. Never reimplement the 056.020-T duplex pumps or the 056.022-T process teardown.
- Define the observer seam so the 056.020-T pump performs its ordered write-and-flush of forwarded bytes FIRST, then delivers copies/immutable chunks. The pump NEVER awaits observer work and NEVER takes a cross-direction observer lock, so observation cannot reorder, delay, or deadlock forwarding in either direction.
- Deliver through a bounded, non-blocking channel. On saturation or observer failure, atomically invalidate the affected capture (mark evidence incomplete) while forwarding continues byte-for-byte unchanged. The observer receives copies or immutable chunks only and never mutates, reorders, drops, or reframes wire bytes.
- Keep raw transaction chunks in memory only for same-run replay by 056.001-T. Persist only redacted structured summaries and digests; never write raw frame bytes to disk.
- Self-test exactly this one evidence-observation domain (no process spawn, no workspace/config, no exact CLI): (1) a paused/slow observer and a failing observer each prove byte-identical forwarded order, half-close propagation, and deadline outcome versus a no-observer control; (2) redaction of secret-bearing argv/env/message fields in persisted summaries; (3) no raw-frame persistence (only redacted summaries/digests reach disk).
- Standalone-crate Rust 1.75 gates pass: `cargo +1.75.0 check|test|build|clippy --manifest-path tools/mcp-probe/Cargo.toml` (clippy with `-D warnings -D clippy::pedantic`). Add no Tokio dependency, no `unsafe`, and no new process-control crate.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Own the copy-only read-only observer seam and in-memory evidence capture (`tools/mcp-probe/src/evidence.rs`) for the standalone diagnostic probe crate. The 056.020-T pump writes and flushes forwarded bytes first, then delivers copies to the observer through bounded non-blocking delivery; the pump never awaits the observer or takes cross-direction locks. Saturation or observer failure atomically invalidates the capture while forwarding continues unchanged. Raw chunks stay in memory for same-run replay; persisted outputs are redacted summaries and digests only. This is one evidence-observation domain with no process lifecycle, workspace, config, or exact-CLI concern.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Own `tools/mcp-probe/src/evidence.rs` (add `observer.rs` only if a second file is genuinely required) plus focused evidence self-tests inside the standalone `tools/mcp-probe/` crate. Consume the 056.020-T transport's post-write, bounded, non-blocking copy-delivery hook; do not reimplement the pumps. The observer receives copies or immutable chunks, never a mutable handle to the forwarded buffer. This task performs no process spawning/teardown (056.022-T), creates no isolated workspace or `.mcp.json` (056.021-T), and runs no real CLI (056.001-T).
<!-- END:implementation-notes -->