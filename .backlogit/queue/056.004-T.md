---
artifact_type: task
created_at: 2026-08-21T20:14:30.6167042Z
dependencies:
    - 056.003-T
    - 056.005-T
    - 056.006-T
    - 056.007-T
    - 056.008-T
    - 056.009-T
    - 056.010-T
    - 056.011-T
id: 056.004-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Runtime verification against newest CLI + rollback/observation window
updated_at: 2026-08-22T02:05:00.0000000Z
---



<!-- BEGIN:acceptance-criteria -->
- `/mcp show graphtor-docs` on the newest CLI: connected, handshake complete, no OS error 232 (evidence captured).
- Serve-ready startup log captured showing successful transport bind.
- Rollback documented: revert shipment commits in reverse dependency order; re-pin prior rmcp if it was bumped.
- Observation window fully specified (manual — no hosted observability; STDIO serve is a local child process): owner is the merging developer (Ship/operator); duration is the next 3 serve starts or 24h (whichever comes first).
- Pre-fix baseline recorded (BRANCH-SENSITIVE to the T0 evidence): for an H0 cause, the nonzero child exit code + early-exit stderr marker + client-visible OS error 232 (from 056.001-T); for an H1 cause, a bounded-`initialize` timeout with the child STILL ALIVE (latency, no early exit); for an H3 cause, one of two modes — mode A (framing/version): the child STILL ALIVE with the framed initialize never negotiating a protocolVersion (no early exit); mode B (client ignores/rejects the pinned cwd): the generated-contract child EARLY-EXITS from a foreign cwd (managed-launch early exit, distinguished from H0a because the CLI ignores the pinned cwd). The success signal is identical for all: a completed initialize handshake with no OS error 232.
- Exact method/invocation captured (BRANCH-SENSITIVE, P1-2): for each observed start run `/mcp show graphtor-docs` on the newest Copilot CLI. On the normal branches capture with `RUST_LOG=debug` stderr redirected to `logs/serve-stderr.log`. When the T2c sink (056.006-T) was activated because the CLI DISCARDS child stderr, READ AND VALIDATE the configured env-gated sink file (e.g. under .graphtor/logs/) instead — requiring logs/serve-stderr.log from a shell redirect is impossible on that branch.
- Files/log signals named: the captured startup evidence — `logs/serve-stderr.log` on the normal branches, or the configured 056.006-T sink file on the T2c branch — shows the serve-ready startup log (transport bound) and no OS error 232; `/mcp show graphtor-docs` reports connected with a completed `initialize` handshake.
- Success trigger: all 3 starts (or the 24h window) complete the handshake with no OS error 232 => outcome `healthy`. Rollback trigger: any OS error 232 recurrence, an exit-before-initialize (H0 or H3 mode B), or a failed/timed-out / never-negotiated initialize handshake (H1 or H3 mode A) => revert shipment commits in reverse dependency order (for H3 mode B, revert to the previously documented client configuration) => outcome `rolled-back`. Outcome (healthy/degraded/rolled-back) recorded in the shipment closure artifact.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Runtime verification, rollback documentation, and closure evidence. Verify against the REAL newest Copilot CLI that `/mcp show graphtor-docs` shows a healthy connected server with a completed initialize handshake and no OS error 232; capture the serve-ready startup log. Document the rollback procedure and a fully-specified post-fix observation window: owner (merging developer / Ship / operator), duration (next 3 serve starts or 24h), the pre-fix T0 baseline (056.001-T) as the "before" state, the exact BRANCH-SENSITIVE per-start invocation (`/mcp show graphtor-docs` with RUST_LOG=debug stderr to logs/serve-stderr.log on the normal branches; read/validate the configured 056.006-T sink file when the T2c sink was activated because the CLI discards child stderr — P1-2), the named log signals (serve-ready line + no OS error 232), and explicit success/rollback triggers. No hosted observability is promised — this is a local manual window.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
This is verification-width (runtime + closure), distinct from the code tasks. Feeds operational-closure. 056.003-T (cmd_serve diagnostics: loud exit-2 errors + serve-ready log) is NON-conditional and always lands. The CURATIVE fix tasks are conditional and exactly one causal branch activates from the T0 evidence: H0a = 056.008-T (managed launch contract, pinned cwd) + 056.009-T (existing-install delivery); H0b = 056.007-T; H0c = 056.010-T (operational remediation); H1 = 056.005-T; H3 = 056.011-T (client/transport compatibility — mode A framing / mode B client ignores the pinned cwd). Diagnosability = 056.006-T (optional). The SELECTED branch always includes a task that produces a healthy initialize handshake (H0c is no longer diagnosability-only; H3 must reach a healthy handshake in whichever mode — mode A red/green, mode B via evidence + a manual compatibility check), so this task's connectivity gate is always satisfiable; closing a non-selected conditional fix task as not-needed SATISFIES this task's dependency on it — T4 does not wait for a conditional task that evidence ruled out.
<!-- END:implementation-notes -->