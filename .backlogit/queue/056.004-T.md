---
artifact_type: task
created_at: 2026-08-21T20:14:30.6167042Z
dependencies:
    - 056.003-T
    - 056.005-T
    - 056.006-T
    - 056.007-T
    - 056.008-T
    - 056.009-T
    - 056.010-T
    - 056.011-T
    - 056.012-T
    - 056.013-T
    - 056.014-T
    - 056.015-T
    - 056.016-T
    - 056.017-T
    - 056.018-T
    - 056.019-T
    - 056.020-T
    - 056.001-T
    - 056.021-T
    - 056.022-T
    - 056.023-T
    - 056.024-T
    - 056.002-T
    - 056.025-T
    - 056.026-T
    - 056.027-T
    - 056.028-T
    - 056.029-T
    - 056.030-T
    - 056.031-T
    - 056.032-T
    - 056.033-T
id: 056.004-T
labels:
    - phase:final
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Runtime verification against newest CLI + rollback/observation window
updated_at: 2026-08-29T16:45:19.4597226Z
---



<!-- BEGIN:acceptance-criteria -->
- Acceptance for THIS bug: on the exact T0 Copilot path/version/build, `/mcp show graphtor-docs` reports the server CONNECTED and INITIALIZED with no OS error 232 against the restored production entry, using ONLY the connection/initialization status the CLI surfaces (and, when shown, the advertised tool list/count) -- not raw JSON-RPC wire fields. T4 is the sole owner of production-entry acceptance and does not expand into a new get_status workspace-fingerprint product feature
- Gate acceptance on the completed evidence chain: `056.001-T` ancestor config-isolation proof plus exact-CLI identity, `056.021-T` isolated-workspace evidence, `056.022-T` direct-Child-handle process cleanup, and `056.023-T` observer/evidence capture. Direct dependencies on `056.001-T`, `056.002-T`, `056.021-T`, `056.022-T`, `056.023-T`, `056.024-T`, `056.025-T`, `056.028-T`, `056.029-T`, `056.030-T`, `056.031-T`, `056.032-T`, and `056.033-T` make final fan-in explicit and robust; the identity verified here must match `056.001-T`'s recorded identity
- The PHASE 1.6 fan-in is mandatory declared-MSRV truthfulness evidence: `056.029-T` must resolve the dependency graph at unchanged Rust 2021 / rust-version 1.75, and `056.030-T` through `056.033-T` must complete the CI and declaration alignment as members of `053-S` before `052-S`. T4 cannot report READY while any of those tasks is queued or blocked, and a completed transport adapter cannot substitute for that evidence
- Require THREE independent `/mcp show graphtor-docs` exact-client sessions, including one diagnostic-gate-off branch. Each session proves actual-client connection and initialization and the absence of OS error 232 using only the fields `/mcp show` exposes, and correlates the exact production config hash / file identity, the Copilot executable/version/build, the Copilot-spawned server PID/executable/build, the canonical workspace cwd, and a timestamp, using no wrapper/temporary config. The correlated JSON-RPC wire fields (`jsonrpc: "2.0"`, correlated id, no error, `result.protocolVersion`) are NOT captured from `/mcp show`; they are proven SEPARATELY by the direct T1 (`056.002-T`) production driver against the same production binary/workspace as a wire-level control. If `/mcp show` reports advertised tools, record their list/count as supporting evidence, but do NOT require a tool invocation unrelated to the reported load bug, and do NOT route missing deterministic tool-call UI to H3-B2
- Before the first start on a managed branch, require the selected-branch delivered-refresh evidence and record its post-refresh production config hash: on a managed-cwd branch this is `056.009-T`'s cwd/recovery delivery evidence, and on a discriminator branch this is `056.027-T`'s discriminator delivery evidence. When both branches are selected, require both
- All three starts use the restored delivered command/args/cwd/env entry. T0's wrapper, temporary entry, wrapper PID, or wrapper-only logs are invalid; capture may use inherited stderr, `056.006-T`'s sink, or non-substituting OS tracing
- Own a future concrete runtime-verification runner script `scripts/verify_copilot_mcp_show.ps1` (test-first acceptance; Stage assigns ownership now, Ship implements it when `056.004-T` lands -- do NOT implement at plan time). The script contract: (1) invoke the exact STRUCTURED Copilot argv NORMALLY (no wrapper, temporary config, or executable substitution); (2) capture the CLI-visible output for each of the three sessions and NEVER treat exit 0 alone as pass; (3) FAIL if OS error 232 / a failed-connection signature appears, or if the CONNECTED/INITIALIZED status expected for the recorded CLI build is absent; (4) record the advertised tool list/count when `/mcp show` exposes it; (5) correlate each run with the production config hash/file identity and a NAMED non-substituting server-start evidence source (inherited stderr, the selected `056.006-T` sink, or OS tracing -- the one gate-off run may NOT rely on the sink); (6) invoke/consume `056.002-T`'s separate read-only production-control mode for the JSON-RPC id/`protocolVersion`/expected-tools/`get_status` wire evidence (not read from `/mcp show`); (7) emit a redacted structured result; and (8) preserve immediate native exit-code checking
- If the sink is selected, the only environment difference is the declared additive `GRAPHTOR_*` gate and unique exclusive absolute path. At least one of three starts must run gate-off through inherited stderr or OS tracing to prove production parity
- The direct T1 (`056.002-T`) production driver SEPARATELY confirms the expected MCP tools, one side-effect-free `get_status`, and the valid initialize wire response (`jsonrpc: "2.0"`, correlated id, no error, `result.protocolVersion`) against the SAME production binary/workspace as a server control; this is a server-side wire-level control, not proof of Copilot UI invocation. For each start record the exact CLI invocation, the correlated production config hash/fields, timestamp, the Copilot-spawned server PID/executable/build/canonical cwd, the `/mcp show` advertised-tool list/count if reported, the direct-driver tool/`get_status`/initialize control result, capture path, and result. `mcp_serve_ready` is preflight evidence only. No wrapper or temporary config is valid in T4
- Deterministic branch replay or temporary H3-B capability proof cannot substitute. An H3-B2 `unsupported-client` classification from `056.019-T` blocks this task; a missing deterministic tool-call UI does not, by itself, imply H3-B2
- Rollback reverses commits, restores config/state/recovery backups, and re-pins rmcp if changed. Healthy requires three starts in 24 hours; fewer is incomplete, secondary-signal miss degraded, and recurrence/non-negotiation rolled back
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Sole production-acceptance gate for THIS bug: verify three exact-identity `/mcp show graphtor-docs` sessions against the restored delivered entry — each proving the server connected and initialized with no OS error 232 using ONLY the fields `/mcp show` surfaces (connection/initialization status and, when shown, advertised tool list/count), including one diagnostic-gate-off start, with the production config hash/file identity correlated to the Copilot-spawned server startup event. The JSON-RPC wire fields (`jsonrpc: "2.0"`, correlated id, no error, `result.protocolVersion`) are proven SEPARATELY by the direct T1 (`056.002-T`) production driver against the same production binary/workspace, not by `/mcp show`; that driver also confirms expected tools and one side-effect-free `get_status` as a server control, not as proof of Copilot UI invocation. Does not expand into a get_status workspace-fingerprint product feature.

<!-- END:description -->

<!-- BEGIN:implementation-notes -->
All conditional tasks must be done or done-plus-not-needed first. T4 direct fan-in is ALL other 056.x tasks (`056.001-T` through `056.033-T`) except `056.004-T`, including the discriminator generation/delivery (`056.026-T`/`056.027-T`), standalone probe CI job (`056.028-T`), dependency resolution (`056.029-T`), primary CI enforcement (`056.030-T`), ordinary documentation alignment (`056.031-T`), canonical agent declaration alignment (`056.032-T`), and Rust authoring-instruction alignment (`056.033-T`). `056.029-T` is not a 052-S member: it is the first dependency-closed member of the dedicated PHASE 1.6 shipment `053-S`; `056.030-T` through `056.033-T` complete its required alignment before `052-S` may start. The task edge `056.028-T -> 056.029-T` makes PHASE 1.5 enforceable without creating its shipment early. All validation remains at Rust/Cargo 1.75; no dynamically raised floor is valid evidence. An H3-B2 unsupported-client classification from `056.019-T` is blocking; a missing deterministic tool-call UI is not, by itself, H3-B2, and handshake completion is the acceptance signal for this bug. T4 waits for completed docs but owns every restored-production actual-client result. It gates on the evidence chain: `056.001-T` ancestor config-isolation proof and exact-CLI identity, `056.002-T` driver (including its read-only production server-control mode), `056.021-T` isolated-workspace evidence, `056.022-T` direct-Child-handle process cleanup, `056.023-T` observer/evidence capture, `056.024-T` safe-primitive decision, and `056.025-T` projection must all be complete before restored-production acceptance. Each of three exact-Copilot sessions correlates the production config hash/file identity with the Copilot-spawned server startup event and proves connection/initialization via the fields `/mcp show` surfaces; JSON-RPC wire fields are proven separately by `056.002-T`. No wrapper or temporary config is used. The T4 runtime-verification runner is `scripts/verify_copilot_mcp_show.ps1`, owned by this task and implemented test-first by Ship when this task lands.
<!-- END:implementation-notes -->