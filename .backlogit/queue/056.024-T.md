---
artifact_type: task
created_at: 2026-08-23T08:23:06.500704Z
dependencies:
    - 056.017-T
id: 056.024-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
    - docs/decisions/2026-08-21-mcp-serve-initialize-os-error-232-deliberation.md
status: queued
title: Decide safe no-follow config mutation primitive
updated_at: 2026-08-23T08:23:06.500704Z
---

<!-- BEGIN:acceptance-criteria -->
- This is a bounded technical decision/spike, NOT production implementation. Depend on 056.017-T; 056.008-T depends on this decision (forward chain `056.017 -> 056.024 -> 056.008`).
- Initial not-needed pass-through: if T0/056.019-T evidence did NOT select managed-config mutation or recovery (no H0a prerequisite and no H3-B1 mechanism), move to `done` with `not-needed: managed config mutation/recovery not selected` and pass evidence unchanged; never block unrelated branches. Only when selected does the bounded decision run, and only the selected branch may block on a missing compliant primitive.
- When selected, determine a Rust-1.75 (MSRV), safe-call-site, no-follow / capability-based file-mutation primitive that gives Windows reparse-point/junction safety for the sensitive managed `.mcp.json` and recovery mutation path WITHOUT relaxing crate-root `#![forbid(unsafe_code)]`.
- Evaluate at least two options and record the comparison: (a) a narrowly justified, audited safe dependency that exposes handle-level no-follow open/reopen semantics on the MSRV toolchain; (b) an implementable std-only contract (open, then verify the same handle's identity/metadata before use) whose guarantees and residual TOCTOU window are stated precisely. Record which std guarantees actually exist; do NOT assert std already provides handle-level no-follow if it does not.
- Prove the chosen semantics with a minimal test or PoC (green on Rust 1.75) demonstrating reparse/junction rejection or same-identity verification across create, read, and restore.
- Create and own a NEW decision artifact at a future path such as `docs/decisions/YYYY-MM-DD-safe-no-follow-config-mutation-primitive.md` recording the option chosen, rationale, MSRV/audit/licensing notes, and residual risk. Do NOT mutate the already-decided OS-232 deliberation except, if selected, a single outbound link added to it later.
- If NO compliant primitive exists on MSRV without `unsafe`, this task returns blocked and blocks 056.018-T and the shipment rather than silently accepting check-then-open on the sensitive recovery path. Managed-config recovery (H0a/H3-B1) cannot proceed until resolved.
- No production `graphtor_core` edit occurs here; the selected primitive is implemented by 056.018-T. Any PoC is bounded and not shipped. Record full-build non-applicability when this decision task makes no production Rust change.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Bounded technical decision/spike that, WHEN T0/056.019-T evidence selects managed-config mutation or recovery, selects a safe, no-follow / capability-based file-mutation primitive usable at safe call sites on Rust 1.75 for Windows reparse safety, without relaxing `#![forbid(unsafe_code)]`. If not selected, it closes `not-needed` and passes evidence unchanged without blocking unrelated branches. When selected, compare a narrowly justified safe dependency against an implementable std-only contract, prove the chosen semantics with a minimal PoC, and record the decision in a NEW `docs/decisions/` artifact (not by rewriting the OS-232 deliberation). If no compliant primitive exists, block 056.018-T and the shipment rather than accepting an unsafe check-then-open for the sensitive recovery path.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Decision/spike only; no production `graphtor_core` edits (that is 056.018-T). When selected, capture the option comparison, MSRV/licensing/audit findings, PoC result, and the recorded decision in a NEW `docs/decisions/` artifact (e.g. `docs/decisions/YYYY-MM-DD-safe-no-follow-config-mutation-primitive.md`); do not rewrite the OS-232 deliberation beyond adding a single outbound link if selected. The honest std-capability finding feeds 056.018-T's `src/path/handle.rs` implementation; a no-compliant-primitive outcome blocks 056.018-T and the shipment. When not selected, close `not-needed` and pass evidence unchanged.
<!-- END:implementation-notes -->