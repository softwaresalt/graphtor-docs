---
artifact_type: task
created_at: 2026-08-22T20:47:25.1034297Z
id: 056.020-T
labels:
    - phase:evidence
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Build transparent MCP byte proxy
updated_at: 2026-08-23T20:58:34.4997165Z
---



<!-- BEGIN:acceptance-criteria -->
- Create a STANDALONE, non-published diagnostic crate rooted at `tools/mcp-probe/` with its OWN `Cargo.toml` that declares an empty `[workspace]` table (so the crate stands outside the production package and workspace), sets `publish = false`, and pins `edition = "2021"` / `rust-version = "1.75"`. This scaffold MUST add crate-root `#![forbid(unsafe_code)]`. Add no root `probe-harness` feature, no root `[[bin]]`, no custom `--target-dir`, and no ACL/DACL/artifact hardening. The standalone crate's ordinary Cargo target directory is a build cache, not an evidence trust boundary.
- Own the core synchronous transport only: `src/main.rs` as a thin composition root plus `src/transport.rs`. Implement raw `std::process` / `std::thread` bounded-buffer full-duplex pumps (independent client-to-child stdin and child-to-client stdout), client EOF triggering the child stdin half-close, child stdout EOF triggering the client-facing close, a continuous bounded stderr drain, and deadline signaling. Never rewrite or reframe protocol bytes. This std-only constraint scopes the CORE transport implementation only; later probe tasks may add narrowly justified non-Tokio dependencies (for example `sysinfo` in 056.022-T and `serde_json` in 056.023-T) in their own modules.
- Expose (but do not implement) the seam consumed by 056.023-T: after each direction's ordered write-and-flush completes, deliver forwarded byte copies through a bounded, non-blocking hook. This task owns no observer, evidence, redaction, workspace, config, process-identity, or exact-CLI concern; with an absent or slow delivery target the pump forwards bytes byte-for-byte unchanged and never blocks.
- Hold the direct `std::process::Child` handles needed to wire stdio for transport self-tests, and OWN bounded, test-only cleanup of those self-test fixture `Child` handles: on EVERY self-test outcome (success, assertion failure, panic/unwind, or deadline/timeout) reap each spawned fixture child through its OWNED direct `std::process::Child` handle (`kill()` then a bounded `wait()`) so no helper process leaks. This cleanup is scoped strictly to the crate's own transport self-test fixtures. Add no `sysinfo`/process-tree ownership and no PID-scan or process-name-wide kills -- fixture cleanup goes only through the owned direct `Child` handle; add no Tokio dependency and no Tokio feature assumptions, and no `unsafe`. Production and wrapper process spawning, identity, lifecycle, and teardown authority remain owned by 056.022-T; this task defines no production or wrapper teardown path.
- Self-test transport only (no actual Copilot CLI, no workspace, no config, no persisted evidence, no observer): duplex forwarding and half-close propagation, bounded stderr drain and bounded buffers under load, deadline signaling, and proof that a slow or absent post-write delivery target never alters or delays the forwarded stream. The self-test helper-child mechanism MUST be platform-portable and stay within this task's width: use a portable in-crate helper (for example a std-only echo/sink mode of the probe's own test binary reached via a `std::env::current_exe` re-exec) rather than an OS-specific external shell or helper binary; if portability genuinely requires a separate helper module, document the split and its justification and keep it inside this task. Every self-test that spawns a helper child reaps it through its owned direct `Child` handle on every outcome (including panic and timeout) so the tests leak no processes.
- Standalone-crate Rust 1.75 gates pass: `cargo +1.75.0 check|test|build|clippy --manifest-path tools/mcp-probe/Cargo.toml` (clippy with `-D warnings -D clippy::pedantic`). The crate is never installed or committed as a binary and is invalid as T4 production evidence.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Build the core synchronous transport of the standalone, non-published diagnostic probe crate at `tools/mcp-probe/` (its own `Cargo.toml` with an empty `[workspace]` table plus `publish = false`, Rust 2021 / MSRV 1.75). It performs raw std-process / std-thread full-duplex byte forwarding between the actual Copilot CLI and the inner MCP server without rewriting protocol bytes, propagates half-closes, drains stderr under a bound, and signals deadlines. It exposes a post-write, bounded, non-blocking copy-delivery seam consumed by 056.023-T but owns no observer/evidence, workspace/config, process identity/teardown, or exact-CLI run. The ordinary Cargo target is a build cache, not a trust boundary.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Own `tools/mcp-probe/Cargo.toml` (empty `[workspace]` table to stand outside the production workspace, `publish = false`, `edition = "2021"`, `rust-version = "1.75"`, crate-root `#![forbid(unsafe_code)]`), the thin `tools/mcp-probe/src/main.rs` composition root, and `tools/mcp-probe/src/transport.rs`, plus focused transport self-tests. Use only `std::process` / `std::thread` for this core transport module (the std-only constraint scopes the core transport; later probe tasks may add narrowly justified non-Tokio deps such as `sysinfo`/`serde_json` in their own modules); add no Tokio and no `unsafe`. Keep the transport self-test helper-child mechanism platform-portable (prefer a std-only echo/sink mode of the probe's own test binary via a `current_exe` re-exec over an OS-specific external helper) and within this task's width; document any helper-module split with justification. The focused transport self-tests OWN bounded, test-only reaping of their own spawned fixture `Child` handles through the direct handle on every outcome (success, failure, panic, timeout) so no helper process leaks; production and wrapper process spawning, identity, lifecycle, and teardown remain 056.022-T (exact PID-only or process-name-wide kills are forbidden -- reaping is only through owned direct `Child` handles). The observer/evidence seam implementation is 056.023-T; process spawning, identity, teardown, and the diagnostic wrapper subcommand are 056.022-T; the isolated workspace/config fixtures are 056.021-T; the exact-CLI run is 056.001-T. Write no `.mcp.json`, create no workspace, run no real CLI.
<!-- END:implementation-notes -->