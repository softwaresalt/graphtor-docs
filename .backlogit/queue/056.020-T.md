---
artifact_type: task
created_at: 2026-08-22T20:47:25.1034297Z
id: 056.020-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Build secure transparent actual-client probe harness
updated_at: 2026-08-22T21:40:03.2689998Z
---



<!-- BEGIN:acceptance-criteria -->
- Add a feature-gated non-default probe binary named `graphtor-mcp-probe` at `tools/mcp-probe/main.rs` with an explicit Cargo target requiring `probe-harness`. Build it only with an explicit target-dir under `logs/probe/<nonce>/`; default release/all-target gates exclude it, while dedicated Rust 1.75 check/test commands cover it. The executable/manifest are owner-only, ephemeral, never installed or committed, and invalid for T4.
- Self-test exactly three groups: concurrent duplex/half-close/stderr behavior; handle-safe config backup/substitute/restore including absence; and redaction plus all-outcome process-tree cleanup. The success group runs two probe legs back-to-back and proves no owned descendants or probe lock state leak.
- Use OS handle-level no-follow/reparse-safe opens for every config/backup read, write, substitute, and restore; inspect and operate through the same verified handle, revalidate destination identity before atomic replacement, and abort on identity change/escape. Check-then-open alone is insufficient.
- Preserve original args and inherited cwd/env except allowlisted diagnostics; record wrapper and inner identities separately; never rewrite protocol bytes.
- Require a caller-supplied recorded approval receipt before user-owned config substitution or raw-frame persistence. Capture allowlists argv/env fields and redacts secret-bearing values.
- On success, failure, or deadline, propagate half-closes, close client-facing pipes, reap the owned CLI/wrapper/server tree, prove no descendants remain, and verify exact config bytes/absence restoration.
- Rust 1.75 targeted probe gates and default repository quality gates pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Create the bounded, feature-gated, non-shipping proxy/config harness used by T0. It must not synthesize the closed-pipe symptom, leak a process between contrast legs, or escape the workspace.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Own `tools/mcp-probe/main.rs`, the feature-gated Cargo target, and focused harness tests. The built binary stays under `logs/probe/<nonce>/`; 056.001-T owns the approved real CLI run and branch ordering.
<!-- END:implementation-notes -->