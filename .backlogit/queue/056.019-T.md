---
artifact_type: task
created_at: 2026-08-22T20:46:37.1376543Z
dependencies:
    - 056.003-T
    - 056.001-T
id: 056.019-T
labels:
    - phase:evidence
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: adjudicate isolated-config and cwd client mechanisms (H3-B)'
updated_at: 2026-08-23T21:24:58.309255Z
---



<!-- BEGIN:acceptance-criteria -->
- 056.019-T is the SOLE H3-B terminal adjudicator. Adjudicate BOTH supported client mechanisms on the exact failing CLI: (1) a documented explicit isolated-config discovery mechanism and (2) a documented working-directory (cwd) mechanism. If T0 emitted neither an H3-B cwd cause nor an `H3-B-candidate` from a Gate-1 ancestor-config merge, move to `done` with `not-needed: H3-B / isolated-config mechanism not evidenced`.
- Isolated-config path: when 056.001-T emits `H3-B-candidate` because Gate 1 showed the exact CLI merges ancestor config (so T0's causal contrast was prevented), own the ONE bounded attempt to find and use a documented explicit isolated-config mechanism on that same exact build. If such a mechanism exists and isolates config, perform exactly ONE deferred control/treatment contrast through the existing 056.001-T exact-cli runner and emit the SAME ordered cause classification (isolation makes the deferred H0 contrast possible); a supported mechanism is H3-B1 and forward evidence that activates the managed-config tasks. Never deliberately read or mutate the user's root config.
- Working-directory path (B1): satisfiable only when the exact T0 executable ignores/rejects the original field but passes a new foreign-directory contrast using a different documented field placement or working-directory mechanism on that same build. Record both contrasts; temporary evidence activates the managed-config tasks (056.017/056.024/056.008/056.018/056.009 and the discriminator delivery 056.027) but cannot satisfy T4.
- Terminal classification (fail-closed; this is the single authority): only a CONCLUSIVE verdict closes the evidence. A proven-supported mechanism is H3-B1 (forward evidence that activates the managed-config tasks) and moves THIS task to `done` with the recorded classification. A proven-unsupported exact identity is H3-B2 `unsupported-client`: it moves THIS task to `done` with the recorded H3-B2 classification and blocks the downstream PHASE 2 production-remediation units and PHASE 3 T4 (which then have no safe client fix). Under H3-B2 the managed-config family is thereby NOT selected; this task does NOT itself mutate any sibling task's status -- Stage's Final Assembly Protocol applies the `done` + `not-needed: H3-B2` disposition to those unselected tasks per the selection gate. H3-B1 or a proven H3-B2 is what lets 049-S close.
- INCONCLUSIVE is NOT terminal and is NOT trusted cause selection. A mechanism neither proven supported nor proven unsupported moves THIS task to `blocked` status (backlogit return-blocked) with the captured evidence, blocks 049-S closure, and REQUIRES a NAMED new bounded Stage follow-up (or explicit operator adjudication) to resolve the mechanism before the evidence unit can close. Inconclusive is never described as `done`, never classified Unsupported, never switches CLI identity, and never closes 049-S.
- Perform exactly one bounded one-shot classification per mechanism (plus at most the one deferred isolated-config contrast); do not loop. Record executable path, version/build, invocation, schema/mechanism, and capability result. Never add a server-side external-path fallback.
- Keep this task operational/client-compatibility only; T4 exclusively owns restored-production acceptance and 056.013-T owns docs.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Conditional H3-B and isolated-config adjudication and the SOLE H3-B terminal. On the exact failing CLI, adjudicate both a documented explicit isolated-config discovery mechanism (owning the one bounded attempt, and one deferred control/treatment contrast through the 056.001-T runner, when 056.001-T emits `H3-B-candidate` from a Gate-1 ancestor-config merge) and a distinct documented working-directory mechanism. Fail-closed terminal semantics: a proven-supported mechanism is H3-B1 forward evidence (done) and a proven-unsupported identity is H3-B2 (done) blocking the downstream remediation units/T4 -- only these conclusive verdicts close 056.019-T and let 049-S close. An INCONCLUSIVE result moves this task to `blocked`, blocks 049-S closure, and requires a named new bounded Stage follow-up/operator adjudication; it is never done, never Unsupported, and never switches CLI identity. Never deliberately read or mutate the user's root config; never confuse H3-B with H0a.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Split from 056.011-T so server framing and client capability remain independent. Sole H3-B terminal adjudicator: consumes 056.001-T's `H3-B-candidate`, adjudicates the isolated-config and working-directory mechanisms as one-shot classifications, and runs at most one deferred 056.001-T control/treatment contrast when isolation becomes possible. Fail-closed: H3-B1 (done, forward evidence) and proven H3-B2 (done, blocking downstream units/T4) are the only terminal verdicts that close this task and let 049-S close; INCONCLUSIVE moves this task to `blocked` (return-blocked with captured evidence), blocks 049-S closure, and requires a named new bounded Stage follow-up. This task records its OWN classification/status only and never mutates a sibling task's status -- under H3-B2 the unselected managed-config tasks are dispositioned `done` + `not-needed` by Stage's Final Assembly Protocol (selection gate), not by this task. Never reads or mutates the user root config deliberately.
<!-- END:implementation-notes -->