---
artifact_type: task
created_at: 2026-08-22T20:46:37.1376543Z
dependencies:
    - 056.003-T
id: 056.019-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: adjudicate isolated-config and cwd client mechanisms (H3-B)'
updated_at: 2026-08-22T21:40:20.1787825Z
---



<!-- BEGIN:acceptance-criteria -->
- Adjudicate BOTH supported client mechanisms on the exact failing CLI: (1) a documented isolated-config discovery mechanism and (2) a documented working-directory (cwd) mechanism. If T0 classifies neither an H3-B cwd cause nor a Gate-1 ancestor-config merge, move to `done` with `not-needed: H3-B / isolated-config mechanism not evidenced`.
- Isolated-config path: if 056.001-T Gate 1 shows the exact CLI merges ancestor config, own the ONE bounded attempt to find and use a documented explicit isolated-config mechanism on that same exact build, and perform a one-shot classification. If such a mechanism exists and isolates config, it activates the managed-config tasks; if none exists, classify B2 and block. Never deliberately read or mutate the user's root config.
- Working-directory path (B1): satisfiable only when the exact T0 executable ignores/rejects the original field but passes a new foreign-directory contrast using a different documented field placement or working-directory mechanism on that same build. Record both contrasts; temporary evidence activates 056.008/056.018/056.009 but cannot satisfy T4.
- If the exact identity supports no safe isolated-config or working-directory mechanism, classify H3-B2 as `unsupported-client`, move managed-config tasks to `done` with rationale, and block shipment/T4 pending an upstream/client resolution. Do not claim a healthy fix by switching CLI identity.
- Perform exactly one bounded one-shot classification per mechanism; do not loop. Record executable path, version/build, invocation, schema/mechanism, and capability result. Never add a server-side external-path fallback.
- Keep this task operational/client-compatibility only; T4 exclusively owns restored-production acceptance and 056.013-T owns docs.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Conditional H3-B and isolated-config adjudication. On the exact failing CLI, adjudicate both a documented isolated-config discovery mechanism (owning the one bounded attempt when 056.001-T Gate 1 shows ancestor-config merge) and a distinct documented working-directory mechanism. Each is a one-shot classification; if the exact build supports neither safely, classify B2 unsupported-client and block. Never deliberately read or mutate the user's root config; never confuse H3-B with H0a.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Split from 056.011-T so server framing and client capability remain independent. Adjudicates both the isolated-config discovery mechanism and the working-directory mechanism as one-shot classifications. B2 is a blocking outcome, not a curative done path. Never reads or mutates the user root config deliberately.
<!-- END:implementation-notes -->