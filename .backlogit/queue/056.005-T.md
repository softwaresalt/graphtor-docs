---
artifact_type: task
created_at: 2026-08-21T20:14:34.1447524Z
dependencies:
    - 056.002-T
id: 056.005-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: defer embedding-model load off the handshake (H1 only)'
updated_at: 2026-08-22T08:41:19.6464688Z
---



<!-- BEGIN:acceptance-criteria -->
- If H1 is not evidenced, move to `done` and append `not-needed: H1 not evidenced`.
- If taken, initialize completes before model load while DB open, locks, pre-v4, and duplicate-intake remain fail-closed pre-serve gates.
- Preserve `DocServer: Clone`: use clone-shared per-server lazy state such as `Arc<tokio::sync::OnceCell<EmbeddingModel>>`, or an equivalent typed design. No module global. Enable Tokio `sync` explicitly if used; add `time` only if implementation introduces Tokio timers. Verify Rust 1.75.
- `search_semantic` and `research_topic` return one machine-readable retryable loading signal with no silent fallback.
- Deterministic tests inject a blocking/failing loader: construction/initialize do not invoke it; first model request starts loading; retry and eventual success are covered without network or wall-clock cache behavior.
- Update sync tests to async equivalents or a typed sync wrapper if signatures change.
- Before implementation, split the task if the evidenced design exceeds the 2-hour/3-file/5-function width ceiling.
- fmt + clippy pedantic + tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Conditional H1 fix: move only embedding-model construction off initialize and load it lazily from clone-shared per-server state using `spawn_blocking`. Preserve Rust 1.75 and deterministic injected-loader proof. Fail-closed workspace gates stay before serve.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Likely surfaces are Cargo.toml, `src/embed/resolver.rs`, and `src/mcp/server.rs`. `DocServer` is cloneable, so a bare non-Clone cell is not a valid replacement for `Option<EmbeddingModel>`. Stop and split before coding if task-width limits would be exceeded.
<!-- END:implementation-notes -->