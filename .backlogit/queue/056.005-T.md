---
artifact_type: task
created_at: 2026-08-21T20:14:34.1447524Z
dependencies:
    - 056.002-T
    - 056.014-T
id: 056.005-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: add shared lazy embedding lifecycle (H1 only)'
updated_at: 2026-08-22T21:38:08.9962614Z
---



<!-- BEGIN:acceptance-criteria -->
- If H1 is not evidenced by T0, move to `done` with `not-needed: H1 not evidenced`.
- Consume 056.014-T's `Loaded`, `Disabled`, and `Failed` outcomes. Add one clone-shared `Uninitialized`/`Loading`/`Ready`/`Disabled`/retryable `Failed` owner in `src/embed/lifecycle.rs`; no module global or bare OnceCell-only model.
- Atomically allow one supervised blocking load. Own/monitor its JoinHandle, translate panic/JoinError to Failed, define cancellation/drop, and serialize `ensure_loading` plus retry transitions so detached work cannot overwrite state.
- Define one versioned MCP availability projection. Loading/Failed share stable code, status, retryable=true, remediation, and optional retry hint. Disabled is retryable=false with a stable reason; `search_semantic` returns typed disabled/no fallback, while `research_topic` may use keyword fallback only when its response metadata declares Disabled and `fallback=keyword`. Neither falls back while Loading/Failed.
- Write and observe failing tests before implementation, then green exactly three groups: single-start concurrency, Disabled parity matrix, and failure/panic/retry/eventual Ready.
- Do not edit `cmd_serve` or background sync here; 056.015-T injects this owner into both consumers. Rust 1.75 and all targeted gates pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Conditional H1 core lifecycle in the embedding domain. Add one supervised lazy owner and a versioned MCP model-availability projection, consuming typed resolver outcomes; 056.015-T owns serve/background-sync wiring.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Own `src/embed/lifecycle.rs` plus the MCP-facing projection in `src/mcp/server.rs`. Tokio feature changes are explicit and MSRV-checked. Resolver semantics remain 056.014-T; orchestration remains 056.015-T.
<!-- END:implementation-notes -->