---
artifact_type: task
created_at: 2026-08-21T20:14:34.1447524Z
dependencies:
    - 056.002-T
id: 056.005-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: defer embedding-model load off the handshake (H1 only)'
updated_at: 2026-08-22T07:59:34.5455188Z
---



<!-- BEGIN:acceptance-criteria -->
- If H1 is not evidenced, close not-needed.
- If taken, initialize completes before embedding-model load while DB open, locks, pre-v4, and duplicate-intake remain fail-closed pre-serve gates.
- Own lazy state per `DocServer` instance; no module global. If using `tokio::sync::OnceCell`, explicitly enable Tokio's `sync` feature in Cargo.toml and verify the dependency remains compatible with Rust 1.75. A different per-instance primitive is acceptable if it preserves the same semantics without unsafe code.
- `search_semantic` and `research_topic` return the same machine-readable retryable loading signal; no silent fallback.
- Deterministic test-first proof injects a blocking/failing model-loader seam: server construction/initialize must not invoke it; first model-dependent request starts loading; retryable errors are stable; eventual success is covered. Do not rely on cold-cache wall-clock timing or network access for Cargo tests.
- Update sync unit tests to async equivalents or a typed sync wrapper if handler signatures change.
- fmt + clippy pedantic + tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Conditional H1 fix: move only embedding-model construction off the initialize path and load it lazily from per-server state, using `spawn_blocking`. Add any required existing Tokio feature explicitly and preserve Rust 1.75. Prove behavior with an injected deterministic loader rather than a cold-cache timeout. Both model-dependent tools expose one retryable loading contract; fail-closed database/workspace gates remain before serve.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Surfaces include Cargo.toml if Tokio `sync` is not already enabled, `src/embed/resolver.rs`, and `src/mcp/server.rs`. Prefer a testable loader/factory abstraction owned by the server instance. The out-of-process T0/T1 transcript confirms the real latency symptom, while unit/integration tests use injected blocking/failing loaders and no network. Do not change `get_info`.
<!-- END:implementation-notes -->