---
artifact_type: task
created_at: 2026-08-21T20:14:34.1447524Z
dependencies:
    - 056.002-T
id: 056.005-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: defer embedding-model load off the handshake (H1 only)'
updated_at: 2026-08-22T20:08:33.7200065Z
---



<!-- BEGIN:acceptance-criteria -->
- If H1 is not evidenced by T0, move to `done` and append `not-needed: H1 not evidenced`.
- If taken, initialize completes before model load while DB open, locks, pre-v4, and duplicate-intake remain fail-closed pre-serve gates.
- Preserve `DocServer: Clone` with clone-shared per-server typed state that distinguishes `Uninitialized`, `Loading`, `Ready(EmbeddingModel)`, `Disabled(reason)`, and retryable `Failed(error)`. An `Arc<tokio::sync::OnceCell<_>>` may store the ready value but is not the whole state machine. No module global. Enable Tokio features explicitly and verify Rust 1.75.
- The first model-dependent request atomically transitions `Uninitialized` to `Loading` and starts one owned `spawn_blocking` load without awaiting it. Concurrent requests while loading return one machine-readable retryable signal. `Ok(None)` transitions to `Disabled` and preserves the existing semantic-search-disabled behavior; failures transition to a typed retryable failure, not perpetual loading.
- `search_semantic` and `research_topic` expose the same typed loading/failure contract and never silently fall back to unranked text during loading.
- Deterministic injected-loader tests cover construction/initialize not loading, single-start concurrency, loading responses, `Ok(None)`, failure/retry, and eventual success without network or wall-clock cache behavior.
- Update sync tests to async equivalents or a typed sync wrapper if signatures change.
- Before implementation, split the task if the evidenced design exceeds the 2-hour/3-file/5-function width ceiling.
- fmt + clippy pedantic + tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Conditional H1 fix: move only embedding-model construction off initialize through clone-shared typed per-server state and one owned `spawn_blocking` load. Preserve Rust 1.75, deterministic injected-loader proof, and fail-closed workspace gates.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Likely surfaces are Cargo.toml, `src/embed/resolver.rs`, and `src/mcp/server.rs`. A bare cell is not sufficient; use an explicit per-server transition model whose clones share one logical load.
<!-- END:implementation-notes -->