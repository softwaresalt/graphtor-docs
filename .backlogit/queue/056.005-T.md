---
artifact_type: task
created_at: 2026-08-21T20:14:34.1447524Z
dependencies:
    - 056.002-T
id: 056.005-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: defer embedding-model load off the handshake (H1 only)'
updated_at: 2026-08-21T20:56:39.0372285Z
---



<!-- BEGIN:acceptance-criteria -->
- If H1 is NOT evidenced: close as not-needed with a one-line rationale (Constitution VI) — no code change. Closing this task as not-needed satisfies 056.004-T's dependency on it.
- If taken: serve answers initialize before the model finishes loading; DB open, lock acquisition, pre-v4 gate, and duplicate-intake preflight REMAIN pre-serve fail-closed gates (not converted to silent per-tool errors).
- The lazy-model OnceCell is owned as DocServer instance state (alongside the existing model field), NOT a module-level global, preserving per-instance Clone-via-Arc test isolation.
- search_semantic AND research_topic return the SAME distinct machine-readable retryable 'loading' signal during the window (a stable error code/kind an agent can branch on, not prose only) — not a silent degrade or a misleading 'disabled' message.
- After first lazy load, semantic search returns correct results.
- If handler signatures change from sync fn to async fn, the existing synchronous server unit tests in src/mcp/server.rs are UPDATED to equivalent async tests (or shielded by a sync-compatible wrapper) — they cannot be asserted to 'pass unchanged' against a changed signature.
- fmt + clippy pedantic + tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
CONDITIONAL on T0/T1 evidence implicating handshake LATENCY (H1) rather than an early exit (H0). Defer ONLY the embedding-model load off the handshake critical path: load the candle all-MiniLM-L6-v2 model lazily via tokio::sync::OnceCell + spawn_blocking so `rmcp::serve_server` binds STDIO and answers initialize/tools/list immediately. Make the affected tool handlers async and return a distinct RETRYABLE 'model still loading' error during the load window (not the existing 'semantic search is disabled' message), and stop research_topic from silently degrading to unranked text search while the model is loading. Because the handlers become async, the existing synchronous server unit tests cannot 'pass unchanged': update them to equivalent async tests or add a sync-compatible wrapper, and state which approach is taken.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Surfaces: src/embed/resolver.rs (resolve_embedding_model), src/mcp/server.rs (search_semantic ~362-368, research_topic ~501-506 silent-fallback, handler signatures). Tool handlers are currently sync fn; awaiting a OnceCell requires async handlers + spawn_blocking for the load rather than blocking a tokio worker. Changing handlers to async breaks the current sync unit tests' call shape, so those tests must be migrated to async (or a sync-compatible wrapper added) — do not claim they pass unchanged. Do NOT change get_info protocol version (proven no-op on rmcp 1.5).
<!-- END:implementation-notes -->