---
artifact_type: task
created_at: 2026-08-21T20:14:34.1447524Z
dependencies:
    - 056.014-T
id: 056.005-T
labels:
    - phase:remedy
    - cause:h1-model-lifecycle
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: add shared lazy embedding lifecycle (H1 only)'
updated_at: 2026-08-23T19:24:02.5047978Z
---



<!-- BEGIN:acceptance-criteria -->
- If H1 is not evidenced by T0, move to `done` with `not-needed: H1 not evidenced`.
- Consume 056.014-T's `Loaded`, `Disabled`, and `Failed` outcomes. Add one clone-shared `Uninitialized`/`Loading`/`Ready`/`Disabled`/retryable `Failed` owner in `src/embed/lifecycle.rs`; no module global or bare OnceCell-only model.
- Atomically allow one supervised blocking load. Own/monitor its JoinHandle, translate panic/JoinError to Failed, define cancellation/drop, and serialize `ensure_loading` plus retry transitions so detached work cannot overwrite state.
- Own only the embedding lifecycle state machine. The versioned MCP availability projection and its `src/mcp/server.rs` consumers (`search_semantic` / `research_topic`) are owned by 056.025-T; expose only a stable typed lifecycle-state accessor for 056.025-T to project from.
- Write and observe failing tests before implementation, then green exactly three groups: single-start concurrency, terminal-Disabled plus drop/cancel behavior, and failure/panic/retry/eventual Ready.
- Do not edit `cmd_serve`, background sync, or `src/mcp/server.rs` here; 056.025-T owns the versioned availability projection and 056.015-T injects this owner into `cmd_serve` and background sync. Rust 1.75 and all targeted gates pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Conditional H1 core lifecycle in the embedding domain. Add one supervised lazy embedding-lifecycle state machine owner consuming typed resolver outcomes; 056.025-T owns the versioned MCP availability projection and 056.015-T owns serve/background-sync wiring.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Own `src/embed/lifecycle.rs` only. Tokio feature changes are explicit and MSRV-checked. The versioned MCP availability projection and `src/mcp/server.rs` consumers are 056.025-T; resolver semantics remain 056.014-T; `cmd_serve`/background-sync orchestration remains 056.015-T.
<!-- END:implementation-notes -->