---
artifact_type: task
created_at: 2026-08-21T22:49:59.990015Z
dependencies:
    - 056.002-T
id: 056.010-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: operational remediation of the H0c fail-closed cause (H0c only)'
updated_at: 2026-08-22T00:35:00.0000000Z
---



<!-- BEGIN:acceptance-criteria -->
- CONDITIONAL - H0c-only. If T0 does NOT evidence a fail-closed gate as the handshake blocker (i.e. evidence selects H0a/H0b/H1), close as not-needed with a one-line rationale; closing as not-needed satisfies 056.004-T's dependency on it.
- The diagnosability sink (056.006-T) surfaces the cause but CANNOT remediate it; without this task the H0c branch cannot reach the healthy handshake 056.004-T requires (Copilot review P1: H0c closure).
- Do NOT weaken any fail-closed gate. Provide a TRACKED operational remediation that repairs the workspace STATE so serve legitimately passes the gate and completes initialize: malformed registry -> fix/replace sources.yaml (or point --config at a valid registry); missing explicit --config -> supply the intended registry (or fall through to zero-config .graphtor/*.db auto-discovery when that is the real intent); pre-v4 schema -> run graphtor-docs upgrade (pre-v4->v4) and verify; duplicate intake -> resolve the duplicate source targeting and re-run.
- The remediation is recorded as an operational recipe in 056.001-T. Any code change to make the cause more ACTIONABLE (e.g. a clearer error naming the offending registry/schema) is bounded to the evidenced cause and preceded by its own red test; the fail-closed behavior itself is never relaxed.
- Verification: after remediation, serve reaches a healthy initialize handshake with no OS error 232 (the same success signal 056.004-T verifies).
- Exactly one H0c sub-cause is the evidenced blocker; non-selected sub-causes close not-needed, but the SELECTED branch MUST reach 056.004-T (it does not close not-needed).
- Safety (strict-safety, S7): ProposedAction = tracked operational remediation that repairs the evidenced fail-closed workspace STATE (fix/replace sources.yaml, supply the intended --config, run the pre-v4->v4 upgrade, or resolve the duplicate intake) plus, only if needed, a bounded actionability code change preceded by its own red test. ActionRisk = high — it touches workspace state or a schema upgrade and could damage data if mishandled, so take a backup of the affected sources.yaml / DB before mutating and require operator approval for a schema upgrade or registry replacement. NEVER weaken or fail-open any gate (Plan Hardening invariant 2). rollback = restore the pre-remediation backup and revert any actionability commit. Recorded in the plan's Risky actions section.
- fmt + clippy pedantic (-D warnings) + tests pass (if any code change is taken).
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
CONDITIONAL on T0 evidencing a legitimate fail-closed gate as the H0c handshake blocker (malformed registry, missing explicit --config, pre-v4 schema, duplicate intake, or another fail-closed cause that exits cmd_serve before the transport binds). Provide a tracked operational remediation that repairs the workspace state so serve passes the gate and completes initialize WITHOUT weakening the gate; the diagnosability sink (056.006-T) alone cannot remediate. Record the remediation recipe in 056.001-T; if a bounded code change is needed to make the cause more actionable, precede it with its own red test. Verify the healthy initialize handshake. Close as not-needed if T0 does not evidence H0c.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Surfaces: the evidenced fail-closed gate in cmd_serve's pre-serve path (src/main.rs ~2446-2655: invalid source registry -> hard error; missing explicit --config -> exit 2; pre-v4 schema gate; duplicate-intake preflight) plus operational state repair. Do NOT convert any fail-closed gate into a fail-open path (Plan Hardening invariant 2). This is the curative owner for the H0c branch so 056.004-T's connectivity gate is satisfiable; diagnosability (056.006-T) is complementary, not a substitute. Depends on the T1 harness (056.002-T) to reproduce/verify the handshake.
<!-- END:implementation-notes -->