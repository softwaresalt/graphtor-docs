---
artifact_type: subtask
created_at: 2026-08-17T06:45:49.5908499Z
dependencies:
    - 055.001.001-ST
id: 055.001.002-ST
labels:
    - dark-mode
    - serve-discovery
    - code
    - pr90
    - binary-classifier
    - post-cap-residual
parent_id: 055.001-T
references:
    - docs/exec-plans/2026-08-16-serve-auto-discovery-followups-plan.md
status: queued
title: Stream ingestible-content classifier via shared FileFilter (full-error-observing)
updated_at: 2026-08-17T07:01:04.8099073Z
---

Post-cap residual decomposition (finding E) — BINARY half. DEPENDS ON 055.001.001-ST (the shared graphtor_core FileFilter must exist first). Refactor source_has_ingestible_content (src/workspace/serve_discovery.rs:333) into a streaming boolean that reuses the shared FileFilter from graphtor_core::acquire instead of building a relative_candidates Vec and calling filter_files once per file. Keep iterating the ENTIRE WalkDir and keep returning false on ANY walk error (fail-closed, unchanged). A traversal short-circuit (return true on first eligible file) is REJECTED: it would skip later walk errors and escalate a partially-unreadable read-only source to the read-WRITE Generation posture. Return found only when the walk completed without error.

Width isolation: graphtor-docs BINARY crate ONLY (src/workspace/serve_discovery.rs streaming refactor + its characterization/differential/warning tests). The library FileFilter API is 055.001.001-ST.

Acceptance (incorporates finding B — O(1) candidate COUNT + structured input_files parity + exact warning cases):
1) Full error-observing walk retained; returns false on any WalkDir error.
2) No per-file Vec; memory no longer scales with document count. Retain an O(1) format-candidate COUNT (an integer counter, NOT merely a boolean): a boolean-only reduction is INSUFFICIENT because it cannot distinguish a zero-candidate tree from a candidates-all-excluded tree, which the aggregate-warning decision depends on.
3) Preserve the input_files aggregate-warning field: the old single batch filter_files emits its S032 warning as warn!(input_files = files.len(), "...all files were excluded") (src/acquire/filter.rs:61-65) — i.e. input_files is a SCALAR COUNT (the number of format-matching candidate files), NOT a per-file path Vec. The streaming implementation MUST carry the SAME scalar input_files count on its single aggregate warning, reproduced from the retained O(1) candidate counter (AC2); parity is exact and testable and MUST NOT be achieved by retaining a per-file Vec (that would defeat AC2's O(1) memory goal).
4) Classification identical to before: differential test vs old filter_files(&all)-then-non-empty over nested paths, multi-segment / union include globs, empty include, and exclude-wins.
5) Regression: an eligible file encountered BEFORE an unreadable subtree returns false, driven through an injected ordered walk-result seam (WalkDir order is unspecified).
6) Aggregate 'all files excluded' warning parity is exact and testable: (a) an excluded-only tree (format-matching candidates existed, none passed) emits EXACTLY ONE aggregate warning carrying the scalar input_files count; (b) an ingestible source (>=1 candidate passes) emits ZERO warnings; (c) a walk error emits NO misleading warning (fail closed to false, no aggregate 'all excluded' message); (d) a zero-candidate tree (no format-matching files at all) emits NO misleading aggregate 'all files excluded' warning. NO per-file warnings in any case.
7) cargo test + cargo clippy --all-targets -- -D warnings -D clippy::pedantic green.