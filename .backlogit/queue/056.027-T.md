---
artifact_type: task
created_at: 2026-08-23T20:44:50.9273783Z
dependencies:
    - 056.026-T
    - 056.018-T
id: 056.027-T
labels:
    - phase:remedy
    - cause:discriminator
    - selection:pending
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Deliver reconciled MCP config discriminator to existing installs
updated_at: 2026-08-23T21:25:43.2092275Z
---

Bounded existing-install delivery of the reconciled `type`/`transport` discriminator, split out so the discriminator remedy ships without the H0a/H3-B1 cwd chain. Composes 056.017-T typed config actions and 056.018-T safe no-follow recovery handles in `cmd_upgrade` to refresh ONLY the marked managed entry's stdio discriminator field. Depends on the discriminator generation task (056.026-T) and 056.018-T; does NOT depend on 056.008-T (cwd generation) or 056.009-T (cwd/recovery delivery). Selected together with 056.026-T when a mismatch is evidenced and existing installs need refresh; not-needed otherwise.

<!-- BEGIN:acceptance-criteria -->
- Run only when the discriminator generation task (056.026-T) is selected (a `type`/`transport` mismatch is evidenced) and existing installs need the reconciled discriminator; otherwise move to `done` with `not-needed: discriminator existing-install delivery not selected`.
- Depend on 056.026-T (for the reconciled generated value) and on 056.018-T (safe no-follow recovery), transitively 056.024-T/056.017-T. Do NOT depend on 056.008-T (cwd generation) or 056.009-T (cwd/recovery delivery). When this task is selected, 056.017-T/056.024-T/056.018-T are also selected because their activation predicates include discriminator existing-install delivery, so no prerequisite closes not-needed while this task still needs its typed API or recovery handles.
- Compose 056.017-T typed config actions and 056.018-T recovery handles in `cmd_upgrade` to refresh ONLY the marked/exact-legacy managed entry's stdio discriminator field: preserve unowned/non-JSON bytes, treat user collision as a typed non-mutating outcome, keep containment a hard `PathViolation`, and back up before any mutation through the verified handle.
- Own only the discriminator-refresh portion of the `cmd_upgrade` mcp_config action, width-separated from 056.009-T's cwd/recovery refresh; do not add cwd/working-directory plumbing, and do not require 056.008-T or 056.009-T.
- Write and observe red tests first, then green exactly three groups: nested marked/legacy discriminator refresh with recovery metadata; no-change/collision/non-JSON preservation without backup; and recovery/mutation failure with original bytes intact.
- On a shipping discriminator remedy unit, own exactly ONE exact-client re-probe after applying the delivery (shipment-interface re-probe rule). Before T4 on a discriminator branch, run the delivered discriminator refresh against the target workspace UNDER THE APPLICABLE OPERATOR APPROVAL (a destructive managed-`.mcp.json` mutation: backup-first through the verified 056.018-T handle, per Principle VII), record the post-refresh production config hash, and retain the recovery artifact.
- T4 exclusively owns restored-production `/mcp show` acceptance; 056.013-T owns discriminator docs. Rust 1.75 and all gates pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Paired with 056.026-T (discriminator generation). `workspace::upgrade::upgrade` stays binary-only; `cmd_upgrade` composes 056.017-T typed API + 056.018-T recovery handles for a discriminator-only refresh of marked managed entries, width-separated from 056.009-T (cwd/recovery delivery). Created because 056.009-T cannot own discriminator delivery without requiring the H0a/H3-B1 cwd chain (it closes not-needed when 056.008-T is unselected). 056.017-T/056.024-T/056.018-T activation predicates are broadened to include this discriminator delivery branch so they produce their APIs/handles rather than closing not-needed while this task depends on them. The existing-install refresh is a destructive managed-config mutation: backup-first via the verified handle and operator-approval-gated before T4 (Principle VII), mirroring 056.009-T.
<!-- END:implementation-notes -->