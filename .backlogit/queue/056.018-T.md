---
artifact_type: task
created_at: 2026-08-22T20:46:21.0252135Z
dependencies:
    - 056.024-T
id: 056.018-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: add contained MCP config recovery artifacts'
updated_at: 2026-08-22T21:40:14.3067676Z
---



<!-- BEGIN:acceptance-criteria -->
- Run only for an evidenced H0a prerequisite or H3-B1; otherwise move to `done` with `not-needed: managed config recovery not selected`.
- Depend on 056.024-T and implement ONLY the primitive that 056.024-T selected. If 056.024-T returned blocked (no compliant MSRV-safe primitive), this task and the shipment stay blocked; do not fall back to an unsafe or check-then-open path on the sensitive recovery path.
- Write and observe failing tests before implementation, then green exactly three groups: contained handle-based creation, collision/permission failure before mutation, and exact restore including prior absence.
- Add a lazy `.graphtor/recovery/` path accessor without adding it to standard footprint/prune/uninstall enumeration; create it only when bytes would change.
- Implement the selected no-follow / reparse-safe file-mutation primitive in `graphtor_core::path` (e.g. `src/path/handle.rs`); the workspace path accessor stays in `src/workspace/paths.rs`. Operate every `.mcp.json` / recovery-parent / backup read/write/restore through the verified handle, stage replacement bytes owner-only, revalidate parent/destination identity, and abort on any identity change or canonical escape. Do NOT assert that std already provides handle-level no-follow; use only the 056.024-T-decided primitive. Check-then-open alone is insufficient.
- Create directories/files with owner-protected ACLs or stricter source permissions, never overwrite, and return typed recovery handles/metadata. Cleanup stays operator-controlled through T4.
- Do not edit `mcp_config.rs`, install/install-full, uninstall, doctor, or `cmd_upgrade`; 056.009-T alone composes this primitive for upgrade. Preserve crate-root `#![forbid(unsafe_code)]` — no unsafe exemption. Rust 1.75 and all targeted gates pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Conditional H0a/H3-B1 recovery primitive implementing ONLY the 056.024-T-selected safe no-follow / reparse-safe primitive in `graphtor_core::path` (e.g. `src/path/handle.rs`), with the workspace path accessor remaining in `src/workspace/paths.rs`. Create and restore owner-protected managed-config backups through the verified handle without widening install/uninstall/doctor behavior and without any unsafe exemption.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Own the selected no-follow primitive in `graphtor_core::path` (`src/path/handle.rs`) plus a bounded workspace recovery module and a lazy path accessor in `src/workspace/paths.rs`. Implement only the 056.024-T decision and preserve `#![forbid(unsafe_code)]`. 056.009-T consumes handles from `cmd_upgrade`; 056.017-T remains the sole managed-config API owner.
<!-- END:implementation-notes -->