---
artifact_type: task
created_at: 2026-08-25T06:18:10.8074399Z
custom_fields:
    blocked_reason: TRANSITIVE dependency on 059.008-T (U8) BLOCKED, via its direct dependencies 059.003-T (U3), 059.004-T (U4), 059.006-T (U6), and 059.009-T (U9) -- U6 and U9 each directly depend on U8; U3/U4 are themselves gated on U6/U9 (see their own blocked_reason). Cannot start until all four unblock. Returned blocked by Ship (051-S) pending Stage re-deliberation of the engine-boundary approach.
dependencies:
    - 059.003-T
    - 059.004-T
    - 059.006-T
    - 059.009-T
id: 059.005-T
labels:
    - security
    - tests
    - test-first
    - filesystem
    - store
parent_id: 059-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-store-toctou-nofollow-handle-plan.md
    - docs/decisions/2026-08-24-store-toctou-nofollow-handle-deliberation.md
status: blocked
title: 'U5: Cross-platform sidecar swap-resistance matrix + per-platform fail-closed signal + junction refusal (tests)'
updated_at: 2026-08-29T21:23:13.7446408Z
---

Add colocated tests covering only the sidecar swap-resistance DELTAS that U3/U4 do not already assert (reference, do not re-implement, the U3/U4 cases). Bounded to at most three test scenarios: (a) the full sidecar-type matrix (-wal/-shm/-journal) fail-closed refusal on both open paths (EngineReadonlyGuard lock and clear_stale_readonly_lock probe/clear); (b) the platform fail-closed signal is asserted correctly per platform (Unix O_NOFOLLOW -> ELOOP open failure; Windows reparse open -> explicit FILE_ATTRIBUTE_REPARSE_POINT refusal via the shared should_refuse_reparse predicate from U2); (c) reuse the try_symlink_file unprivileged-skip pattern and add a Windows junction variant so the Windows refusal path executes on unprivileged CI where possible, skipping (not failing) where symlink/reparse creation is unprivileged and reporting executed-vs-skipped. The Windows retained-handle engine non-interference check (former delta (d)) and the broader non-name-surrogate reparse regression (former delta (e)) are split into U10 (059.010-T); the deterministic normal-CI should_refuse_reparse predicate unit test and the Windows literal-equality / single-source structural proof (former deltas (f)/(g)) are split into U11 (059.011-T). Reference (do not re-implement) U6 intermediate-directory and U9 engine-open coverage; this task depends on both.

<!-- BEGIN:acceptance-criteria -->
- Delta (a) matrix (sidecar types -wal/-shm/-journal x both open paths) fail-closed refusal passes on the host platform.
- Delta (b) per-platform fail-closed signal is asserted: Unix O_NOFOLLOW -> ELOOP open failure; Windows reparse open -> explicit FILE_ATTRIBUTE_REPARSE_POINT refusal through the shared should_refuse_reparse predicate.
- Delta (c) suite skips (not fails) where the platform refuses unprivileged symlink/reparse creation; the Windows junction variant covers the Windows refusal where symlink creation is denied; executed-vs-skipped is reported.
- Bounded to exactly these three sidecar swap-resistance scenarios ((a)-(c)). The Windows retained-handle engine non-interference assertion (former delta (d)) and the broader non-name-surrogate reparse regression (former delta (e)) are owned by U10 (059.010-T); the deterministic should_refuse_reparse predicate unit test and the Windows literal-equality / single-source structural proof (former deltas (f)/(g)) are owned by U11 (059.011-T); only referenced here.
- Intermediate-directory swap coverage is provided by U6 and engine-open containment by U9; both are only REFERENCED here.
- Passes clippy::pedantic -D warnings; no unwrap/expect; safe Rust 1.75; #![forbid(unsafe_code)] preserved.
<!-- END:acceptance-criteria -->
