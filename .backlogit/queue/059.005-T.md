---
artifact_type: task
created_at: 2026-08-25T06:18:10.8074399Z
dependencies:
    - 059.003-T
    - 059.004-T
id: 059.005-T
labels:
    - security
    - tests
    - test-first
    - filesystem
    - store
parent_id: 059-F
priority: medium
references:
    - docs/exec-plans/2026-08-24-store-toctou-nofollow-handle-plan.md
    - docs/decisions/2026-08-24-store-toctou-nofollow-handle-deliberation.md
status: queued
title: 'U5: Cross-platform swap-resistance deltas + Windows handle-mode validation (tests)'
updated_at: 2026-08-25T07:32:00.1049398Z
---

<!-- BEGIN:description -->
Add colocated tests covering only the DELTAS that U3/U4 do not already assert (reference, do not re-implement, the U3/U4 cases): (a) the full sidecar-type matrix (-wal/-shm/-journal) fail-closed refusal on both open paths; (b) the platform fail-closed signal is asserted correctly per platform (Unix O_NOFOLLOW -> ELOOP open failure; Windows reparse open -> explicit FILE_ATTRIBUTE_REPARSE_POINT refusal); (c) reuse the try_symlink_file unprivileged-skip pattern and add a Windows junction variant so the Windows refusal path executes on unprivileged CI where possible; (d) validate the Windows retained handle (FILE_READ_ATTRIBUTES|FILE_WRITE_ATTRIBUTES, full share mode) does NOT block the engine's own db/WAL open -- scoped to that single assertion (Option A retained-handle vs the documented Option C identity-verified re-open fallback decision; record the outcome in the PR); (e) broader-fail-closed-policy regression (Windows): create a legitimate NON-redirecting reparse file whose reparse tag is NOT a name surrogate (a non-name-surrogate reparse point creatable without the symlink privilege where feasible) and assert the guarded open_no_follow path REFUSES it, proving the explicitly broader fail-closed policy adopted in U2 rather than a narrow name-surrogate-only check, so the intentional Windows/Unix breadth asymmetry is pinned by a test rather than left implicit. Skip GRACEFULLY (skip, not fail) when the test setup lacks the required privilege/support to create such a reparse file on unprivileged CI; in that case the doc-comment's accepted-breadth statement stands as the recorded behavior. Report executed-vs-skipped. Consistent with docs/exec-plans/2026-08-24-store-toctou-nofollow-handle-plan.md U5 delta (e).
<!-- END:description -->

<!-- BEGIN:acceptance-criteria -->
- Delta matrix (sidecar types x both open paths x per-platform fail-closed signal) passes on the host platform.
- Suite skips (not fails) where the platform refuses unprivileged symlink creation; the Windows junction variant covers the Windows refusal where symlink creation is denied.
- The engine-open non-interference assertion (d) passes, confirming the retained handle share/access mode does not block the engine db/WAL open.
- Delta (e) broader-fail-closed regression: a legitimate non-name-surrogate / non-redirecting reparse file is REFUSED by open_no_follow, proving the explicitly broader fail-closed policy (not a narrow name-surrogate-only check) and pinning the intentional Windows/Unix breadth asymmetry; the test skips gracefully (skip, not fail) when the setup lacks the privilege/support to create the reparse file, and whether it executed or skipped is reported.
- Whether the Windows refusal test executed or skipped is reported; the Windows handle-mode decision (Option A vs C) is recorded in the PR.
- Passes clippy::pedantic -D warnings; no unwrap/expect.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Depends on U3 and U4 (references their scenarios; does not duplicate them). Prefer colocated #[cfg(test)] tests; add a single narrowly-scoped tests/ integration file only if a full engine open is required for assertion (d).
<!-- END:implementation-notes -->