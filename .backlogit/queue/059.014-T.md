---
artifact_type: task
created_at: 2026-08-30T00:30:14.6891231Z
id: 059.014-T
labels:
    - security
    - toctou
    - residual-risk
    - sign-off
    - gate
parent_id: 059-F
priority: high
references:
    - docs/decisions/2026-08-29-store-toctou-engine-boundary-redeliberation-deliberation.md
status: queued
title: 'Operator sign-off: accept engine-open leaf + intermediate-directory redirection residual risk (gate before 059-F implementation)'
updated_at: 2026-08-30T09:09:01.5390237Z
---

<!-- BEGIN:description -->
NON-CODE approval gate. Implementation of the rescoped 059-F (U1 onward) MUST NOT begin until this task is done. Requires explicit operator sign-off on the Accepted-Residual-Risk Record in the 2026-08-29 re-deliberation decision: the cozo bare-path engine-open redirection — reachable by a **leaf swap as well as an intermediate-directory swap** (Accepted-Residual-Risk Record, decision lines 328–347) — is accepted as a bounded residual, with compensating controls, for the single-developer local-only trust model. Sign-off must NOT be read as a claim that the original Principles III/IV fail-closed DoD passed. On sign-off, record approver + date and the design-doc reference; then U1 (059.001-T) may proceed.
<!-- END:description -->

<!-- BEGIN:acceptance-criteria -->
- The operator explicitly signs off on the Accepted-Residual-Risk Record in `docs/decisions/2026-08-29-store-toctou-engine-boundary-redeliberation-deliberation.md`.
- PRECONDITION — Control #3 operational guidance MUST be committed before sign-off can close: the serve trust-boundary design doc (`docs/design-docs/2026-07-15-consumption-first-serve-and-trust-boundary.md`) MUST state that every store-opening command — `serve`, every write-mode command that reaches `DataStore::open_sqlite` (`sync` and `prewarm` via `with_locked_database_store`), AND every read-only command that reaches `DataStore::open_sqlite_readonly` (`status` and every query subcommand) — requires the workspace **root directory namespace, the workspace root's own parent directory (ambiently trusted by the U7 bootstrap; `open_dir_nofollow` cannot reject a real-directory swap of the root), and every parent directory component leading to each database** — as well as the leaf entries — to be non-attacker-writable; MUST explain that replacing a leaf requires write/delete/rename authority on its parent directory (so protecting only the leaf's write bit is insufficient, and for a `root/graph.db` layout the root directory itself is the relevant parent); MUST NOT limit the requirement to an active serve window or to write-mode commands (it MUST cover the read-only `status` and query subcommand opens that reach the same bare-path `open_sqlite_instance` constructor via `DataStore::open_sqlite_readonly`); and MUST describe the cozo bare-path engine-open re-resolution residual. (Landed 2026-08-29 in this re-deliberation PR under the "Operator trust boundary: workspace directory write access" subsection; sign-off verifies its presence.) Sign-off MUST NOT close while this guidance is absent.
- On sign-off, record the approver, the date, and the design-doc reference in this item.
- Sign-off is NOT a claim that the original Principles III/IV fail-closed DoD passed; it accepts the named engine-open residual only (redirection reachable by a leaf swap as well as an intermediate-directory swap), with the tracked closure path `059.013-T`.
<!-- END:acceptance-criteria -->


<!-- BEGIN:stage-ratification -->
Stage convergence ratification (2026-08-30, PR #114, durable tracked evidence).

**Gate state.** Stage independently confirms `059.014-T` status is `queued` and is the SOLE
operator sign-off gate for the rescoped `059-F` scope. It is NOT `done` and NOT bypassed. The gate
acts structurally as the `blocks` dependency edge `059.001-T <- 059.014-T`.

**What this ratification does and does not cover — the two acts are on different clocks.**

* **Already completed and ratified (NOT gated on this sign-off):** the `blocked -> queued` status
  normalization of the covering feature `059-F` and the feasible units
  `059.001/002/003/004/005/006/010/011-T`, plus the feasible-DAG rewire (U8/U9 edge drops, U12
  re-point). Ship performed that normalization in violation of fail-closed P-010; Stage
  independently reviewed it on 2026-08-30 and ratified the resulting `queued` dispositions as
  semantically correct **while this gate is still `queued`**. This ratification exists specifically
  to affirm that already-normalized state. It does **not** retroactively legalize the Ship
  mutation. Normalization was never conditioned on sign-off: a `queued`-but-not-ready status is
  exactly the intake-valid disposition that lets this gate function as a dependency edge, so
  ratifying it grants no early execution authority.
* **Still gated on this sign-off (NOT started):** successor-shipment assembly under Stage Step 5.5,
  and implementation of the rescoped `059-F` (U1 onward). Neither may begin until this task is
  `done`.

**Ownership after sign-off.** Stage (not Ship) exclusively owns any further scope normalization and
the successor-shipment assembly. Because the ten near-term items were harvested in an earlier
session, Step 5.5's default Mode H harvest-only scope guard cannot admit them; the durable **Mode R**
authorization — covering feature `059-F`; exact `handoff_ids` `059-F, 059.001-T, 059.002-T,
059.003-T, 059.004-T, 059.005-T, 059.006-T, 059.010-T, 059.011-T, 059.014-T`; parent-first assembly
order `059-F -> 059.014-T -> 059.001-T -> 059.002-T -> 059.006-T -> 059.003-T -> 059.004-T ->
059.005-T -> 059.010-T -> 059.011-T`; exclusions `059.008-T` (terminal), `059.009-T` (deferred),
`059.012-T` / `059.013-T` (later shipments); and `059.007-T` (`done`/archived) cited as an
intentional terminal prerequisite rather than a member — is recorded in
`docs/decisions/2026-08-30-stage-ratify-059-f-normalization-ownership-deliberation.md`
§ *Mode R Authorization for Successor-Shipment Assembly*. Stage must re-validate that exact set at
assembly time and must never manufacture a stash entry or synthetic harvest.

**Evidence durability.** This tracked body section is the durable source-of-truth record; the
earlier `backlogit comment add` ratification lived only in gitignored `.backlogit/logs/*.jsonl` and
the disposable index and is therefore non-authoritative for PR evidence. No status change, no
dependency change, no sign-off marked done.
<!-- END:stage-ratification -->