---
artifact_type: task
created_at: 2026-08-30T00:30:14.6891231Z
id: 059.014-T
labels:
    - security
    - toctou
    - residual-risk
    - sign-off
    - gate
parent_id: 059-F
priority: high
references:
    - docs/decisions/2026-08-29-store-toctou-engine-boundary-redeliberation-deliberation.md
status: queued
title: 'Operator sign-off: accept engine-open leaf + intermediate-directory redirection residual risk (gate before 059-F implementation)'
updated_at: 2026-08-30T09:57:40.2350702Z
---

<!-- BEGIN:description -->
NON-CODE approval gate. Implementation of the rescoped 059-F (U1 onward) MUST NOT begin until this task is done. Requires explicit operator sign-off on the Accepted-Residual-Risk Record in the 2026-08-29 re-deliberation decision: the cozo bare-path engine-open redirection — reachable by a **leaf swap as well as an intermediate-directory swap** (Accepted-Residual-Risk Record, decision lines 328–347) — is accepted as a bounded residual, with compensating controls, for the single-developer local-only trust model. Sign-off must NOT be read as a claim that the original Principles III/IV fail-closed DoD passed. On sign-off, record approver + date and the design-doc reference; then U1 (059.001-T) may proceed.
<!-- END:description -->

<!-- BEGIN:acceptance-criteria -->
- The operator explicitly signs off on the Accepted-Residual-Risk Record in `docs/decisions/2026-08-29-store-toctou-engine-boundary-redeliberation-deliberation.md`.
- PRECONDITION — Control #3 operational guidance MUST be committed before sign-off can close: the serve trust-boundary design doc (`docs/design-docs/2026-07-15-consumption-first-serve-and-trust-boundary.md`) MUST state that every store-opening command — `serve`, every write-mode command that reaches `DataStore::open_sqlite` (`sync` and `prewarm` via `with_locked_database_store`), AND every read-only command that reaches `DataStore::open_sqlite_readonly` (`status` and every query subcommand) — requires the workspace **root directory namespace, the workspace root's own parent directory (ambiently trusted by the U7 bootstrap; `open_dir_nofollow` cannot reject a real-directory swap of the root), and every parent directory component leading to each database** — as well as the leaf entries — to be non-attacker-writable; MUST explain that replacing a leaf requires write/delete/rename authority on its parent directory (so protecting only the leaf's write bit is insufficient, and for a `root/graph.db` layout the root directory itself is the relevant parent); MUST NOT limit the requirement to an active serve window or to write-mode commands (it MUST cover the read-only `status` and query subcommand opens that reach the same bare-path `open_sqlite_instance` constructor via `DataStore::open_sqlite_readonly`); and MUST describe the cozo bare-path engine-open re-resolution residual. (Landed 2026-08-29 in this re-deliberation PR under the "Operator trust boundary: workspace directory write access" subsection; sign-off verifies its presence.) Sign-off MUST NOT close while this guidance is absent.
- On sign-off, record the approver, the date, and the design-doc reference in this item.
- Sign-off is NOT a claim that the original Principles III/IV fail-closed DoD passed; it accepts the named engine-open residual only (redirection reachable by a leaf swap as well as an intermediate-directory swap), with the tracked closure path `059.013-T`.
<!-- END:acceptance-criteria -->


<!-- BEGIN:stage-ratification -->
Stage convergence ratification (2026-08-30, PR #114, durable tracked evidence).

**Gate state.** Stage independently confirms `059.014-T` status is `queued` and is the SOLE
operator sign-off gate for the rescoped `059-F` scope. It is NOT `done` and NOT bypassed. The gate
acts structurally as the `blocks` dependency edge `059.001-T <- 059.014-T`.

**This task is an external prerequisite, never a shipment member.** Under the Step 5.5 Mode R
contract in `.github/agents/_stage.agent.md`, the authorization names two disjoint exact sets:
`member_ids` (the 8 shipment members - the eight implementation tasks
`059.001/002/006/003/004/005/010/011-T`, in dependency order) and `prerequisite_ids` (exactly `059.007-T` and this task,
`059.014-T`). The covering feature `059-F` is in neither set: this is a partial-feature shipment,
so `059-F` stays in the P-015 protected set. Prerequisites are gates: they are satisfied, never
shipped, and never converted into members to make assembly proceed. This task therefore:

* is **not** part of `assembly_ids` and never appears in the successor-shipment manifest or in the
  assembly order;
* leaves the queue on `done` - after operator sign-off it moves to `done` and is archived, so it
  could never satisfy the live-queue member validation in Step 5.5 condition 2a(a). Treating it as
  a member would force either a validation failure or an illegitimate exception;
* was **never normalized**. It has been `queued` since creation, was never `blocked`, and is not
  one of the nine normalized members. A count that reaches ten by adding this task to the nine
  members is conflating the member set with the gate set;
* gates assembly through its dependency edge, not through membership. Assembly stays blocked while
  any `prerequisite_ids` entry is unsatisfied: `059.007-T` is already satisfied
  (`done`/archived - absence from the live queue is the expected shape of a satisfied prerequisite,
  not a validation failure), and this task is not.

`handoff_ids` in the authorization is only the 10-ID auditable union of the two sets. It is a
citation convenience and is never the assembly set.

**What this ratification does and does not cover - the two acts are on different clocks.**

* **Already completed and ratified (NOT gated on this sign-off):** the `blocked -> queued` status
  normalization of the nine normalized members - covering feature `059-F` plus the eight
  implementation tasks `059.001/002/003/004/005/006/010/011-T` - plus the feasible-DAG rewire
  (U8/U9 edge drops, U12 re-point). Ship performed that normalization in violation of fail-closed
  P-010; Stage independently reviewed it on 2026-08-30 and ratified the resulting `queued`
  dispositions as semantically correct **while this gate is still `queued`**. This ratification
  exists specifically to affirm that already-normalized state. It does **not** retroactively
  legalize the Ship mutation. Normalization was never conditioned on sign-off: a
  `queued`-but-not-ready status is exactly the intake-valid disposition that lets this gate function
  as a dependency edge, so ratifying it grants no early execution authority.
* **Still gated on this sign-off (NOT started):** successor-shipment assembly under Stage Step 5.5,
  and implementation of the rescoped `059-F` (U1 onward). Neither may begin until this task is
  `done`.

**Ownership after sign-off.** Stage (not Ship) exclusively owns any further scope normalization and
the successor-shipment assembly. Because the members were harvested in an earlier session, Step
5.5's default Mode H harvest-only scope guard cannot admit them; the durable **Mode R**
authorization - covering feature `059-F` (a **partial-feature** covering root: it retains five live
children outside the manifest, so under P-015 it is held in the protected set and is **never** a
member); `member_ids` exactly the 8 task IDs `059.001-T, 059.002-T, 059.006-T, 059.003-T,
059.004-T, 059.005-T, 059.010-T, 059.011-T`, which are also the parent-first assembly order;
`prerequisite_ids` exactly `059.007-T` and `059.014-T`; `handoff_ids` the 10-ID audit union of
those two disjoint sets; exclusions
`059.008-T` (terminal), `059.009-T` (deferred), `059.012-T` / `059.013-T` (later shipments) - is
recorded in
`docs/decisions/2026-08-30-stage-ratify-059-f-normalization-ownership-deliberation.md`
§ *Mode R Authorization for Successor-Shipment Assembly*. Stage must re-validate each set against
its own rules at assembly time and must never manufacture a stash entry or synthetic harvest.

**Evidence durability.** This tracked body section is the durable source-of-truth record. The first
convergence pass recorded its ratification only as local-only, gitignored `backlogit comment add`
history in `.backlogit/logs/*.jsonl` plus the disposable index; a later persistence-correction pass
wrote the tracked `stage-ratification` sections that are authoritative for PR evidence. No status
change, no dependency change, no sign-off marked done.
<!-- END:stage-ratification -->