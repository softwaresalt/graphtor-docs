---
artifact_type: task
created_at: 2026-08-22T20:45:26.9684311Z
dependencies:
    - 056.025-T
id: 056.015-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: preserve background sync with lazy model state (H1 only)'
updated_at: 2026-08-22T21:37:40.0565442Z
---



<!-- BEGIN:acceptance-criteria -->
- If H1 is not evidenced by T0, move to `done` with `not-needed: H1 not evidenced`.
- Depend on 056.025-T (chain `... 005 -> 025 -> 015 ...`). Own ONLY `cmd_serve` lifecycle injection and background-sync subscription/orchestration; consume the versioned Loading/Failed/Disabled MCP availability projection from 056.025-T and the shared lazy owner from 056.005-T. Do not define projection types or edit `search_semantic`/`research_topic` fallback here (that is 056.025-T).
- After 056.003-T establishes the `cmd_serve` preflight seam, write and observe failing orchestration tests before implementation, then green exactly three groups: read-only initialize before load (the 056.025-T availability projection is present but unmodified); Generation first-failure then serialized retry/Ready/one embed; and loader panic/server-drop cleanup.
- `cmd_serve` creates one owner from 056.005/056.014, passes clones to `DocServer` and `spawn_background_sync`, and never awaits model construction before `serve_server`.
- Either model-dependent consumer calls the shared serialized `ensure_loading`; no consumer starts a separate loader. Background sync remains subscribed across `Failed → Loading → Ready`, may request the one policy-controlled retry, embeds once after Ready, and exits only on Ready completion, Disabled, or owner shutdown.
- Panic/JoinError publishes Failed; server drop cancels/joins owned work without detached completion or perpetual Loading. Disabled preserves no-embedding behavior. Rust 1.75 and all targeted gates pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Conditional H1 serve orchestration after the preflight seam. Own `cmd_serve` lifecycle injection and background-sync subscription/orchestration only: inject one shared lazy embedding owner into `DocServer` and Generation sync, and serialize recovery from a first load failure. The versioned MCP availability projection is consumed from 056.025-T, not defined here.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Own `src/main.rs` serve/background-sync wiring plus one integration surface; the `src/mcp/server.rs` versioned availability projection is owned by 056.025-T (consumed here). 056.003-T must land first (transitively upstream); the lifecycle state machine remains 056.005-T and resolver semantics remain 056.014-T.
<!-- END:implementation-notes -->