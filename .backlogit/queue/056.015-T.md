---
artifact_type: task
created_at: 2026-08-22T20:45:26.9684311Z
dependencies:
    - 056.005-T
id: 056.015-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: preserve background sync with lazy model state (H1 only)'
updated_at: 2026-08-22T21:37:40.0565442Z
---



<!-- BEGIN:acceptance-criteria -->
- If H1 is not evidenced by T0, move to `done` with `not-needed: H1 not evidenced`.
- Own the versioned MCP availability projection and its `src/mcp/server.rs` wiring (moved here from 056.005-T): Loading/Failed share a stable code, status, retryable=true, remediation, and optional retry hint; terminal Disabled is retryable=false with a stable reason. `search_semantic` returns a typed disabled result with no fallback; `research_topic` may use keyword fallback only when its response metadata declares Disabled and `fallback=keyword`; neither falls back while Loading/Failed.
- After 056.003-T establishes the `cmd_serve` preflight seam, write and observe failing orchestration tests before implementation, then green exactly three groups: read-only initialize before load plus the versioned Loading/Failed/Disabled availability-projection matrix; Generation first-failure then serialized retry/Ready/one embed; and loader panic/server-drop cleanup.
- `cmd_serve` creates one owner from 056.005/056.014, passes clones to `DocServer` and `spawn_background_sync`, and never awaits model construction before `serve_server`.
- Either model-dependent consumer calls the shared serialized `ensure_loading`; no consumer starts a separate loader. Background sync remains subscribed across `Failed → Loading → Ready`, may request the one policy-controlled retry, embeds once after Ready, and exits only on Ready completion, Disabled, or owner shutdown.
- Panic/JoinError publishes Failed; server drop cancels/joins owned work without detached completion or perpetual Loading. Disabled preserves no-embedding behavior. Rust 1.75 and all targeted gates pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Conditional H1 serve orchestration and MCP availability projection after the preflight seam. Own the versioned MCP availability projection and `src/mcp/server.rs` wiring, inject one shared lazy embedding owner into MCP handlers and Generation sync, and serialize recovery from a first load failure.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Own `src/main.rs` serve/background-sync wiring and the `src/mcp/server.rs` versioned availability projection (moved from 056.005-T) plus one integration surface. 056.003-T must land first (transitively upstream); the lifecycle state machine remains 056.005-T and resolver semantics remain 056.014-T.
<!-- END:implementation-notes -->