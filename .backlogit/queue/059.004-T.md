---
artifact_type: task
created_at: 2026-08-25T06:17:33.7634387Z
custom_fields:
    source_stash_id: E86A6E56
    source_stash_kind: bug
    source_stash_path: stash.jsonl
    source_stash_priority: high
    source_stash_text: 'Security hardening (POST-CAP residual from PR #96 Copilot review, 2026-08-17): write-mode stale-lock-clearing TOCTOU in clear_stale_readonly_lock (src/db/store.rs:649). SAME check/use race as the EngineReadonlyGuard::lock/Drop symlink-swap TOCTOU tracked in 5905CDEE, but on a DIFFERENT, currently-uncovered code path. In clear_stale_readonly_lock the per-sidecar loop no-follow-checks each candidate with is_reparse_point(&candidate) (symlink_metadata, ~store.rs:665) and THEN, on separate later lines, performs PATH-BASED operations that follow the link: candidate.exists() (~:677), fs::metadata(&candidate) (~:680), and set_readonly(&candidate, false) -> fs::set_permissions(path, ...) (~:685). An attacker with workspace write access can swap a sidecar for a symlink/junction to an external target BETWEEN the is_reparse_point check and the set_permissions call; the write-mode open path would then clear the read-only bit on that external target, and the subsequent write-mode open could proceed against the linked file. 5905CDEE''s remedy is scoped ONLY to EngineReadonlyGuard::lock/Drop (main db index 0 + sidecar capture/restore), so this write-mode clearing path in clear_stale_readonly_lock remains unaddressed. REMEDY (identity-bound, no-follow -- do NOT prescribe another path re-check, which is itself TOCTOU-vulnerable because the path can be swapped between the re-check and set_permissions): open each sidecar candidate once with no-follow/no-reparse semantics (O_NOFOLLOW on Unix; FILE_FLAG_OPEN_REPARSE_POINT / no-follow open on Windows) and drive EVERY permission operation -- the read-only probe AND the read-only clear -- through that retained handle (fchmod / SetFileInformationByHandle); prohibit any path-based chmod fallback; if a no-follow handle cannot be obtained, FAIL CLOSED (refuse the write-mode open) rather than mutating via a re-resolved path. Preserve the existing regression tests (e.g. open_sqlite_refuses_a_dangling_symlinked_wal_sidecar and the refuse-linked-sidecar cases). RELATIONSHIP: this is the same-mechanism sibling of 5905CDEE (guard lock/Drop path) -- RELATES TO 5905CDEE; it was corroborated by the Correctness Reviewer P3 note that flagged TOCTOU specifically on clear_stale_readonly_lock during the 970AE45A/5D98DBCC adversarial review, whereas 5905CDEE (Security Reviewer P1) covers only the guard path. Needs its own spike/deliberation as a security-mechanism change (Constitution Principle III/IV). SCOPE GUARD: do NOT expand shipment 047-S / 048-S or the current dark activation scope to implement this -- deferred, separately-planned security bug. Source: PR #96 post-cap (cycle-3 hard-stop) residual conversion.'
dependencies:
    - 059.002-T
id: 059.004-T
labels:
    - security
    - code
    - test-first
    - filesystem
    - store
links:
    - target_id: 059.003-T
      link_type: related_to
parent_id: 059-F
priority: high
status: queued
title: 'U4: Bind clear_stale_readonly_lock probe/clear to no-follow handles (code, test-first)'
updated_at: 2026-08-25T06:19:10.6683193Z
---

Rewrite clear_stale_readonly_lock to be handle-bound. For each candidate: disambiguate genuinely-absent from present-but-link using symlink_metadata (NotFound -> skip). A present entry whose link/reparse status is set (keyed off the reparse attribute, matching is_reparse_point breadth so Windows JUNCTIONS are caught, not just Unix symlinks) is refused (fail closed), preserving open_sqlite_refuses_a_dangling_symlinked_wal_sidecar. Otherwise open_no_follow the candidate, probe read-only via the handle, and clear via the handle. Remove the standalone is_reparse_point + path-based exists()/metadata/set_readonly sequence. Fail closed on link/reparse or unobtainable handle with a traceable error. The handle uses attribute-level access (U2) so clearing read-only on an already-read-only regular file does not hit ERROR_ACCESS_DENIED on Windows. Consumes stash E86A6E56.

<!-- BEGIN:acceptance-criteria -->
Test-first — 2 NEW scenarios (authored failing first) plus regression re-verification:
- NEW (a) open_sqlite_refuses_to_clear_readonly_through_a_swapped_sidecar_symlink (fail-closed-at-open): a sidecar planted as a link to an external read-only target is refused, the external target is never made writable, AND the open is refused with an error (assert the refusal, not merely the unchanged bit).
- NEW (b) clearing read-only on an already-read-only regular file succeeds through the attribute-access handle (guards the Windows ERROR_ACCESS_DENIED pitfall).
- Regression (must pass unchanged): open_sqlite_refuses_a_dangling_symlinked_wal_sidecar, open_sqlite_clears_a_stale_readonly_lock_left_by_a_crashed_session, and a genuinely-absent sidecar is skipped (no error).
- No path-based set_readonly remains in the function; passes clippy::pedantic -D warnings; no unwrap/expect.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Depends on U2 helpers and U1 deps. Disambiguation must key off the reparse attribute (is_reparse_point breadth) so Windows junctions are caught, not just is_symlink(). Attribute-level handle access (FILE_READ_ATTRIBUTES|FILE_WRITE_ATTRIBUTES) is required to clear the readonly bit on an already-readonly file. Source stash E86A6E56 (PR #96 post-cap residual; Correctness Reviewer P3). Sibling of 5905CDEE / 059.003-T.
<!-- END:implementation-notes -->