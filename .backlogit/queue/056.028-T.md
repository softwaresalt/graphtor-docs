---
artifact_type: task
created_at: 2026-08-23T20:45:16.6228428Z
dependencies:
    - 056.020-T
id: 056.028-T
labels:
    - phase:evidence
    - cause:probe-ci
parent_id: 056-F
priority: medium
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Add dedicated CI job for the standalone mcp-probe crate
updated_at: 2026-08-23T21:25:48.4771039Z
---

The `tools/mcp-probe/` diagnostic crate has its own `Cargo.toml`/`Cargo.lock` and stands outside the production workspace, so the main fmt->clippy->test->audit pipeline does not cover it. This task ADDS a dedicated CI job/step (with a test-first seeded-violation proof) that runs the standalone-crate gates against the probe manifest and lockfile. It is created (owner named) by Stage now and IMPLEMENTED by Ship per this contract; it is NOT a member of the frozen eight-task 049-S evidence manifest, and it feeds T4's fan-in so the probe CI job is green before restored-production acceptance. Stage assembles it into an unconditional PHASE 1.5 evidence-infrastructure release unit immediately AFTER 049-S closes and BEFORE any selected remedy shipment (it is deliberately not in 049-S because its workflow file depends on the standalone crate delivered by 049-S). It is not cause-selected -- the `cause:probe-ci` label is a categorization, not a remedy `selection:` gate -- and no shipment manifest is created at plan time.

<!-- BEGIN:acceptance-criteria -->
- Own only the `.github/workflows` change that adds a dedicated CI job/step for the standalone `tools/mcp-probe/` crate; no Rust/probe source edits (those remain 056.020-T/056.022-T/056.023-T/056.021-T/056.001-T).
- The dedicated job runs, against `tools/mcp-probe/Cargo.toml` / `tools/mcp-probe/Cargo.lock`: `cargo +1.75.0 fmt --check`, `cargo +1.75.0 clippy --all-targets -- -D warnings -D clippy::pedantic`, `cargo +1.75.0 test`, `cargo +1.75.0 build`, and `cargo audit --file tools/mcp-probe/Cargo.lock`, because the probe stands outside the production workspace and the default pipeline (which targets the root manifest) never reaches it.
- Depend on 056.020-T (crate scaffold exists). Gate the job on paths under `tools/mcp-probe/**` where the CI supports path filters; otherwise run it unconditionally.
- Test-first CI contract: add or extend a workflow lint/dry-run proving the job selects the standalone manifest/lockfile and fails on a seeded clippy or `cargo audit` violation before the job is wired into required checks.
- CI/workflow width only; MSRV `1.75` pinned; do not modify the production build/test/audit gates. This task is NOT a member of the frozen eight-task 049-S evidence manifest.
- Release-unit placement: this task is the sole member of the unconditional PHASE 1.5 evidence-infrastructure release unit that Stage assembles immediately after 049-S closes and before any selected remedy shipment. It is not cause-selected (no remedy `selection:` gate; `cause:probe-ci` is a categorization label). The unit's readiness gate is this job green against the probe manifest/lockfile, and it remains in the 056.004-T (T4) fan-in, so restored-production acceptance cannot pass until it is green. No shipment manifest is created at plan time.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
The probe crate is standalone/non-published with its own lockfile (sysinfo added by 056.022-T, serde_json by 056.023-T), so root `cargo audit`/`cargo test` do not cover it and a dedicated CI job is required. Stage creates this task now (naming the owner for the standalone-probe CI contract flagged by review); Ship implements the `.github/workflows` job with the seeded-violation red proof per the acceptance criteria. Evidence-infrastructure follow-up outside 049-S, included in 056.004-T (T4) fan-in so the probe CI job is green before restored-production acceptance.
<!-- END:implementation-notes -->
