---
artifact_type: task
created_at: 2026-08-21T20:44:56.6762384Z
dependencies:
    - 056.002-T
id: 056.006-T
parent_id: 056-F
priority: medium
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: opt-in startup diagnosability sink (only if stderr redirect insufficient)'
updated_at: 2026-08-21T20:44:56.6762384Z
---



<!-- BEGIN:acceptance-criteria -->
- Adjudication (retain, evidence-gated — NOT speculative logging): this task is kept only because it targets a distinct evidenced condition the default cannot cover — T0 showing the CLI DISCARDS the child's stderr so the logs/serve-stderr.log redirect is impossible/insufficient. The default logs/ stderr capture solves the common case; a runtime sink is justified only when that capture is unavailable.
- Default disposition: close as not-needed with a one-line rationale when the documented logs/serve-stderr.log redirect fully solves the evidenced case — no code change. Closing as not-needed satisfies 056.004-T's dependency on it.
- If taken: the sink is env-gated and OFF by default; enabling it does not contaminate stdout.
- If taken: the sink captures the pre-serve early-exit reasons — the direct eprintln!/AutoStream startup messages at ~src/main.rs 2504/2549/2635 are either converted to tracing or the sink tees the real stderr stream. Do NOT claim a tracing-only sink captures those eprintln! writes.
- fmt + clippy pedantic (-D warnings) + tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
CONDITIONAL/OPTIONAL. Isolated out of T2 (056.003-T) to avoid bundling. Default diagnosability is the documented stderr-redirect recipe already exercised in T0 (serve 2> logs/serve-stderr.log) — avoid speculative runtime logging complexity. Only if that recipe proves insufficient (e.g. the CLI discards child stderr and redirection is impractical), add an env-gated, off-by-default opt-in file-log sink in src/logging/init.rs. If built, it MUST actually capture the pre-serve early-exit messages that are emitted via direct eprintln! (not tracing).
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Surfaces: src/logging/init.rs (currently .with_writer(std::io::stderr)); serve path in src/main.rs. Note the eprintln! early-exit messages (e.g. 'no databases found to serve') are NOT routed through tracing, so a tracing file sink alone would silently miss them — either migrate those messages to tracing or tee stderr. Prefer the documented redirect recipe over new runtime logging when sufficient.
<!-- END:implementation-notes -->