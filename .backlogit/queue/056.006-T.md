---
artifact_type: task
created_at: 2026-08-21T20:44:56.6762384Z
dependencies:
    - 056.003-T
id: 056.006-T
labels:
    - phase:remedy
    - cause:diagnostic-sink
    - selection:pending
parent_id: 056-F
priority: medium
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: opt-in startup diagnosability sink (only if stderr redirect insufficient)'
updated_at: 2026-08-23T20:55:12.5156467Z
---



<!-- BEGIN:acceptance-criteria -->
- Select only when T0 proves child stderr unavailable AND env inheritance is proven (see next bullet); otherwise move to `done` with `not-needed: production sink not required or env inheritance not proven`. T4 never falls back to executable substitution.
- Env-inheritance is a PROVABLE criterion, not an assumption: 056.001-T sets a unique sentinel environment variable on the exact-CLI process and the 056.022-T wrapper observes/records whether the child inherited it (a bounded probe-owned sentinel observation, no production-config change). This task activates only when that recorded evidence shows the exact CLI both discards child stderr and propagates inherited environment.
- Standing backlog dependency is `056.003-T` (typed events) only. Composing AFTER 056.011-T applies ONLY when the sink and H3-A are co-selected in the SAME shipment. Stage assembly rule: in a co-selected shipment, order `056.003-T -> (056.011-T if present) -> 056.006-T` so the sink composes LAST, after transport, and never restyles prior serve/transport/projection wiring. When the sink ships without H3-A there is no 056.011-T ordering.
- The sink is off by default and stdout remains protocol-clean. T0/T4 set its gate and a unique absolute owner-authorized sink path on the probe-owned CLI environment; do not add generated target env fields.
- Open one per-attempt file with exclusive create/no-follow semantics under the authorized root. Include nonce/run id, PID, config hash, and line-delimited events; reject existing, relative, linked, stale, or non-contained paths.
- Capture every 056.003-T typed preflight exit, propagated-error event, and `mcp_serve_ready`; continuously surface initialization/write failure because no alternate child sink exists.
- Write and observe these three test groups failing (observed-red) before implementation, then green them: unique gate-on event capture, linked/collision/write refusal, and gate-off or env-unavailable no-file/no-stdout behavior.
- Rust 1.75, fmt, clippy pedantic, and tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Conditional production-safe diagnostic sink only when inherited child stderr is unavailable and the target CLI propagates an env gate proven by a bounded probe-owned sentinel observation. Standing dependency is 056.003-T (typed events); it composes after 056.011-T ONLY when the sink and H3-A are co-selected in one shipment (a shipment-assembly ordering, not a standing edge), and never restyles prior wiring.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Standing dep is 056.003-T (typed events). The 'compose after 056.011-T' ordering is a co-selection-only shipment-assembly edge (sink + H3-A in one shipment); Stage orders `056.003 -> (056.011 if present) -> 056.006` at assembly. Env inheritance is proven by 056.001-T's sentinel env plus 056.022-T's wrapper observation, not assumed. Consumes typed events and does not edit individual guard messages. A shared sink is forbidden; each owned attempt gets a unique exclusive file.
<!-- END:implementation-notes -->