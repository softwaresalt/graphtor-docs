---
artifact_type: task
created_at: 2026-08-23T07:21:04.8440011Z
dependencies:
    - 056.020-T
id: 056.022-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Own probe process identities and teardown
updated_at: 2026-08-23T07:21:55.8404054Z
---



<!-- BEGIN:acceptance-criteria -->
- Depend on 056.020-T. Own the probe process lifecycle module (`tools/mcp-probe/process.rs`) and its focused tests; `tools/mcp-probe/main.rs` wiring may be updated sequentially after 056.020-T to compose this module onto the transparent proxy.
- Establish ownership at spawn, not post-hoc only: the outer runner holds the exact CLI-assigned Copilot `Child` and the wrapper holds the exact inner-server `Child`. Record PID plus process-start-time identity for the runner, wrapper, and inner server at spawn, and continuously maintain the observed owned-identity registry while the processes are live.
- Teardown uses the direct `Child` kill/wait wherever a handle exists, then a deepest-first fallback that matches on exact PID plus process-start-time through the existing safe `sysinfo` enumeration. Never kill on PID alone or on any identity mismatch; never name-kill or broad-kill.
- Repeat bounded verification until every RECORDED owned identity is confirmed absent. If ownership cannot be proven for a recorded identity, the evidence fails and the exact residual identities are surfaced for operator action; do not claim atomic whole-tree ownership or that an unowned descendant was reaped.
- Add no `unsafe` and no new process crate; reuse the existing safe `sysinfo` dependency only. This task performs no workspace creation, no config generation, no persisted-evidence run, and no actual Copilot CLI launch for acceptance.
- Self-test process lifecycle in at most three grouped scenarios: (1) normal completion plus back-to-back runs leaving no recorded identity leaked; (2) deadline/error teardown; (3) intermediate child exit and PID-reuse mismatch where a reused PID with a different start-time is refused as a non-match.
- Rust 1.75 targeted probe gates (`cargo +1.75.0 check`/`test`/`build`/`clippy` with `--features probe-harness --bin graphtor-mcp-probe`) and the default repository quality gates pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Own probe process spawning, exact process identity, and all-outcome teardown, composed onto the 056.020-T transparent byte proxy. Establish ownership at spawn: the outer runner holds the exact Copilot `Child` and the wrapper holds the exact inner-server `Child`, each recorded by PID plus process-start-time and maintained in a live owned-identity registry. Teardown prefers the direct `Child` handle, then falls back deepest-first to exact PID-plus-start-time matches through existing safe `sysinfo`, never killing on PID alone, on identity mismatch, or by name. Bounded verification repeats until every recorded owned identity is absent; unproven ownership fails evidence and surfaces exact residual identities for operator action rather than claiming atomic whole-tree ownership.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Own `tools/mcp-probe/process.rs` and its focused tests; sequentially update `tools/mcp-probe/main.rs` to compose this lifecycle onto the 056.020-T proxy. Reuse the existing safe `sysinfo` dependency for enumeration; add no `unsafe` and no new process crate. Record PID + process-start-time at spawn for runner, wrapper, and inner server; refuse any match whose start-time differs (PID reuse). This task creates no isolated workspace (056.021-T), writes no `.mcp.json` (056.021-T), and runs no real CLI for acceptance (056.001-T). Keep self-tests grouped to at most three scenarios.
<!-- END:implementation-notes -->