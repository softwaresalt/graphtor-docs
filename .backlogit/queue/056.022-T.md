---
artifact_type: task
created_at: 2026-08-23T07:21:04.8440011Z
dependencies:
    - 056.020-T
id: 056.022-T
labels:
    - phase:evidence
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Own probe process identities and teardown
updated_at: 2026-08-23T19:22:53.7097463Z
---



<!-- BEGIN:acceptance-criteria -->
- Depend on 056.020-T (forward chain `056.020 -> 056.022 -> 056.023 -> 056.021`). Own `tools/mcp-probe/src/process.rs`, the versioned `wrapper` subcommand, and their focused tests inside the standalone probe crate; sequentially update `tools/mcp-probe/src/main.rs` to compose process spawning and the wrapper onto the 056.020-T transport. Never reimplement the byte pumps.
- Own the versioned `wrapper` subcommand and its argv contract: `--inner-exe`, repeated original `--inner-arg`, `--evidence-output`, and `--run-nonce`. All wrapper-only args are byte-identical between the control and treatment legs and are the allowed diagnostic parity difference (056.021-T's treatment leg alone adds the candidate cwd). The wrapper wires the inner `Child`'s stdio to the 056.020-T pumps and preserves exit code, stderr, half-close, and deadline behavior unchanged; it owns the direct inner `Child` guard.
- Direct `std::process::Child` handles are the ONLY kill/wait authority. The wrapper owns the inner-server `Child` guard; the exact-CLI runner (the 056.001-T composition point) owns the Copilot `Child` guard. Each guard kills and waits its own child on every outcome (success, error, deadline). The wrapper PID is observed-only and is never used to kill.
- An injectable process-observation trait is REQUIRED (not optional). The production implementation uses a standalone `sysinfo` dependency to observe PID, process-start-time, executable, parent, and a launch nonce for diagnostics and deterministic tests ONLY (a different crate is acceptable only if explicitly equivalent); enumeration can verify and report but MUST NOT kill. Remove every `sysinfo` / PID / start-time KILL fallback and every claim of exact arbitrary-descendant identity or atomic whole-tree ownership.
- Fail closed on identity ambiguity: a same-second start-time (or otherwise indistinguishable identity) is ambiguous and is never treated as a confirmed match. If residual wrapper or unknown descendant processes remain after the owned guards run, the evidence fails and the exact diagnostic identities (PID / start-time / executable / parent) are surfaced for operator-approved action; the task never kills them itself.
- Add no `unsafe` (preserve crate-root `#![forbid(unsafe_code)]`) and no new process-control crate beyond the standalone `sysinfo` observation dependency. This task creates no isolated workspace, writes no `.mcp.json`, owns no observer/evidence module, persists no evidence itself, and runs no actual Copilot CLI for acceptance.
- Self-test in at most three grouped scenarios through the injectable observation adapter: (1) normal completion and back-to-back runs cleaned up by direct handles with no leaked owned child, and the wrapper forwarding inner stdio through the 056.020-T pumps with unchanged exit/stderr/half-close; (2) deadline/error teardown by direct handles; (3) deterministic PID-reuse ambiguity where a reused PID with a same-second/indistinguishable start-time is reported ambiguous (fails closed) rather than matched or killed, and an observed-only residual descendant is surfaced, not reaped.
- Standalone-crate Rust 1.75 gates pass: `cargo +1.75.0 check|test|build|clippy --manifest-path tools/mcp-probe/Cargo.toml` (clippy with `-D warnings -D clippy::pedantic`); because this crate now owns a separate `Cargo.lock` with the `sysinfo` dependency, also run `cargo audit --file tools/mcp-probe/Cargo.lock` as a standalone-probe gate.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Own probe process spawning, teardown, and the versioned `wrapper` subcommand for the standalone diagnostic crate, composed onto the 056.020-T transport. The wrapper argv contract (`--inner-exe`, repeated `--inner-arg`, `--evidence-output`, `--run-nonce`) is byte-identical between control and treatment legs; the wrapper wires the inner `Child` to the 056.020-T pumps and preserves exit/stderr/half-close/deadline behavior. Direct `std::process::Child` handles are the sole kill/wait authority: the exact-CLI runner owns the Copilot `Child` guard and the wrapper owns the inner-server `Child` guard, each reaping its own child on every outcome; the wrapper PID is observed-only. An injectable process-observation trait is REQUIRED, with a standalone `sysinfo` production implementation that observes PID / start-time / executable / parent / nonce for diagnostics and deterministic tests but MUST NOT kill; same-second identity is ambiguous and fails closed. Residual wrapper or unknown descendants fail the evidence and surface exact diagnostic identities for operator-approved action instead of any whole-tree kill. A standalone `cargo audit` gate covers this crate's separate lockfile.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Own `tools/mcp-probe/src/process.rs`, the versioned `wrapper` subcommand, and their focused tests; sequentially update `tools/mcp-probe/src/main.rs` to compose spawning/teardown and the wrapper onto the 056.020-T transport. Model `sysinfo` observation behind a REQUIRED injectable adapter/trait so tests can supply deterministic PID/start-time identities; the standalone `sysinfo` production impl verifies and reports only and never kills (a different crate is acceptable only if explicitly equivalent). Add no `unsafe` and no new process-control crate beyond `sysinfo`; add the standalone `cargo audit --file tools/mcp-probe/Cargo.lock` gate. This task creates no isolated workspace (056.021-T), writes no `.mcp.json` (056.021-T), owns no observer/evidence module (056.023-T), and runs no real CLI for acceptance (056.001-T). Keep self-tests grouped to at most three scenarios.
<!-- END:implementation-notes -->