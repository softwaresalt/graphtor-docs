---
artifact_type: task
created_at: 2026-08-23T07:21:04.8440011Z
dependencies:
    - 056.020-T
id: 056.022-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Own probe process identities and teardown
updated_at: 2026-08-23T07:21:55.8404054Z
---



<!-- BEGIN:acceptance-criteria -->
- Depend on 056.020-T (forward chain `056.020 -> 056.022 -> 056.023 -> 056.021`). Own `tools/mcp-probe/src/process.rs` and its focused tests inside the standalone probe crate; sequentially update `tools/mcp-probe/src/main.rs` to compose process spawning onto the 056.020-T transport. Never reimplement the byte pumps.
- Direct `std::process::Child` handles are the ONLY kill/wait authority. The exact-CLI runner (the 056.001-T composition point) owns the Copilot `Child` guard; the wrapper owns the inner-server `Child` guard. Each guard kills and waits its own child on every outcome (success, error, deadline). The wrapper PID is observed-only and is never used to kill.
- Remove every `sysinfo` / PID / start-time KILL fallback and every claim of exact arbitrary-descendant identity or atomic whole-tree ownership. A safe `sysinfo` adapter behind a trait MAY observe PID, process-start-time, executable, parent, and a launch nonce for diagnostics and deterministic tests, but enumeration can only verify and report — it MUST NOT kill.
- Fail closed on identity ambiguity: a same-second start-time (or otherwise indistinguishable identity) is ambiguous and is never treated as a confirmed match. If residual wrapper or unknown descendant processes remain after the owned guards run, the evidence fails and the exact diagnostic identities (PID / start-time / executable / parent) are surfaced for operator-approved action; the task never kills them itself.
- Add no `unsafe` and no new process-control crate; the `sysinfo` adapter is observe-only. This task creates no isolated workspace, writes no `.mcp.json`, owns no observer/evidence, persists nothing, and runs no actual Copilot CLI for acceptance.
- Self-test in at most three grouped scenarios through an injectable observation adapter: (1) normal completion and back-to-back runs cleaned up by direct handles with no leaked owned child; (2) deadline/error teardown by direct handles; (3) deterministic PID-reuse ambiguity where a reused PID with a same-second/indistinguishable start-time is reported ambiguous (fails closed) rather than matched or killed, and an observed-only residual descendant is surfaced, not reaped.
- Standalone-crate Rust 1.75 gates pass: `cargo +1.75.0 check|test|build|clippy --manifest-path tools/mcp-probe/Cargo.toml` (clippy with `-D warnings -D clippy::pedantic`).
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Own probe process spawning and teardown for the standalone diagnostic crate, composed onto the 056.020-T transport. Direct `std::process::Child` handles are the sole kill/wait authority: the exact-CLI runner owns the Copilot `Child` guard and the wrapper owns the inner-server `Child` guard, each reaping its own child on every outcome. The wrapper PID is observed-only. A safe `sysinfo` adapter behind a trait may observe PID / start-time / executable / parent / nonce for diagnostics and deterministic tests but MUST NOT kill; same-second identity is ambiguous and fails closed. Residual wrapper or unknown descendants fail the evidence and surface exact diagnostic identities for operator-approved action instead of any whole-tree kill.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Own `tools/mcp-probe/src/process.rs` and its focused tests; sequentially update `tools/mcp-probe/src/main.rs` to compose spawning/teardown onto the 056.020-T transport. Model `sysinfo` observation behind an injectable adapter/trait so tests can supply deterministic PID/start-time identities; the adapter verifies and reports only and never kills. Add no `unsafe` and no new process-control crate. This task creates no isolated workspace (056.021-T), writes no `.mcp.json` (056.021-T), owns no observer/evidence (056.023-T), and runs no real CLI for acceptance (056.001-T). Keep self-tests grouped to at most three scenarios.
<!-- END:implementation-notes -->