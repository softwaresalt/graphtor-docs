---
artifact_type: shipment
created_at: 2026-08-29T07:25:48.9469871Z
custom_fields:
    items:
        - 056.011-T
dependencies:
    - 049-S
    - 053-S
id: 052-S
priority: high
status: queued
title: 'H3-A remedy: pre-initialize server/discover transport compatibility'
updated_at: 2026-08-29T16:44:22.8969104Z
---

Task-only H3-A transport-compatibility release unit. Its sole member is `056.011-T`, and it runs only after `049-S` and the complete PHASE 1.6 prerequisite unit `053-S` (`blocks` dependencies on both shipments). PHASE 1.5 ordering is enforced now by the task path `056.028-T -> 056.029-T -> 056.011-T`; no PHASE 1.5 shipment manifest exists until `049-S` closes, and no future manifest mutation is the authority for `052-S` ordering. Before claim, `052-S` must verify that all five `053-S` members are terminal, including the direct `056.029-T` dependency of `056.011-T`. Cargo 1.75's failure to parse the current edition-2024 `rmcp 1.5.0` manifest is established. `056.029-T` resolves the dependency graph at the unchanged Rust 2021 / rust-version 1.75 floor; `056.011-T` consumes that completed evidence and has no separate hard-coded MSRV entry gate. Its ordinary task validation remains at Rust/Cargo 1.75 after the dependency graph parses and builds. The implementation remains one bounded pre-initialize `server/discover` response/continuation, preserves ping passthrough, requires normal initialize then tools, performs no arbitrary pre-initialize dispatch, and proves the exact response shape with before/after exact-Copilot evidence. T4 remains the only restored-production acceptance owner.