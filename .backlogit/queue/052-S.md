---
artifact_type: shipment
created_at: 2026-08-29T07:25:48.9469871Z
custom_fields:
    items:
        - 056.011-T
dependencies:
    - 049-S
    - 053-S
id: 052-S
priority: high
status: queued
title: 'H3-A remedy: pre-initialize server/discover transport compatibility'
updated_at: 2026-08-29T09:32:42.0054042Z
---

Task-only H3-A transport-compatibility release unit. Its sole member is `056.011-T`; it runs only after `049-S`, the explicit `056.028-T` PHASE 1.5 dependency, and the PHASE 1.6 declared-MSRV unit `053-S` (`blocks` dependencies `049-S` and `053-S`). The declared-MSRV question is NOT adjudicated here: the PR #108 review established that Cargo 1.75 predates edition 2024 (stable from Rust/Cargo 1.85) and cannot parse the resolved edition-2024 `rmcp 1.5.0` manifest, so the former mandatory `cargo +1.75.0 check --all-targets` entry gate was a deterministic failure, not a measurement. `053-S`/`056.029-T` resolves that mismatch first; as its first step `056.011-T` then runs `cargo +<resolved declared rust-version> check --all-targets` once against the current unmodified pin, reading the floor from `Cargo.toml` as `056.029-T` left it rather than a hard-coded `+1.75.0`. A nonzero result blocks this shipment and returns it to Stage for a new bounded MSRV follow-up; it never permits adapter continuation or an MSRV-compatibility claim. The implementation remains one bounded pre-`initialize` `server/discover` response/continuation, preserves ping passthrough, requires normal initialize then tools, performs no arbitrary pre-initialize dispatch, and proves the exact response shape with before/after exact-Copilot evidence. T4 remains the only restored-production acceptance owner.