---
artifact_type: task
created_at: 2026-08-25T16:37:14.6276353Z
custom_fields:
    blocked_reason: 'DIRECT dependency on 059.008-T (U8) is REMOVED (2026-08-29 re-deliberation, Option B): U8 recorded terminally BLOCKED (the accepted engine-open residual), and U6 does NOT require the engine binding — it contains the chmod paths (leaf via U2 + intermediate-directory via the cap-std root-relative walk). U6 depends on 059.001-T (U1), 059.002-T (U2), and 059.007-T (U7, done). Transitively gated on the operator sign-off 059.014-T via U1 -> 059.014-T; cannot start until U1 lands and 059.014-T sign-off is done.'
dependencies:
    - 059.001-T
    - 059.002-T
    - 059.007-T
id: 059.006-T
labels:
    - security
    - filesystem
    - toctou
    - containment
    - code
    - test-first
parent_id: 059-F
priority: high
references:
    - docs/exec-plans/2026-08-24-store-toctou-nofollow-handle-plan.md
    - docs/decisions/2026-08-24-store-toctou-nofollow-handle-deliberation.md
status: blocked
title: 'U6: Integrate beneath-root opener and permission boundary (composes U2; gates U9/U3-U5/U10)'
updated_at: 2026-08-30T02:37:26.315112Z
---

Implement the production beneath-root opener and permission-operation boundary after U7 feasibility (PASS; U8's engine-boundary feasibility recorded BLOCKED and is an accepted decided input, not a prerequisite), U1 dependency adoption, and U2's leaf no-follow handle + permission primitives. Resolve the main database and sidecars relative to the retained workspace-root capability and COMPOSE U2's open_no_follow / capture_perms / set_readonly_via_handle primitives (do not re-implement or duplicate the leaf refusal or permission mechanics), return identity-bound handles for permission capture/mutation, and fail closed on escape or link/reparse inputs. This task ends at the contained opener and permission boundary; actual configure_sqlite_wal and SQLite/Cozo engine-open integration belongs to the DEFERRED U9 (later separate shipment, gated on 059.013-T), not the near-term path.

<!-- BEGIN:acceptance-criteria -->
- Test-first scenario 1: a normal database and sidecar beneath the retained root open through the capability boundary and round-trip exact handle-bound permissions by COMPOSING U2's open_no_follow / capture_perms / set_readonly_via_handle primitives (not a duplicate leaf implementation).
- Test-first scenario 2: an intermediate-directory swap after root bootstrap is refused and no external file is opened or permission-mutated; report executed-versus-skipped where platform privileges limit the link fixture.
- Test-first scenario 3: a table-driven refusal matrix covers unavailable root authority, absolute/.. escape, and final-component symlink/reparse (delegated to U2's leaf refusal primitive) without any path-based fallback.
- Integrated opener passes cargo +1.75.0 check --all-targets, clippy pedantic, targeted tests, and contains no in-crate unsafe or unwrap/expect.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Depends on U1 for adopted dependencies, U7 (PASS) and U8 (BLOCKED — accepted decided input, not a gate) as the two recorded feasibility results, and U2 for the leaf no-follow handle + permission primitives this unit composes. U2 precedes U6 and is NOT gated by it. The DEFERRED U9 consumes this opener and implements the actual WAL/SQLite/Cozo engine boundary on a later separate shipment (gated on 059.013-T); it is NOT on the 059-F near-term path. U3-U5 and U10 depend on U6 only (their former U9 dependency is dropped); U11 depends on U2 (which precedes U6). Because the engine open is the accepted residual, no downstream unit claims complete containment after permission-only integration — the engine-open leaf and intermediate-directory redirection remains the named accepted residual. Keep this unit to the three scenarios above, composing (not duplicating) U2's primitives.
<!-- END:implementation-notes -->
