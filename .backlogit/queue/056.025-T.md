---
artifact_type: task
created_at: 2026-08-23T09:48:28.8468307Z
dependencies:
    - 056.005-T
id: 056.025-T
labels:
    - phase:remedy
    - cause:h1-model-lifecycle
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Project embedding lifecycle through MCP status
updated_at: 2026-08-23T19:24:06.7070845Z
---

<!-- BEGIN:acceptance-criteria -->
- If H1 is not evidenced by T0, move to `done` with `not-needed: H1 not evidenced`.
- Depend on 056.005-T (chain `... 014 -> 005 -> 025 -> 015 ...`). Consume the stable typed lifecycle-state accessor exposed by 056.005-T; own no embedding lifecycle state machine, no `cmd_serve` wiring, and no background-sync orchestration (those are 056.005-T and 056.015-T).
- Own ONLY the versioned Loading/Failed/Disabled MCP availability projection types and their `src/mcp/server.rs` consumers: Loading/Failed share a stable code, status, retryable=true, remediation, and an optional retry hint; terminal Disabled is retryable=false with a stable reason. `search_semantic` returns a typed disabled result with no fallback; `research_topic` may use keyword fallback only when its response metadata declares Disabled and `fallback=keyword`; neither falls back while Loading or Failed.
- Write and observe failing projection/tool-contract tests BEFORE implementation (red-first), then green exactly three groups: the versioned Loading/Failed/Disabled availability-projection matrix; `search_semantic` typed-disabled no-fallback; and `research_topic` keyword fallback only under Disabled `fallback=keyword`.
- Do not edit `cmd_serve`, background sync, or the embedding lifecycle state machine here; 056.015-T injects the shared lazy owner into `cmd_serve`/background sync and consumes this projection.
- Rust 1.75, fmt, clippy pedantic, and targeted projection/tool-contract tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Conditional H1 MCP availability projection. Own the versioned Loading/Failed/Disabled projection types and the `src/mcp/server.rs` availability/fallback metadata consumed by `search_semantic` and `research_topic`, projecting from 056.005-T's stable typed lifecycle-state accessor. Red-first projection and tool-contract tests only; no `cmd_serve`, background-sync, or lifecycle-state-machine ownership.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Own the versioned MCP availability projection in `src/mcp/server.rs` (moved out of 056.005-T and 056.015-T) plus focused projection/tool-contract tests. Project from 056.005-T's typed lifecycle-state accessor; 056.015-T owns `cmd_serve` lifecycle injection and background-sync orchestration and consumes this projection; resolver semantics remain 056.014-T.
<!-- END:implementation-notes -->
