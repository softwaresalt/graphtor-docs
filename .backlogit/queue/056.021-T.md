---
artifact_type: task
created_at: 2026-08-23T06:38:43.9751872Z
dependencies:
    - 056.023-T
id: 056.021-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Create isolated probe workspace and config fixtures
updated_at: 2026-08-23T07:24:05.8119295Z
---



<!-- BEGIN:acceptance-criteria -->
- Depend on 056.023-T (forward chain `056.020 -> 056.022 -> 056.023 -> 056.021`). Compose the 056.020-T transport, the 056.022-T process guards, and the 056.023-T observer/evidence seam; never reimplement the byte pumps, the process teardown, or the evidence capture.
- Own ONLY `tools/mcp-probe/src/workspace.rs` plus the sequential composition wiring into `tools/mcp-probe/src/main.rs`. Own no observer/evidence module (that is 056.023-T) and add no new production path primitive.
- Create a fresh isolated workspace under the canonical repository path `logs/probe/<nonce>` through the Rust probe using exclusive creation (fail if the path already exists). Validate every path component with the existing `graphtor_core::path::validate_path` and `is_reparse_point` helpers before use.
- State the threat model explicitly: this protects against accidental escape of the repository root and a pre-existing reparse point/junction on the workspace path. It does not defend against a malicious same-user process that can modify source or the workspace, and the documented `validate_path`/`is_reparse_point` TOCTOU window is an accepted residual risk for this non-sensitive, same-user diagnostic workspace.
- Generate the temporary control and treatment `.mcp.json` only inside the isolated workspace. Never read-modify-write, back up, restore, or substitute the user `.mcp.json`; therefore no config backup, restore, or approval receipt is required. Control and treatment are byte-equivalent to the production server entry semantics except that both use the diagnostic wrapper as `command`; the treatment entry alone differs by adding the candidate working-directory (cwd) mechanism. Add no other env, args, target, or stdio-discriminator difference.
- Build an owned nested ancestor-discovery fixture inside the isolated workspace: an owned parent directory holding a deliberately invalid/sentinel ancestor `.mcp.json`, and a child run directory holding the intended temporary `.mcp.json`. This lets 056.001-T prove, with the exact target CLI, that the nearest child config shadows (and does not merge) the ancestor config before any causal contrast. Everything stays inside the owned workspace; the repository-root `.mcp.json` is never assumed unread.
- Own the exact workspace cleanup/retention policy: never delete anything outside the exact owned `logs/probe/<nonce>` workspace, and keep destructive-cleanup approval constraints explicit (operator approval before any destructive cleanup beyond the owned workspace). The redacted evidence written into the workspace is owned by 056.023-T.
- This task performs no actual production acceptance; the exact-CLI run is 056.001-T and restored-production acceptance remains solely T4. Standalone-crate Rust 1.75 gates pass: `cargo +1.75.0 check|test|build|clippy --manifest-path tools/mcp-probe/Cargo.toml`.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Own the isolated probe workspace and the control/treatment plus ancestor/child config fixtures used by the exact-CLI evidence run (056.001-T). The Rust probe creates a fresh workspace under the canonical repository `logs/probe/<nonce>` by exclusive creation, generates temporary control and treatment `.mcp.json` entries inside that workspace only, and builds an owned nested ancestor/child config fixture. It composes -- never reimplements -- the 056.020-T transport, the 056.022-T process guards, and the 056.023-T observer/evidence seam, and owns no observer/evidence module. It never reads, modifies, backs up, or restores the user `.mcp.json`, so no approval receipt is required for config. The explicit threat model protects against accidental escape and pre-existing reparse points for a non-sensitive, same-user diagnostic workspace; the accepted same-user TOCTOU residual risk is stated. No new production path primitive is introduced.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Own `tools/mcp-probe/src/workspace.rs`; sequentially wire it into `tools/mcp-probe/src/main.rs` after 056.023-T composes the observer/evidence seam. Reuse only the existing `graphtor_core::path::validate_path` / `is_reparse_point` helpers (the code graph confirms these are the only path primitives) with an explicit accepted-TOCTOU residual-risk statement; add no new production path primitive and no observer/evidence module (056.023-T). The nested fixture places a sentinel/invalid ancestor `.mcp.json` in an owned parent and the intended temporary `.mcp.json` in a child run directory, all inside the isolated workspace. Self-tested behaviors: exclusive workspace creation and reparse rejection, in-workspace control/treatment plus nested ancestor/child fixture generation with production parity except wrapper/cwd, and owned-workspace-only cleanup.
<!-- END:implementation-notes -->