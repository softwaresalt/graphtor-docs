---
artifact_type: task
created_at: 2026-08-29T08:03:04.4834045Z
id: 056.029-T
labels:
    - phase:prerequisite
    - cause:msrv-declaration
    - msrv:blocking
parent_id: 056-F
priority: medium
references:
    - docs/decisions/2026-08-29-mcp-serve-discover-preinitialize-evidence.md
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Resolve declared rust-version vs edition-2024 rmcp dependency mismatch
updated_at: 2026-08-29T09:33:03.9436749Z
---

<!-- BEGIN:acceptance-criteria -->
- Treat the declared-MSRV mismatch as an established blocker, not an unmeasured risk. Record in the resolution artifact: stable Cargo accepted `edition = "2024"` only from Rust/Cargo 1.85; the workspace declares `edition = "2021"` / `rust-version = "1.75"`; `Cargo.toml` pins `rmcp = "1.5"` and `Cargo.lock` resolves `rmcp 1.5.0`; the vendored `rmcp-1.5.0/Cargo.toml` declares `edition = "2024"` and no `rust-version`. A one-shot `cargo +1.75.0 check --all-targets` reproduction may be captured as evidence at implementation time, but a nominally pending measurement is never the gate and its failure is never treated as new information.
- Choose exactly ONE bounded resolution and record it, with rejected alternatives, in a new dated `docs/decisions/` artifact (do not rewrite the OS-232 deliberation or the 2026-08-29 `server/discover` evidence record):
  - (a) raise the declared `rust-version` to a truthful floor that parses edition-2024 dependency manifests, and align every place the floor is asserted -- `Cargo.toml`, `tools/mcp-probe/Cargo.toml`, `.github/workflows/*`, `.github/instructions/*`, `AGENTS.md`, `.github/copilot-instructions.md`, and the affected `docs/` guidance; or
  - (b) select an MSRV-compatible rmcp strategy (an rmcp release whose manifest parses at the declared floor, or an equivalent dependency resolution) that keeps the declared floor truthful.
- Do not preselect or implement either option at plan time, and never silently waive the mismatch, silently relax `rust-version`, or silently bump `rmcp`.
- Width isolation: this task owns the dependency/toolchain declaration and the documentation/CI text that asserts it. It performs no MCP handshake change, no H3-A transport adapter work, no `src/mcp/**` behavior change, and no probe-crate behavior change. If the chosen option additionally requires a broad source-level edition/API migration, that migration is split into a named follow-up task rather than widening this one; any rmcp version change stays reviewed inside this task or inside that named split.
- Release-unit placement and ordering: sole member of queued shipment `053-S`. `053-S` carries a `blocks` dependency on `049-S`; `052-S` carries a `blocks` dependency on `053-S`, so `052-S` transitively waits for `049-S` and for this unit. When Stage assembles the PHASE 1.5 `056.028-T` shipment after `049-S` closes, it adds that shipment as an explicit `053-S` dependency before claim; no PHASE 1.5 manifest is created at plan time (assembly gate: `049-S` closed).
- `056.011-T` carries a standing `blocks` dependency on `056.029-T`, and its own first step validates against the RESOLVED declared `rust-version` recorded here (`cargo +<resolved floor> check --all-targets`), never a hard-coded `+1.75.0`.
- Terminal readiness: this unit is terminal only when the declared floor is truthful and the ordinary gates -- `cargo fmt --all -- --check`, `cargo clippy --all-targets -- -D warnings -D clippy::pedantic`, `cargo test --all-targets`, `cargo audit`, and `cargo build --release` -- pass at that resolved floor, plus the standalone probe-crate gates at the same floor. Until then `053-S`, `052-S`, and `056.011-T` remain blocked; a completed transport adapter can never substitute for this evidence.
- Not cause-selected: no `selection:*` label, exactly as for `056.028-T`. It remains in `056.004-T` (T4) fan-in, so restored-production acceptance cannot report READY while the declared-MSRV question is unresolved or blocked.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
The earlier framing -- an unshipped follow-up that merely consumed a `056.011-T` measurement -- was invalidated by the PR #108 review: Cargo 1.75 predates edition 2024 (stable from Rust/Cargo 1.85), so it cannot parse the resolved `rmcp 1.5.0` manifest and the mandatory `cargo +1.75.0 check --all-targets` entry gate was a guaranteed failure that would have parked `052-S` permanently. Correcting the DAG (dedicated `053-S` ahead of `052-S`) is preferred over deferring a known blocker to execution. Keeping the resolution in its own release unit preserves width isolation: an unresolved toolchain-policy change never rides along with the transport remedy, and the transport remedy never runs against a declaration it cannot satisfy. The rmcp 1.8.x exclusion still stands, but only on unproven MSRV parity, wider transitive API surface, and rollback isolation -- edition does not discriminate 1.5.0 from 1.8.0. Whichever option is chosen, the declared floor must end up truthful; `056.011-T` then reads that floor rather than assuming `1.75.0`.
<!-- END:implementation-notes -->

<!-- BEGIN:description -->
Selected declared-MSRV / dependency-compatibility release unit, re-scoped on 2026-08-29 during the PR #108 review after the question was shown to be a deterministic blocker rather than an open measurement. Stable Cargo did not accept `edition = "2024"` until Rust/Cargo 1.85, the workspace declares `rust-version = "1.75"`, and the resolved `rmcp 1.5.0` manifest declares `edition = "2024"` with no `rust-version`, so `cargo +1.75.0` cannot parse the dependency manifest and the previously mandatory `cargo +1.75.0 check --all-targets` entry gate could only ever fail. The DAG is corrected rather than deferred: `056.029-T` is the sole member of queued shipment `053-S` (PHASE 1.6), which runs after `049-S` and the PHASE 1.5 `056.028-T` unit and BEFORE `052-S`, so the H3-A remedy never executes against an unresolvable toolchain declaration. Implementation picks exactly one bounded resolution -- raise the true declared `rust-version` and align docs/CI, or select an MSRV-compatible rmcp strategy through a bounded decision -- and neither option is preselected or implemented at plan time. Like `056.028-T`, this unit is not cause-selected: `cause:msrv-declaration` is a categorization, so the `phase:remedy` `selection:` gate does not apply to its `053-S` membership. It stays in `056.004-T` (T4) fan-in.
<!-- END:description -->