---
artifact_type: task
created_at: 2026-08-29T08:03:04.4834045Z
id: 056.029-T
labels:
    - phase:followup
    - cause:msrv-declaration
    - msrv:unverified
parent_id: 056-F
priority: medium
references:
    - docs/decisions/2026-08-29-mcp-serve-discover-preinitialize-evidence.md
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Adjudicate declared rust-version against the edition-2024 rmcp pin
updated_at: 2026-08-29T08:49:45.4408419Z
---

Bounded, unshipped evidence-disposition follow-up created during the H3-A review. It does not independently build or change the project: `056.011-T` owns the single actual current-pin `cargo +1.75.0 check --all-targets` entry gate. `056.029-T` consumes that immutable redacted result so Stage can truthfully record its terminal disposition for T4. Cargo metadata (workspace Rust 1.75; resolved rmcp 1.5.0 edition 2024 with no rust-version) is a risk signal, not the outcome. On failure, 052-S remains blocked and Stage separately scopes the policy/remediation decision; this task cannot authorize adapter continuation. It has no shipment membership, no `selection:*` label, and is T4 fan-in only.

<!-- BEGIN:acceptance-criteria -->
- Consume and cite the single actual `cargo +1.75.0 check --all-targets` result recorded by `056.011-T` before any H3-A harness, adapter, manifest, or dependency change. Verify that the evidence identifies the current unmodified pin, resolved rmcp version, toolchain identity, and exit result. Do not rerun the nominally pre-change gate independently.
- If the recorded check passes, Stage records the limited current-pin result in a redacted backlog comment or memory and moves this evidence-only follow-up to `done`. Passing does not prove a future dependency bump or waive ordinary quality gates.
- If the recorded check is nonzero, Stage records a redacted failure summary and keeps this task `blocked`; `052-S` / `056.011-T` remain blocked before adapter implementation continues. Do not declare the adapter MSRV-compatible, silently relax `rust-version`, or treat edition metadata as a substitute for the failed measurement.
- A failed result requires a separately scoped Stage deliberation or remediation unit to decide policy alignment. It may investigate candidates such as the declared MSRV floor or a dependency resolution, but must not preselect or implement either from metadata alone.
- `056.029-T` remains unshipped and has no `selection:*` label. `056.004-T` depends on it so T4 cannot report final readiness while the declared-MS​​RV question is unresolved or blocked.
- This task must not change the rmcp pin, MCP handshake behavior, H3-A transport adapter, source files, workflow files, Cargo metadata, or user-facing documentation.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
The edition-2024 / missing-rust-version metadata corrects the old discriminator for excluding rmcp 1.8.x, but it is a compatibility-risk signal rather than build evidence. The rmcp 1.8.x exclusion remains based on unproven MSRV parity, wider transitive API surface, and rollback isolation. `056.011-T` is the sole executor of the pre-change entry gate; this unshipped Stage follow-up consumes and dispositions the one record so no divergent measurement can appear. A nonzero result blocks 052-S and prevents any MSRV-compatibility assertion until a separately reviewed resolution is complete. Keeping it outside shipment 052-S avoids silently combining an unresolved toolchain-policy change with the transport remedy while the T4 fan-in preserves final-release truthfulness.
<!-- END:implementation-notes -->