---
artifact_type: feature
commit: 642c3e4
created_at: 2026-08-21T20:12:55.6530509Z
custom_fields:
    harness_status: pending
    source_stash_id: 7BF1961D
    source_stash_kind: bug
    source_stash_path: stash.jsonl
    source_stash_priority: high
    source_stash_text: 'Graphtor-docs MCP server fails during client initialization on load: Failed to connect to MCP server "graphtor-docs" because the transport pipe closes with OS error 232 while sending the initialize request. The regression began with the most recent Copilot.exe (GitHub Copilot CLI / GHCP CLI) builds. Reproduce with /mcp show graphtor-docs, compare recent CLI versions, and inspect server startup logs and initialize-handshake compatibility.'
id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
    - docs/decisions/2026-08-21-mcp-serve-initialize-os-error-232-deliberation.md
status: queued
title: Fix MCP serve initialize-handshake regression (Copilot CLI OS error 232)
updated_at: 2026-08-23T07:26:57.163098Z
---

<!-- BEGIN:description -->
Recent GitHub Copilot CLI builds fail to connect to the graphtor-docs MCP server: the STDIO transport pipe closes with Windows OS error 232 (ERROR_NO_DATA) while the client sends the initialize request. OS error 232 is a write to a closed pipe, so the server process is exiting before the handshake completes — most plausibly one of cmd_serve's pre-serve early-exit / fail-closed paths (cwd-relative .graphtor/*.db discovery, lock contention / stale-lock pid reuse, or a fail-closed config/schema gate), triggered by a change in how the new CLI launches the child (cwd/env/lifecycle) rather than any graphtor-docs code change. Restore connectivity evidence-first and test-first. Full diagnosis: docs/decisions/2026-08-21-mcp-serve-initialize-os-error-232-deliberation.md; plan: docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md.
<!-- END:description -->

<!-- BEGIN:dod -->
- `/mcp show graphtor-docs` connects on the exact newest failing CLI identity with completed initialize and no OS error 232.
- Feature scope is 25 tasks (056.001-T..056.025-T). The probe is a standalone, non-published diagnostic crate at `tools/mcp-probe/` (own `Cargo.toml` with an empty `[workspace]` table plus `publish = false`, Rust 2021 / MSRV 1.75, crate-root `#![forbid(unsafe_code)]`), split by width: 056.020-T owns the std-only core synchronous transport (std-process/std-thread duplex pumps, half-close, bounded stderr drain, deadlines) — later probe tasks may add narrowly justified non-Tokio deps; 056.023-T owns the copy-only observer seam, in-wrapper JSON-RPC correlation/redaction, and redacted evidence (`evidence.rs`, standalone `serde_json`); 056.021-T owns the isolated workspace plus control/treatment and ancestor/child config fixtures using a probe-local std-only containment check (no `graphtor_core` import); 056.022-T owns process spawning, teardown by direct `Child` handles only, and the versioned `wrapper` subcommand (a required injectable observation trait with a standalone `sysinfo` impl that observes but never kills, plus a standalone `cargo audit` gate). The probe sub-chain is `056.020 -> 056.022 -> 056.023 -> 056.021 -> 056.001`.
- T0 (056.001-T) owns `exact_cli.rs` and the exact-cli subcommand (the wrapper is 056.022-T) and runs a ONE-SHOT exact-client classification inside the isolated workspace, reading only the redacted evidence file: it first proves ancestor config-isolation against the owned nested fixture, runs exactly one control/treatment pair through the 056.022-T wrapper handoff (identical wrapper args except treatment `cwd`), emits the current ordered cause classification, then ends `done`. It NEVER declares H3-B2; a Gate-1 merge or both-legs-foreign result emits typed `H3-B-candidate` and continues forward to 056.019-T. It never loops, reruns per correction, waits for production health, or reopens a downstream task.
- Every causal task is visited once in one authoritative forward order. At its turn a task either records `not-needed:` and passes evidence unchanged, or implements exactly its one owned correction and runs exactly one exact-client re-probe through the shared runner. Backward-pointing or unowned evidence blocks T4 and creates a NEW bounded Stage follow-up rather than looping in place. 056.010-T owns exactly one H0c repair; a second H0c gate is a new bounded follow-up that blocks T4.
- Every task ends with all tests green. The shared driver task commits no branch-specific failing assertion; each curative task observes red and greens it before completion.
- Conditional tasks ruled out by evidence move to `done` with a `not-needed: <rationale>` comment. H3-B2 unsupported-client blocks shipment rather than claiming done.
- Diagnostics preserve unconditional fatal stderr and add typed events. H0b uses one conservative Database/Workspace policy and approval-gated exact legacy-lock recovery.
- H1 uses typed resolver outcomes (056.014-T), a lazy embedding lifecycle state machine (056.005-T), the versioned Loading/Failed/Disabled MCP availability projection (056.025-T), `cmd_serve`/background-sync orchestration (056.015-T), and serialized background-sync retry without blocking initialize.
- Managed config has one typed API owner (056.017-T). 056.024-T (chain `056.017 -> 056.024 -> 056.008`) closes `not-needed` when no managed-config mutation/recovery is selected, otherwise decides an MSRV-1.75, safe-call-site, no-follow config-mutation primitive without relaxing `#![forbid(unsafe_code)]` and records it in a NEW `docs/decisions/` artifact (or blocks 056.018/shipment); 056.008-T reconciles the generator `type`/`transport` discriminator unconditionally and generates the evidence-selected cwd contract; 056.018-T implements only the 056.024-T decision in `graphtor_core::path`; upgrade composes them and refreshes the target workspace before T4. The "never read/mutate user `.mcp.json`" invariant is scoped to T0/probe only; production managed-config tasks 056.008/056.009/056.018 may read/mutate/back up/recover the configured workspace `.mcp.json` with typed ownership and approval where destructive.
- 056.011-T owns transport types/wiring after 056.015-T and reacquires the exact-client transaction SEPARATELY before and after the fix through the 056.022-T wrapper (semantic initialize correlation plus redacted transcript digest, not raw-frame replay or persistence); 056.019-T is the sole H3-B terminal, adjudicating both isolated-config and working-directory mechanisms and running at most one deferred contrast when isolation becomes possible; T4 alone owns restored-production actual-client acceptance and its direct fan-in is all other tasks 001..025 except 004.
- Documentation tasks update only selected operator surfaces: 056.012-T owns `docs/troubleshooting.md` plus the existing `### search_semantic`/`### research_topic`/`### get_status` availability headings in `docs/mcp-tools.md` and an explicitly named `Runtime diagnostics and recovery` subsection created if needed (no CLI-reference ownership); 056.013-T owns the managed-launch content under the existing `### 2. Configure your MCP client` heading in `docs/mcp-tools.md` plus the `### serve`/`### install`/`### upgrade`/`### uninstall` sections of `docs/cli-reference/graphtor-docs.md` and reconciles the docs `type` vs generator `transport` discriminator against the CLI-honored field.
- All quality/MSRV gates pass, rollback is documented, and T4 records three exact-Copilot `/mcp show graphtor-docs` sessions — each with a completed, valid initialize (`jsonrpc: 2.0`, correlated id, no error, `result.protocolVersion`) and no OS error 232 against the restored production entry, correlated to the Copilot-spawned server startup event — including one diagnostic-gate-off start. `/mcp show` advertised tools are recorded as supporting evidence; a direct T1 production driver separately confirms expected tools and one side-effect-free `get_status` as a server control, not as proof of Copilot UI invocation.
<!-- END:dod -->

<!-- BEGIN:goals -->
- Restore graphtor-docs MCP connectivity for the newest GitHub Copilot CLI (no OS error 232; initialize handshake completes).
- Diagnose the true root cause with captured evidence (child exit code + stderr) before changing code.
- Harden the implicated pre-serve failure mode so benign launch differences (cwd, stale lock) no longer cause a silent exit-before-initialize.
- Make serve startup failures diagnosable even when the client discards child stderr.
<!-- END:goals -->