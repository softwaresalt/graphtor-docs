---
artifact_type: feature
created_at: 2026-08-21T20:12:55.6530509Z
custom_fields:
    harness_status: pending
    source_stash_id: 7BF1961D
    source_stash_kind: bug
    source_stash_path: stash.jsonl
    source_stash_priority: high
    source_stash_text: 'Graphtor-docs MCP server fails during client initialization on load: Failed to connect to MCP server "graphtor-docs" because the transport pipe closes with OS error 232 while sending the initialize request. The regression began with the most recent Copilot.exe (GitHub Copilot CLI / GHCP CLI) builds. Reproduce with /mcp show graphtor-docs, compare recent CLI versions, and inspect server startup logs and initialize-handshake compatibility.'
id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
    - docs/decisions/2026-08-21-mcp-serve-initialize-os-error-232-deliberation.md
status: queued
title: Fix MCP serve initialize-handshake regression (Copilot CLI OS error 232)
updated_at: 2026-08-25T07:48:16.0955952Z
---

<!-- BEGIN:description -->
Recent GitHub Copilot CLI builds fail to connect to the graphtor-docs MCP server: the STDIO transport pipe closes with Windows OS error 232 (ERROR_NO_DATA) while the client sends the initialize request. OS error 232 is a write to a closed pipe, so the server process is exiting before the handshake completes — most plausibly one of cmd_serve's pre-serve early-exit / fail-closed paths (cwd-relative .graphtor/*.db discovery, lock contention / stale-lock pid reuse, or a fail-closed config/schema gate), triggered by a change in how the new CLI launches the child (cwd/env/lifecycle) rather than any graphtor-docs code change. Restore connectivity evidence-first and test-first. Full diagnosis: docs/decisions/2026-08-21-mcp-serve-initialize-os-error-232-deliberation.md; plan: docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md.
<!-- END:description -->

<!-- BEGIN:dod -->
- This bug completes in PHASES delivered as separate release units, not as one 28-task chain. The umbrella feature 056-F is NEVER placed in a shipment that would archive it while remedy or acceptance tasks remain: it is the protected covering feature (partial-feature, task-only manifests exclude it per P-015) of the evidence shipment and of the later remedy and acceptance units. P-001/P-016 hold, but with precise roles: P-001 ensures ONE release unit is in flight at a time and P-016 prevents parallel implementation branches/worktrees -- NEITHER performs intra-feature cause selection. Selection is an explicit label + shipment-membership gate owned by Stage (see the selection-gate bullet). Shipments execute sequentially in one active branch/worktree, and Stage assembles the next remedy or acceptance unit only after the evidence unit's classification is available. Task count is 28 (056.001-T..056.028-T).
- PHASE 1, evidence foundation (the only currently queued shipment, 049-S, a task-only manifest of 056.020-T, 056.022-T, 056.023-T, 056.021-T, 056.001-T, 056.002-T, 056.003-T, 056.019-T). This unit is the "evidence foundation + parity-safe always-on diagnostics", NOT literally zero production changes: 056.003-T is an intentionally non-conditional, always-valuable production `cmd_serve` diagnostics seam (typed preflight-exit events + `mcp_serve_ready`) that lands on every cause. Its deliverable is a trusted exact-Copilot classification/cause-selection record plus that always-on diagnostics seam, NOT a production remedy. Acceptance: the standalone probe crate builds and self-tests green; T0 (056.001-T) proves ancestor config-isolation, runs exactly one control/treatment pair against the affected build (plus one bounded additional pair against a last-known-stable build when available), and emits the ordered selected cause family or families, with 056.019-T adjudicating the H3-B client mechanism when a candidate is emitted; the independent green out-of-process driver (056.002-T) and cmd_serve diagnostics (056.003-T) land green; and a bounded parity control (via the 056.002-T driver) proves a healthy `initialize` against the real binary AND that 056.003-T's `mcp_serve_ready`/typed diagnostics (stderr + tracing only) add no stdout bytes and do not alter stdout/protocol behavior. The always-on diagnostics seam is acceptable under YAGNI because startup-failure observability is valuable for EVERY cause family and for all future support, independent of which remedy is later selected. FAIL-CLOSED H3-B terminal semantics (single authority, aligned with 056.019-T): only a CONCLUSIVE H3-B1 (supported: done, forward evidence) or a proven H3-B2 (unsupported-client: done, blocking the downstream PHASE 2 units and PHASE 3 T4 which then have no safe client fix) is a terminal evidence classification that closes 056.019-T and lets 049-S close. An INCONCLUSIVE H3-B verdict is NOT terminal and NOT trusted cause selection: it moves 056.019-T to `blocked` with captured evidence, BLOCKS 049-S closure, and REQUIRES a named new bounded Stage follow-up (or operator adjudication); it is never described as done and never closes 049-S. Evidence-unit acceptance MUST NOT require the final restored-production T4 (056.004-T). The probe sub-chain is 056.020 -> 056.022 -> 056.023 -> 056.021 -> 056.001; the independent green driver 056.002 -> 056.003 (diagnostics); and 056.019 (sole H3-B terminal) consumes both 056.001 and 056.003. (056.002-T is a T0-agnostic reusable green driver and carries no dependency on 056.001-T.)
- The probe is a standalone, non-published diagnostic crate at `tools/mcp-probe/` (own `Cargo.toml` with an empty `[workspace]` table plus `publish = false`, Rust 2021 / MSRV 1.75, crate-root `#![forbid(unsafe_code)]`), split by width: 056.020-T owns the std-only core synchronous transport (duplex pumps, half-close, bounded stderr drain, deadlines, and a platform-portable in-crate helper-child self-test mechanism) AND bounded, test-only reaping of its own self-test fixture `Child` handles on every outcome; 056.023-T owns the copy-only observer seam, in-wrapper JSON-RPC correlation/redaction, and redacted evidence (`evidence.rs`, standalone `serde_json`); 056.021-T owns the isolated workspace plus control/treatment and ancestor/child config fixtures using a probe-local std-only containment check (no `graphtor_core` import); 056.022-T owns process spawning, teardown by direct `Child` handles implemented as RAII (`Drop`) guards that reap on every outcome INCLUDING panic/unwind, and the versioned `wrapper` subcommand (a required injectable observation trait with a standalone `sysinfo` impl that observes -- including the sentinel env-inheritance observation for 056.006-T -- but never kills, plus a standalone `cargo audit` gate). A dedicated standalone-probe CI job (056.028-T) covers the crate's separate manifest/lockfile; it is deliberately NOT in 049-S because its workflow depends on the standalone crate delivered by 049-S. Stage assembles it as an unconditional PHASE 1.5 evidence-infrastructure release unit (owner: Stage; ordering: immediately after 049-S closes and before any selected remedy shipment; readiness gate: standalone-probe CI green), not cause-selected, and it remains in T4 fan-in. Production and wrapper process lifecycle stays 056.022-T; exact PID-only or process-name-wide kills are forbidden -- every teardown or fixture cleanup goes only through owned direct `Child` handles.
- T0 (056.001-T) owns `exact_cli.rs` and the exact-cli subcommand (the wrapper is 056.022-T) and runs a ONE-SHOT exact-client classification inside the isolated workspace, reading only the redacted evidence file: it first proves ancestor config-isolation against the owned nested fixture, runs exactly one control/treatment pair against the affected build -- plus one bounded additional pair against a last-known-stable build when available (at most two pairs total, never more) -- through the 056.022-T wrapper handoff (identical wrapper args except treatment `cwd`), sets a sentinel env var and records the wrapper's inherited-or-not observation for 056.006-T env-inheritance selection, emits the current ordered cause classification, then ends `done`. It NEVER declares H3-B2; a Gate-1 merge or both-legs-foreign result emits typed `H3-B-candidate` and continues forward to 056.019-T. It never loops, reruns per correction, waits for production health, or reopens a downstream task.
- Explicit selection gate (dependency-ready != selected): every PHASE 2 (phase:remedy) task carries a machine-queryable `selection:pending` label plus its `cause:<family>` label. After 049-S closes, Stage consumes the T0/056.019-T classification, changes ONLY the selected cause families' tasks from `selection:pending` to `selection:selected`, and ONLY THEN may create a queued remedy shipment containing those selected tasks. Unselected tasks keep `selection:pending` until final disposition (moved to `done` with a `not-needed:<T0 evidence id/rationale>` comment during PHASE 3 assembly); because they have no shipment membership, no Ship/Orchestrator routing can claim them. The enforceable pre-shipment gate (Stage/Orchestrator MUST run before creating or claiming any remedy shipment, and MUST NOT add a `selection:pending` task to any shipment) is the query `SELECT id, labels FROM items WHERE id IN (<candidate member ids>) AND (labels NOT LIKE '%selection:selected%' OR labels LIKE '%selection:pending%')` -- any returned row FAILS the gate and the shipment MUST NOT be created. This is a required gate, not advisory. This label + shipment-membership gate -- NOT P-001 -- is the claim gate; P-001 only bounds one in-flight release unit and P-016 only prevents parallel implementation topology. Unselected hypothetical remedy tasks are NOT marked backlog status `blocked` (that would misrepresent them); they stay `queued` + `selection:pending` with no shipment membership until dispositioned not-needed.
- PHASE 2, selected remediation units (NOT queued for Ship until T0 evidence selects them; each stays a queued follow-up with NO shipment membership and `selection:pending`), grouped by cause family: managed-config cwd/recovery (056.017-T, 056.024-T, 056.008-T, 056.018-T, 056.009-T); type/transport discriminator (056.026-T generation, 056.027-T existing-install delivery); lock recovery (056.016-T, 056.007-T); H0c state repair (056.010-T); H1 model lifecycle (056.014-T, 056.005-T, 056.025-T, 056.015-T); H3-A transport compatibility (056.011-T); diagnostic sink (056.006-T, only if evidence selects it). Each family entry depends on the evidence foundation (056.003-T, or 056.019-T for the H3-B-selected managed-config/discriminator groups); intra-family order is a true dependency edge, but unrelated cause families are NOT chained into a total order -- sequential execution is enforced by the selection gate + Orchestrator/Ship one shipment at a time under P-001, not by fake cross-cause edges.
- Shipment-interface re-probe rule (replaces any global "every selected causal task implements and re-probes" rule): every SHIPPING production-remediation release unit owns exactly ONE exact-client re-probe after applying its correction. Characterization (056.016-T) and decision/PoC (056.024-T) tasks are NOT corrections and perform no re-probe. Conditional tasks a selected family does not need close with a `not-needed:<rationale>` comment and pass evidence unchanged; H3-B2 unsupported-client blocks shipment rather than claiming done. Each shipping curative task observes its own red and greens it before completion; the shared driver commits no branch-specific failing assertion.
- The managed-config cwd/recovery group and the discriminator group are internally complete and activate per selection, using the corrected DAG because delivery is split. cwd/recovery: 056.019 -> 056.017; then 056.017 -> 056.008 (cwd generation) IN PARALLEL WITH 056.017 -> 056.024 -> 056.018 (recovery); then {056.008, 056.018} -> 056.009 (cwd/recovery existing-install delivery). Discriminator: 056.019 -> 056.026 (UNCONDITIONAL generator `type`/`transport` reconciliation when a mismatch is evidenced, else not-needed) and {056.026, 056.018} -> 056.027 (bounded existing-install discriminator delivery). A discriminator-only remedy shipment = 056.026 + 056.027 + their safe-mutation prerequisites 056.017/056.024/056.018 (whose activation predicates INCLUDE discriminator existing-install delivery, so they are selected -- not not-needed -- and produce their typed API/recovery handles); 056.008 (cwd generation) and 056.009 (cwd delivery) close not-needed in a discriminator-only remedy. No prerequisite in either chain closes not-needed while a selected downstream member still depends on its API/handles. 056.024-T blocks only 056.018/shipment; it never blocks 056.008-T's cwd generation or 056.026-T's discriminator generation. When both the cwd and discriminator remedies are co-selected into one managed-config shipment, Stage orders 056.026 before 056.008 at assembly time (discriminator field reconciled first, then cwd composed) -- a shipment-assembly co-selection edge, not a standing backlog dependency. The "never read or mutate user `.mcp.json`" invariant is scoped to T0/probe only; production managed-config tasks (056.008/056.009/056.018/056.027) may read, mutate, back up, and recover the configured workspace `.mcp.json` with typed ownership and approval where destructive.
- H1 uses typed resolver outcomes (056.014-T), a lazy embedding lifecycle state machine (056.005-T), the versioned Loading/Failed/Disabled MCP availability projection (056.025-T), and `cmd_serve`/background-sync orchestration (056.015-T) with serialized retry that does not block initialize. 056.010-T owns exactly one H0c repair; a second H0c gate is a NEW bounded follow-up that blocks T4. 056.011-T owns transport types/wiring and reacquires the exact-client transaction SEPARATELY before and after its fix through the 056.022-T wrapper (semantic initialize correlation plus redacted transcript digest, not raw-frame replay or persistence); its standing dependency is 056.003-T, and it lands after the 056.015-T serve-orchestration restructure ONLY when H1 and H3-A are co-selected in one shipment (a shipment-assembly co-selection edge, not a standing cross-family backlog edge). 056.019-T is the sole H3-B terminal, adjudicating both isolated-config and working-directory mechanisms.
- PHASE 3, final acceptance and documentation (a later dependent unit, NOT queued for Ship now). 056.012-T, 056.013-T, and 056.004-T remain queued follow-ups outside 049-S and depend on completion of every remediation actually selected by T0 (unselected remedies close `not-needed` = done); they must not be prematurely Ship-ready. Final Assembly Protocol (Stage-owned, single executable procedure): after all selected remedy shipments complete, Stage (1) confirms every PHASE 2 task is terminal -- either `done` (selected + shipped) or `done` with a `not-needed:<T0 evidence id/rationale>` disposition (unselected); (2) for any selected NEW follow-up created during remediation (e.g. a second H0c gate, or an H3-B-inconclusive follow-up), Stage adds it to the final gate by a single Stage dependency update on the fan-in owners (056.004-T, and 056.012-T for docs) BEFORE creating the PHASE 3 shipment -- never by reopening a completed code task; (3) only then does Stage create the queued PHASE 3 shipment. The single registry-backed acceptance gate is 056.004-T (T4), whose fan-in is every other 056.x task; the invariant is that no PHASE 3 task becomes Ship-ready until every PHASE 2 task reaches a terminal disposition and no future follow-up requires reopening a completed code task. Where backlogit supports shipment-to-shipment `blocks` dependencies, Stage may additionally sequence the PHASE 3 shipment after the selected remedy shipments; otherwise the task-dependency fan-in above is the gate.
- T4 (056.004-T) is the SOLE owner of restored-production actual-client acceptance; its direct fan-in is all tasks 056.001..056.028 except 056.004 (including the discriminator generation/delivery 056.026/056.027 and the standalone probe CI job 056.028). Acceptance: the exact newest Copilot's `/mcp show graphtor-docs` proves actual-client connection and initialization and the ABSENCE of OS error 232 against the restored production entry using ONLY the fields the CLI exposes (connection/initialization status and, when shown, advertised tool list/count), across three sessions including one diagnostic-gate-off start, each correlating the production config hash/file identity with the Copilot-spawned server startup event. The correlated JSON-RPC wire fields (jsonrpc 2.0, correlated id, no error, `result.protocolVersion`) are proven SEPARATELY by the direct T1 production driver/control (056.002-T) via its read-only production-workspace 'server control' mode against the same production binary/workspace, NOT by `/mcp show`. No wrapper, temporary config, executable substitution, or wrapper-only log is valid T4 evidence.
- Documentation updates only selected operator surfaces: 056.012-T owns `docs/troubleshooting.md` plus the existing `### search_semantic` / `### research_topic` / `### get_status` availability headings in `docs/mcp-tools.md` and an explicitly named `Runtime diagnostics and recovery` subsection created if needed (no CLI-reference ownership); 056.013-T owns the managed-launch content under the existing `### 2. Configure your MCP client` heading in `docs/mcp-tools.md`, the `### serve` / `### install` / `### upgrade` / `### uninstall` sections of `docs/cli-reference/graphtor-docs.md`, and -- when discriminator evidence selects a change -- the Quick Start `.mcp.json` example in `README.md`, reconciling the docs/README `type` vs generator `transport` discriminator (the 056.026-T reconciled field) against the CLI-honored field.
- All quality/MSRV gates pass and rollback is documented for each shipping remediation unit; the settled removed designs (custom Cargo target/DACL hardening, user-config substitution, raw-frame persistence) are NOT reintroduced.
<!-- END:dod -->

<!-- BEGIN:goals -->
- Restore graphtor-docs MCP connectivity for the newest GitHub Copilot CLI (no OS error 232; initialize handshake completes).
- Diagnose the true root cause with captured evidence (child exit code + stderr) before changing code.
- Harden the implicated pre-serve failure mode so benign launch differences (cwd, stale lock) no longer cause a silent exit-before-initialize.
- Make serve startup failures diagnosable even when the client discards child stderr.
<!-- END:goals -->