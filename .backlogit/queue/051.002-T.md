---
artifact_type: task
created_at: 2026-07-14T18:56:06.9466449-07:00
dependencies:
    - 051.001-T
id: 051.002-T
labels:
    - phase-2
    - install
    - cli
    - rust
parent_id: 051-F
priority: medium
references:
    - docs/exec-plans/2026-07-14-consumption-first-graphtor-plan.md
    - docs/decisions/2026-07-14-consumption-first-graphtor-deliberation.md
status: queued
title: 'P2-T2a: install --with-ingestion CLI flag + plumbing'
updated_at: 2026-07-15T08:03:15.0334197-07:00
---

Add --with-ingestion to InstallArgs (src/cli/mod.rs:274) and thread it through cmd_install (src/main.rs) to select the install PATH: the consumption-first default routes to install_minimal() (P2-T1 / 051.001-T) while --with-ingestion routes to the PRESERVED full-scaffold install() (invoked by the P2-T2b scaffold path). This task is the CLI flag + plumbing ONLY; the full-scaffold creation/extension is P2-T2b (051.008-T). Files: src/cli/mod.rs, src/main.rs (cmd_install). Depends on P2-T1. Test-first.

<!-- BEGIN:acceptance-criteria -->
--with-ingestion is parsed on the install command and threaded to cmd_install as the install-path selector; the absent flag selects the consumption-first default (install_minimal()); the present flag selects the preserved full install() path; no scaffold behaviour is implemented in this task (asserted via the plumbed selector reaching the correct install path).
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Split from the original P2-T2 (thread 9: exactly three files violated the <3-files rule). This task owns cli/mod.rs + main.rs plumbing (2 files); P2-T2b (051.008-T) owns the full-scaffold install() path + sources.yaml. The opt-in path is additive to the P2-T1 default; both paths coexist (install_minimal() default, preserved install() for --with-ingestion) per the backward-compat design in P2-T1.
<!-- END:implementation-notes -->