---
artifact_type: task
created_at: 2026-07-14T18:56:06.9466449-07:00
dependencies:
    - 051.001-T
id: 051.002-T
labels:
    - phase-2
    - install
    - cli
    - rust
parent_id: 051-F
priority: medium
references:
    - docs/exec-plans/2026-07-14-consumption-first-graphtor-plan.md
    - docs/decisions/2026-07-14-consumption-first-graphtor-deliberation.md
status: active
title: 'P2-T2a: install --with-ingestion CLI flag + plumbing'
updated_at: 2026-07-15T14:08:28.7195005-07:00
---

Add --with-ingestion to InstallArgs (src/cli/mod.rs:274) and thread it through cmd_install (src/main.rs) to select the install PATH: the consumption-first default routes to install_minimal() (P2-T1 / 051.001-T) while --with-ingestion routes to the PRESERVED full-scaffold install() (invoked by the P2-T2b scaffold path). Because this selector changes the DEFAULT install to install_minimal(), the command-level `Install` doc comment that clap renders as `install --help` (src/cli/mod.rs:115-120) currently PROMISES the default creates `bin/data/cache/config/logs` and copies the binary - behaviour that no longer occurs on the default path. Update the `Install` doc comment so `--help` describes the consumption-first minimal default and documents `--with-ingestion` as the opt-in for the full scaffold (PR #88 thread PRRT_kwDORiB5E86RNyVA). This task is the CLI flag + plumbing + help-text update ONLY; the full-scaffold creation/extension is P2-T2b (051.008-T). Files: src/cli/mod.rs, src/main.rs (cmd_install). Depends on P2-T1. Test-first.

<!-- BEGIN:acceptance-criteria -->
--with-ingestion is parsed on the install command and threaded to cmd_install as the install-path selector; the absent flag selects the consumption-first default (install_minimal()); the present flag selects the preserved full install() path; no scaffold behaviour is implemented in this task (asserted via the plumbed selector reaching the correct install path). The `install --help` output (clap-rendered from the updated `Install` doc comment) describes the minimal consumption-first default and the `--with-ingestion` opt-in, and NO LONGER claims the default creates the full `bin/data/cache/config/logs` scaffold or copies the binary; a help-text assertion (e.g. rendering the command help and matching on the updated wording) locks this.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Split from the original P2-T2 (thread 9: exactly three files violated the <3-files rule). This task owns cli/mod.rs + main.rs plumbing (2 files); P2-T2b (051.008-T) owns the full-scaffold install() path + sources.yaml. The opt-in path is additive to the P2-T1 default; both paths coexist (install_minimal() default, preserved install() for --with-ingestion) per the backward-compat design in P2-T1. The `Install` doc comment (src/cli/mod.rs:115-120) lives in a file already in scope, so the help-text update adds no new file; clap derives `install --help` directly from that doc comment, so updating the doc comment IS the help-text change and the help assertion renders/inspects the command help to prove the stale full-scaffold promise is gone.
<!-- END:implementation-notes -->