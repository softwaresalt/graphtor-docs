---
artifact_type: task
created_at: 2026-07-15T00:47:35.1984022-07:00
dependencies:
    - 051.002-T
    - 051.004-T
id: 051.008-T
labels:
    - phase-2
    - install
    - scaffold
    - rust
parent_id: 051-F
priority: medium
references:
    - docs/exec-plans/2026-07-14-consumption-first-graphtor-plan.md
    - docs/decisions/2026-07-14-consumption-first-graphtor-deliberation.md
status: queued
title: 'P2-T2b: Opt-in full-ingestion scaffold + managed marker'
updated_at: 2026-07-15T08:03:15.6617386-07:00
---

When --with-ingestion is set (P2-T2a), invoke the PRESERVED full-scaffold install() (src/workspace/install.rs:34 - already creates .graphtor/{bin,data,cache,config,logs} + copies the binary; unchanged by P2-T1) and complete the generation footprint: write the template sources.yaml (init) and write .mcp.json via the shared MCP config writer (051.004-T / P2-T3) so the managed server entry uses the pinned .graphtor/bin path AND carries the managed-entry provenance marker; retain the existing managed .gitignore behaviour (unless --no-gitignore). The graphtor dev workspace uses this path. Files: src/workspace/install.rs. Depends on P2-T2a (flag) + P2-T3 (shared writer). Test-first.

<!-- BEGIN:acceptance-criteria -->
--with-ingestion produces the full layout (via the preserved install()) + sources.yaml + copied binary; the written .mcp.json server entry uses the pinned .graphtor/bin/graphtor-docs path AND contains the managed-entry provenance marker (written through the shared P2-T3 writer); .gitignore is managed (unless --no-gitignore); default (no flag) creates none of these and writes no .gitignore.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Scaffold path only; CLI flag/plumbing is P2-T2a (051.002-T). Builds on the PRESERVED full install() (P2-T1 keeps install() intact per the backward-compat design) rather than re-creating the scaffold from scratch. Binary-resolution ladder + provenance marker + atomic write are owned by P2-T3 (051.004-T) and consumed here so uninstall (P2-T5b) can identify the managed entry. INV: marker parity across the minimal (install_minimal()) and full (install()) install paths.
<!-- END:implementation-notes -->