---
artifact_type: task
created_at: 2026-08-22T00:30:52.5313368Z
dependencies:
    - 056.002-T
id: 056.011-T
parent_id: 056-F
priority: medium
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: rmcp/client transport compatibility fix (H3 only)'
updated_at: 2026-08-22T00:30:52.5313368Z
---

<!-- BEGIN:description -->
CONDITIONAL on T0/T1 evidence implicating H3 - an rmcp 1.5 vs newest Copilot CLI transport/framing incompatibility - rather than an early exit (H0) or model latency (H1). This task owns the ONLY real H3 lever (an rmcp bump and/or a minimal client-transport framing fix), kept LIVE but low-confidence per the deliberation, so H3 has a traceable queued owner instead of an implicit Ship-created task. Reach a healthy initialize handshake, keep the rmcp bump isolated and reversible, and do NOT change get_info (a proven no-op on rmcp 1.5). Prove it test-first with an observed-red handshake test (child still alive, initialize never negotiates) that greens after the fix. Close as not-needed if T0/T1 does not implicate H3.
<!-- END:description -->

<!-- BEGIN:acceptance-criteria -->
- CONDITIONAL - H3-only (low confidence but LIVE). If T0/T1 does NOT implicate H3, close as not-needed with a one-line rationale; closing as not-needed satisfies 056.004-T's dependency on it. H3 = an rmcp 1.5 vs newest Copilot CLI transport/framing incompatibility: the child stays ALIVE (no early-exit code, ruling out H0) and is not merely slow model latency (ruling out H1), yet the initialize handshake never negotiates a protocolVersion.
- Distinct width: MCP dependency / transport surface (Cargo.toml rmcp pin; rmcp serve_server + transport-io wiring). NOT cmd_serve, mcp_config, or lock. ~2h single-width.
- The evidenced compatibility fix reaches a HEALTHY initialize handshake: bump rmcp (1.8.0 available) and/or apply the minimal client-transport framing fix the newest CLI requires, keeping the rmcp bump ISOLATED (its own commit) so it can be pinned back independently.
- No get_info protocol-echo change (proven no-op on rmcp 1.5; H2 ruled out) - this task is transport/framing/version only.
- If an rmcp bump pulls transitive API changes (serve_server signature, schemars re-export), they are handled in THIS task's own review per docs/compound/best-practices/rmcp-1-5-serve-server-pattern-2026-04-30.md.
- Observed-red test (item 8 per-surface test-first): the 056.002-T transport harness (or a variant pinned to the CLI's framing) is RED on the H3 incompatibility (child still ALIVE, no early exit, initialize never negotiates a protocolVersion) and GREEN after the fix. The SOLE pass assertion stays a successful initialize response; the reproduced framing failure is diagnostic evidence only, never the expected assertion.
- Rollback: revert the rmcp bump and re-pin rmcp 1.5 - the isolation keeps it reversible.
- fmt + clippy pedantic (-D warnings) + tests pass; existing MCP tests (tests/mcp_manifest_test.rs) continue to pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Surfaces: Cargo.toml rmcp = "1.5" (features server, transport-io); the serve_server + STDIO transport wiring in src/main.rs::cmd_serve and src/mcp/server.rs. rmcp 1.8.0 is available (per the deliberation); a bump may pull transitive API changes (serve_server signature, schemars re-export) - re-verify against docs/compound/best-practices/rmcp-1-5-serve-server-pattern-2026-04-30.md. H3 discriminator vs H0/H1: the child is ALIVE and reading (no early-exit code, not a cold-model latency stall) yet the framed initialize never negotiates a protocolVersion. Keep the bump ISOLATED (its own commit) so re-pinning rmcp 1.5 is a clean revert. Depends on 056.002-T (the transport harness must exist to reproduce the framing failure).
<!-- END:implementation-notes -->