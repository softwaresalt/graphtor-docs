---
artifact_type: task
created_at: 2026-08-22T00:30:52.5313368Z
dependencies:
    - 056.002-T
id: 056.011-T
parent_id: 056-F
priority: medium
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: client/transport compatibility fix (H3 only, two modes)'
updated_at: 2026-08-22T02:05:00.0000000Z
---

<!-- BEGIN:description -->
CONDITIONAL on T0/T1 evidence implicating H3 - a client/transport incompatibility that SURVIVES the H0a cwd pin and is neither an early-exit discovery/lock/fail-closed cause (H0) nor model latency (H1). This task owns CLIENT/TRANSPORT COMPATIBILITY with TWO modes, each reaching a healthy initialize handshake, kept LIVE but low-confidence per the deliberation so H3 has a traceable queued owner instead of an implicit Ship-created task. MODE A (framing/version): the child stays ALIVE yet the framed initialize never negotiates a protocolVersion -> bump rmcp (1.8.0 available) and/or apply a minimal client-transport framing fix, kept ISOLATED and reversible; do NOT change get_info (a proven no-op on rmcp 1.5); prove it test-first with an observed-red handshake test (child still alive, initialize never negotiates) that greens after the fix. MODE B (client ignores/rejects the managed cwd): the 056.008-T generated-contract launch provably starts the child in a FOREIGN cwd - a managed-launch EARLY EXIT (NOT child-alive) - so the H0a cwd pin cannot cure it -> repair by an EVIDENCE-BACKED client-compatibility adjustment (select/document a supported CLI version, or a client-supported working-directory mechanism the CLI honors), verified by a MANUAL compatibility check that reaches a healthy handshake; NO server-side external-path fallback (runtime containment unchanged, F1/F2/F3/N1). Distinguish H3 from H0a by generated-contract / client-capability evidence: H0a = the CLI HONORS the pinned cwd (056.008-T's pin cures it); H3 mode B = the CLI IGNORES/REJECTS the pinned cwd. Close as not-needed if T0/T1 does not implicate H3.
<!-- END:description -->

<!-- BEGIN:acceptance-criteria -->
- CONDITIONAL - H3-only (low confidence but LIVE). If T0/T1 does NOT implicate H3, close as not-needed with a one-line rationale; closing as not-needed satisfies 056.004-T's dependency on it. H3 = a client/transport incompatibility that SURVIVES the H0a cwd pin and is neither an early-exit cause (H0) nor model latency (H1).
- TWO modes, each reaching a HEALTHY initialize handshake with a red/green OR manual compatibility verification (every mode must be capable of reaching a healthy handshake).
- MODE A (framing/version, child alive): the child stays ALIVE (no early-exit code) yet the framed initialize never negotiates a protocolVersion. Fix: bump rmcp (1.8.0 available) and/or apply the minimal client-transport framing fix, keeping the rmcp bump ISOLATED (its own commit) so it can be pinned back independently. Observed-red test: the 056.002-T transport harness (or a framing-pinned variant) is RED on the incompatibility (child alive, no early exit, initialize never negotiates) and GREEN after the fix; the SOLE pass assertion stays a successful initialize response, the framing failure is diagnostic evidence only.
- MODE B (client ignores/rejects the managed cwd, managed-launch early exit): the 056.008-T generated-contract launch provably starts the child in a FOREIGN cwd - a managed-launch EARLY EXIT (NOT child-alive) - so the H0a cwd pin cannot cure it. Fix: an EVIDENCE-BACKED client-compatibility adjustment (select/document a supported CLI version, or a client-supported working-directory mechanism the CLI honors) - NO server-side external-path fallback (runtime containment unchanged, F1/F2/F3/N1). Verification: the 056.008-T generated-contract test provides the RED evidence that the CLI ignores the pinned cwd (foreign-cwd early exit), and a MANUAL compatibility check on the selected/documented client configuration reaches a healthy initialize handshake (no automated red/green is possible when the fix is a client-version/mechanism selection outside this repo).
- Distinguish H3 from H0a by generated-contract / client-capability evidence: H0a = the CLI HONORS the pinned cwd (056.008-T's pin cures it); H3 mode B = the CLI IGNORES/REJECTS the pinned cwd.
- Distinct width: MCP dependency / transport surface (Cargo.toml [dependencies] rmcp pin; rmcp serve_server + transport-io wiring) plus, for mode B, the documented client-launch compatibility recipe hosted in 056.001-T (no repo code change). NOT cmd_serve, mcp_config, or lock. ~2h single-width.
- No get_info protocol-echo change (proven no-op on rmcp 1.5; H2 ruled out) - transport/framing/version (mode A) and client-compatibility (mode B) only.
- If a mode-A rmcp bump pulls transitive API changes (serve_server signature, schemars re-export), they are handled in THIS task's own review per docs/compound/best-practices/rmcp-1-5-serve-server-pattern-2026-04-30.md.
- Rollback: mode A - revert the rmcp bump and re-pin rmcp 1.5 (the isolation keeps it reversible); mode B - revert to the previously documented client configuration (no repo code change).
- fmt + clippy pedantic (-D warnings) + tests pass; existing MCP tests (tests/mcp_manifest_test.rs) continue to pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Surfaces: MODE A - Cargo.toml [dependencies] rmcp pin (features server, transport-io); the serve_server + STDIO transport wiring in src/main.rs::cmd_serve and src/mcp/server.rs. rmcp 1.8.0 is available (per the deliberation); a bump may pull transitive API changes (serve_server signature, schemars re-export) - re-verify against docs/compound/best-practices/rmcp-1-5-serve-server-pattern-2026-04-30.md. Keep the bump ISOLATED (its own commit) so re-pinning rmcp 1.5 is a clean revert. MODE B - no repo code change: the fix is a client-launch compatibility adjustment (a supported CLI version or a client-honored working-directory mechanism) documented in the 056.001-T recipe; NO server-side external-path fallback (the runtime stays cwd-anchored by containment, F1/F2/F3/N1). H3 discriminators: vs H0/H1 (mode A) the child is ALIVE and reading (no early-exit code, not a cold-model latency stall) yet the framed initialize never negotiates a protocolVersion; vs H0a (mode B) generated-contract / client-capability evidence shows the CLI IGNORES/REJECTS the pinned cwd (managed-launch early exit from a foreign cwd) rather than honoring it. Depends on 056.002-T (the transport harness must exist to reproduce mode A); the mode-B foreign-cwd evidence comes from the 056.008-T generated-contract launch.
<!-- END:implementation-notes -->