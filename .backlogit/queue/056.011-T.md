---
artifact_type: task
created_at: 2026-08-22T00:30:52.5313368Z
dependencies:
    - 056.001-T
    - 056.002-T
id: 056.011-T
parent_id: 056-F
priority: medium
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: client/transport compatibility fix (H3 only, two modes)'
updated_at: 2026-08-22T07:58:48.4112422Z
---

<!-- BEGIN:description -->
Conditional H3 compatibility. Mode A handles a live-child framing/version incompatibility. Mode B handles an actual CLI that ignores/rejects configured `cwd`, and explicitly chooses B1 (supported CLI that honors managed cwd, so 056.008/009 remain active) or B2 (different supported working-directory mechanism, so those tasks close). Every mode reaches a healthy actual-client initialize without weakening server containment.
<!-- END:description -->

<!-- BEGIN:acceptance-criteria -->
- H3 only. Mode A = child remains alive but valid initialize framing never negotiates. Mode B = T0's actual-client probe proves the current CLI ignores/rejects a known `cwd`. Neither mode is defined as surviving a cwd pin.
- Mode A: minimal framing fix or isolated rmcp bump, with an observed-red framing harness and successful initialize after. Any bump must compile, clippy, and test under repository MSRV Rust 1.75; reject a release whose transitive MSRV exceeds it. No `get_info` echo change.
- Mode B has two explicit submodes. B1: select a supported CLI version that honors managed `cwd`; keep 056.008-T and 056.009-T active, then repeat the probe and `/mcp show`. B2: select a documented client-supported working-directory mechanism independent of managed `cwd`; close 056.008-T/056.009-T not-needed and verify that mechanism plus `/mcp show`.
- Both B submodes must record a healthy actual-client initialize and no OS error 232. No server-side external-path fallback.
- If H3 is not evidenced, close not-needed.
- Rollback: re-pin rmcp for A or restore the previous documented client configuration for B.
- fmt + clippy pedantic + tests pass for code changes.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Mode A touches the rmcp dependency/stdio wiring and must preserve Rust 1.75. Mode B is an external client-compatibility decision grounded by T0; it owns the B1/B2 disposition before 056.008-T proceeds. B1 still needs managed contract generation and existing-install delivery. B2 documents and verifies the alternate supported mechanism. No target-derived authorization or server fallback.
<!-- END:implementation-notes -->