---
artifact_type: task
created_at: 2026-08-22T00:30:52.5313368Z
dependencies:
    - 056.003-T
id: 056.011-T
labels:
    - phase:remedy
    - cause:h3a-transport
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: fix server transport compatibility (H3-A only)'
updated_at: 2026-08-23T19:24:15.2527335Z
---

<!-- BEGIN:description -->
Conditional H3-A server transport/framing compatibility, owning only transport types and rmcp/stdio wiring after 056.015-T. Reacquire the exact-client transaction SEPARATELY before and after the fix through the 056.022-T wrapper and 056.023-T in-wrapper observer; each run validates semantic initialize correlation plus the redacted transcript digest/identity. It does NOT require byte-identical raw replay or raw-frame persistence. T4 alone owns restored-production actual-client acceptance.
<!-- END:description -->

<!-- BEGIN:acceptance-criteria -->
- If T0 does not evidence H3-A, move to `done` with `not-needed: H3-A not evidenced`; H3-B belongs to 056.019-T.
- Own ONLY transport types and rmcp/stdio wiring, landing AFTER 056.015-T's serve/projection wiring; do not restyle or re-wire 056.015-T's work.
- Do NOT require persisted or byte-identical raw frames. Reacquire the exact-client transaction SEPARATELY before and after the fix through the 056.022-T wrapper and the 056.023-T in-wrapper observer; each acquisition validates semantic initialize correlation (the exact request id to a `jsonrpc: "2.0"` non-error `result.protocolVersion`) plus the redacted transcript digest/identity. Add/observe the H3-A red from the before-run, then green it with the minimal fix and re-validate from the after-run. No raw bytes are written to disk; generic direct requests are insufficient.
- Inspect candidate metadata before implementation. rmcp 1.8.x is excluded under Rust 1.75. Prefer a minimal transport/framing adaptation around pinned 1.5.x; use another release only after documenting edition-2021/MSRV compatibility. If the fix requires a fork/patch override or exceeds width, halt for a separate deliberation rather than inventing it here. No `get_info` echo change.
- Any dependency change passes fmt, clippy, tests, audit, release build, and `cargo +1.75.0 check --all-targets`.
- Rollback re-pins the prior rmcp. T4 exclusively owns three exact-CLI production-entry starts; docs remain 056.013-T.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Own only transport types and rmcp/stdio wiring after 056.015-T; validate H3-A by SEPARATE before/after exact-client reacquisitions through the 056.022-T wrapper and the 056.023-T observer (semantic initialize correlation plus redacted transcript digest/identity), not raw-frame replay or persistence, and prove Rust 1.75 compatibility. 056.019-T owns client cwd compatibility.
<!-- END:implementation-notes -->