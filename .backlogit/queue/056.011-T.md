---
artifact_type: task
created_at: 2026-08-22T00:30:52.5313368Z
dependencies:
    - 056.001-T
    - 056.002-T
id: 056.011-T
parent_id: 056-F
priority: medium
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Conditional: client/transport compatibility fix (H3 only, two modes)'
updated_at: 2026-08-22T07:25:00.0000000Z
---

<!-- BEGIN:description -->
CONDITIONAL on T0/T1 evidence implicating H3 - a client/transport incompatibility that is neither an early-exit discovery/lock/fail-closed cause (H0) nor model latency (H1). This task owns CLIENT/TRANSPORT COMPATIBILITY with TWO modes, each reaching a healthy initialize handshake, kept LIVE but low-confidence per the deliberation. MODE A (framing/version): the child stays ALIVE yet the framed initialize never negotiates a protocolVersion -> bump rmcp (1.8.0 available) and/or apply a minimal client-transport framing fix, kept ISOLATED and reversible; do NOT change get_info (a proven no-op on rmcp 1.5); prove it test-first with an observed-red handshake test that greens after the fix. MODE B (client ignores/rejects configured cwd): T0's backup-first MANUAL real-CLI diagnostic-entry probe records that the target CLI launches the probe in a FOREIGN cwd despite a known `cwd` field. This proves the H0a lever is unavailable before 056.008-T is selected, so 056.008-T closes not-needed and this task repairs the condition with an EVIDENCE-BACKED client-compatibility adjustment (select/document a supported CLI version, or a client-supported working-directory mechanism the CLI honors), verified by a MANUAL compatibility check that reaches a healthy handshake. No server-side external-path fallback; runtime containment remains unchanged (F1/F2/F3/N1).
<!-- END:description -->

<!-- BEGIN:acceptance-criteria -->
- CONDITIONAL - H3-only (low confidence but LIVE). If T0/T1 does NOT implicate H3, close as not-needed with a one-line rationale; closing as not-needed satisfies 056.004-T's dependency on it. H3 = a client/transport incompatibility that SURVIVES the H0a cwd pin and is neither an early-exit cause (H0) nor model latency (H1).
- TWO modes, each reaching a HEALTHY initialize handshake with a red/green OR manual compatibility verification (every mode must be capable of reaching a healthy handshake).
- MODE A (framing/version, child alive): the child stays ALIVE (no early-exit code) yet the framed initialize never negotiates a protocolVersion. Fix: bump rmcp (1.8.0 available) and/or apply the minimal client-transport framing fix, keeping the rmcp bump ISOLATED (its own commit) so it can be pinned back independently. Observed-red test: the 056.002-T transport harness (or a framing-pinned variant) is RED on the incompatibility (child alive, no early exit, initialize never negotiates) and GREEN after the fix; the SOLE pass assertion stays a successful initialize response, the framing failure is diagnostic evidence only.
- MODE B (client ignores/rejects configured cwd): T0's temporary, backup-first real-CLI diagnostic-entry probe records that the actual child cwd remains foreign despite a known `cwd` field. Fix: an EVIDENCE-BACKED client-compatibility adjustment (select/document a supported CLI version, or a client-supported working-directory mechanism the CLI honors) - NO server-side external-path fallback (runtime containment unchanged, F1/F2/F3/N1). Verification: repeat the same real-CLI probe and then `/mcp show graphtor-docs` under the selected client configuration; both must show the honored project-root cwd and a healthy initialize handshake. No automated red/green is possible when the fix is client selection outside this repo.
- Distinguish H3 from H0a with T0's real-CLI capability evidence: H0a = the target CLI HONORS a known `cwd`, so 056.008-T can generate the pin; H3 mode B = the target CLI IGNORES/REJECTS it, so 056.008-T closes not-needed.
- Distinct width: MCP dependency / transport surface (Cargo.toml [dependencies] rmcp pin; rmcp serve_server + transport-io wiring) plus, for mode B, the documented client-launch compatibility recipe hosted in 056.001-T (no repo code change). NOT cmd_serve, mcp_config, or lock. ~2h single-width.
- No get_info protocol-echo change (proven no-op on rmcp 1.5; H2 ruled out) - transport/framing/version (mode A) and client-compatibility (mode B) only.
- If a mode-A rmcp bump pulls transitive API changes (serve_server signature, schemars re-export), they are handled in THIS task's own review per docs/compound/best-practices/rmcp-1-5-serve-server-pattern-2026-04-30.md.
- Rollback: mode A - revert the rmcp bump and re-pin rmcp 1.5 (the isolation keeps it reversible); mode B - revert to the previously documented client configuration (no repo code change).
- fmt + clippy pedantic (-D warnings) + tests pass; existing MCP tests (tests/mcp_manifest_test.rs) continue to pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Surfaces: MODE A - Cargo.toml [dependencies] rmcp pin (features server, transport-io); the serve_server + STDIO transport wiring in src/main.rs::cmd_serve and src/mcp/server.rs. rmcp 1.8.0 is available (per the deliberation); a bump may pull transitive API changes (serve_server signature, schemars re-export) - re-verify against docs/compound/best-practices/rmcp-1-5-serve-server-pattern-2026-04-30.md. Keep the bump ISOLATED (its own commit) so re-pinning rmcp 1.5 is a clean revert. MODE B - no repo code change: the fix is a client-launch compatibility adjustment documented in the 056.001-T recipe; NO server-side external-path fallback. H3 discriminators: mode A leaves the child alive and reading yet initialize never negotiates; mode B is established by T0's real-CLI diagnostic-entry probe showing that configured cwd is ignored/rejected. The explicit 056.001-T dependency owns mode-B evidence; 056.002-T owns the mode-A transport harness.
<!-- END:implementation-notes -->