---
artifact_type: subtask
created_at: 2026-08-17T06:47:39.6120933Z
id: 054.001.003-ST
labels:
    - dark-mode
    - read-only-serve
    - observability
    - pr90
    - post-cap-residual
parent_id: 054.001-T
references:
    - docs/exec-plans/2026-08-16-readonly-serve-guarantee-hardening-plan.md
    - .github/instructions/release-observability.instructions.md
status: queued
title: Capture release-observability evidence for the A1 startup-log change
updated_at: 2026-08-17T06:47:39.6120933Z
---

Post-cap residual decomposition (finding D): the Unit A1 runtime startup-log wording change (open_engine_readonly log line, src/db/store.rs:209-212) is a runtime-surface change and therefore requires release-observability evidence before shipment 047-S merges (release-observability capability pack). Produce a bounded monitoring/rollback record for this specific log change.

Width isolation: OBSERVABILITY / operational artifact only (a release-observability evidence block carried into the 047-S operational-closure record). No code, no test, no doc-contract wording (those are 054.001.002-ST / 054.002-T).

Required evidence (all five):
1) Owner — the named person/role who watches the change post-merge.
2) Baseline — the current log line ('opened engine-enforced read-only SQLite DataStore (filesystem lock active)'), where/when it is emitted (once per open_engine_readonly at serve start, ReadOnly posture), and any log-scraping/alerting that currently keys on that substring.
3) Bounded post-deploy observation window — explicit duration + owner (e.g. first N serve starts or first X days).
4) Failure threshold — the concrete signal that the reworded line broke operator reasoning or downstream log parsing/alerting (e.g. an alert/dashboard that matched the old substring now silent, or misleading unconditional read-only claims resurfacing).
5) Rollback trigger + rollback procedure — the named condition that triggers rollback and the exact revert (revert the wording commit; guard runtime is unchanged, so the revert is text-only and safe).

Acceptance:
1) A release-observability evidence block containing all five elements (owner, baseline, observation window, failure threshold, rollback trigger + procedure) is recorded and carried into the 047-S operational-closure artifact.
2) The rollback procedure names the wording-revert path and confirms rollback affects no guard/runtime behavior.

<!-- BEGIN:implementation-notes -->
Release-observability instructions: the monitoring plan + rollback discipline are releasability evidence for a runtime-surface change, produced BEFORE merge and carried into operational closure. This is a low-risk text change, so the plan is a bounded MANUAL observation window rather than a dashboard build — but owner, failure threshold, and rollback trigger MUST be explicit. Independent of the red-test/impl ordering (authored from the design); no code dependency. Sibling of 054.001.001-ST (red test) and 054.001.002-ST (impl).
<!-- END:implementation-notes -->