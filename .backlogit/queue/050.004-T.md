---
artifact_type: task
created_at: 2026-07-14T18:55:26.9221488-07:00
dependencies:
    - 050.002-T
id: 050.004-T
labels:
    - phase-1
    - serve
    - classification
    - rust
parent_id: 050-F
priority: medium
references:
    - docs/exec-plans/2026-07-14-consumption-first-graphtor-plan.md
    - docs/decisions/2026-07-14-consumption-first-graphtor-deliberation.md
status: active
title: 'P1-T2: Content-derived posture classification (three-way, fail-safe)'
updated_at: 2026-07-15T14:08:26.2913122-07:00
---

In serve_discovery, classify each candidate db into a per-db ServeMode with a THREE-WAY outcome that runs BEFORE the write-path run_duplicate_intake_preflight (src/main.rs:2398-2406). Expose a PURE function returning BOTH the Vec<(PathBuf, ServeMode)> classification AND the filtered per-database Generation source groups (the subset of source groups whose resolved target db is Generation), so downstream preflight/background sync receive ONLY Generation sources and never the full SourceConfig. Depends on P1-T1. Test-first.

<!-- BEGIN:acceptance-criteria -->
Three high-level verification scenarios (all substantive checks retained):

(a) happy-path / mixed classification — a local source whose path exists AND has >=1 ingestible file promotes ONLY the candidate db whose resolved (canonicalized) path equals that source's resolved target db -> Generation (real non-empty source -> Generation); the pure function ALSO returns filtered Generation-only source groups, so a MIXED config (valid source -> db A, stale/unresolvable source -> db B) yields a Generation group for ONLY db A while db B is ReadOnly and excluded, and mixed valid+stale sources targeting DIFFERENT dbs return only the valid db's source group.

(b) fail-safe / read-only defaults — absent/empty/stale/unresolvable sources -> ReadOnly (existing-but-empty path -> ReadOnly; stale sources.yaml paths absent -> ReadOnly); a resolvable source NEVER promotes an unrelated co-resident dropped db (source-backed db + co-resident dropped db -> only the source's target db is Generation, the dropped db stays ReadOnly).

(c) fail-closed / malformed input — a malformed/unparseable sources.yaml -> fail-closed hard Err(GraphtorError).
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Change cmd_serve call ordering so classification precedes the preflight. 'Resolvable real source' = path exists AND >=1 ingestible file (locked stricter definition per decision doc). Returning filtered Generation source groups (not the full SourceConfig) is what lets P1-T3 hand ONLY Generation sources to run_duplicate_intake_preflight and spawn_background_sync, which internally re-splits its SourceConfig via split_plan_by_database (src/main.rs:2268-2269) - a full config would otherwise re-schedule stale/read-only targets.
<!-- END:implementation-notes -->