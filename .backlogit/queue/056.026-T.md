---
artifact_type: task
created_at: 2026-08-23T20:44:27.4319238Z
dependencies:
    - 056.019-T
id: 056.026-T
labels:
    - phase:remedy
    - cause:discriminator
    - selection:pending
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Reconcile generated MCP config type/transport discriminator (unconditional)
updated_at: 2026-08-23T20:47:44.4541268Z
---

Unconditional generator discriminator reconciliation split out of 056.008-T. Whenever exact-CLI evidence (T0 / 056.019-T) shows a docs/generator `type` vs `transport` mismatch, reconcile the generated stdio discriminator in `managed_server_value` to the exact CLI-honored field and align exact legacy-shape recognition. Owns ONLY the `managed_server_value` discriminator field plus exact legacy-shape recognition and focused generation tests. Depends only on the completed evidence classification (056.019-T), not on 056.017/056.024/056.018. Closes not-needed when no mismatch is evidenced. Fresh-install generation only; existing-install delivery is the paired discriminator-delivery task.

<!-- BEGIN:acceptance-criteria -->
- Depend ONLY on 056.019-T (completed evidence classification); do NOT depend on 056.017-T/056.024-T/056.018-T. This task is the discriminator-family generation root off the evidence spine.
- If exact-CLI evidence shows no `type`/`transport` mismatch, move to `done` with `not-needed: no type/transport mismatch evidenced` and pass evidence unchanged.
- When a mismatch is evidenced, reconcile the generated stdio discriminator in `managed_server_value` to the exact CLI-honored field regardless of the primary H0/H1/H3 cause. Own ONLY the discriminator field of `managed_server_value` plus exact legacy-shape recognition (`is_exact_legacy_shape`) and focused generation tests; do NOT own the cwd/working-directory field (056.008-T) or any mutation/recovery/upgrade delivery.
- Preserve marker ownership, exact unowned/non-JSON byte preservation, and legacy-shape recognition of BOTH the pre-reconciliation and reconciled discriminator so already-installed managed entries still refresh.
- Write and observe red generation tests first, then green exactly three groups: discriminator reconciled to the CLI-honored field; legacy-shape recognition across old and new discriminator; and containment/marker preservation. This proves generation only -- not client capability, existing-install delivery, or production acceptance.
- Co-selection assembly rule: when both this discriminator remedy and the H0a/H3-B1 cwd remedy (056.008-T) are selected into one managed-config shipment, Stage orders this task before 056.008-T at assembly time (discriminator field reconciled first, then the cwd field composed) -- a shipment-assembly co-selection edge, not a standing backlog dependency.
- Rust 1.75, fmt, clippy pedantic, and targeted generation tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Split from 056.008-T's former unconditional discriminator part. Sole owner of the generated `managed_server_value` discriminator field plus legacy-shape recognition in `src/workspace/mcp_config.rs`; 056.017-T remains the sole owner of the typed mutation signature/outcomes and 056.008-T owns only the cwd/working-directory generation. Existing-install discriminator delivery is the paired new delivery task. Not-needed when no `type`/`transport` mismatch is evidenced.
<!-- END:implementation-notes -->