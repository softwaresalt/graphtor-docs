---
artifact_type: task
created_at: 2026-08-21T20:13:57.9801621Z
dependencies:
    - 056.002-T
id: 056.003-T
labels:
    - phase:evidence
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Harden cmd_serve diagnostics and emit preflight-complete event
updated_at: 2026-08-23T19:23:14.426317Z
---



<!-- BEGIN:acceptance-criteria -->
- Inventory every pre-transport normal exit across `cmd_serve`, duplicate-intake preflight, and `open_serve_databases`; route them through one typed `ServePreflightExit` seam (or equivalent exhaustive enum) rather than a fixed count. Include pre-v4 and duplicate-intake exits.
- Preserve the existing unconditional `eprintln!`/`errfmt` fatal message for each exit and additionally emit a stable `tracing::error!` event. `RUST_LOG=off` must not silence the user-facing failure. stdout stays protocol-clean.
- Before propagating registry/open/schema errors, emit one stable structured serve-preflight error event; the existing top-level renderer remains the sole fatal renderer for propagated errors.
- Emit `mcp_serve_ready` only after all gates pass and immediately before `serve_server`; it means preflight complete/about to call, not handshake or loop entry.
- Reuse the T1 shared test-driver module. Write and observe these three groups failing (observed-red) before implementation, then green them: exhaustive formatter/event mapping for typed exits; representative propagated error plus `RUST_LOG=off` loud stderr; and ready event with a real seeded fixture.
- Rust 1.75, fmt, clippy pedantic, and tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Non-conditional serve observability. Route every pre-transport normal exit through a typed diagnostic seam, preserve unconditional fatal stderr, add structured events, and emit `mcp_serve_ready` only immediately before `serve_server`; 056.012-T owns docs.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Own the typed seam/formatter in `src/workspace/serve_preflight.rs` and minimal `src/main.rs` wiring plus shared-driver coverage. Do not convert loud failures into tracing-only output or duplicate the top-level fatal renderer. 056.015-T depends on this task before editing `cmd_serve`.
<!-- END:implementation-notes -->