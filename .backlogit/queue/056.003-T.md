---
artifact_type: task
created_at: 2026-08-21T20:13:57.9801621Z
dependencies:
    - 056.002-T
id: 056.003-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Harden cmd_serve pre-serve resolution (green, H0a-only conditional)
updated_at: 2026-08-21T20:56:44.2829204Z
---



<!-- BEGIN:acceptance-criteria -->
- CONDITIONAL — this task is H0a-only. If T0 evidences H0b, H1, or a fail-closed gate (H0c) instead of H0a (launch-cwd discovery), close this task as not-needed with a one-line rationale; closing as not-needed satisfies 056.004-T's dependency on it. Its H0a-specific acceptance below cannot be satisfied on a non-H0a branch, so it must NOT be forced.
- The T1 harness passes for the H0a branch: the server no longer exits before initialize for the reproduced benign changed-cwd launch.
- Only the SINGLE evidenced H0a runtime cause is implemented here (no bundled stale-lock, logging, or managed-launch-config work — the managed .mcp.json launch contract is the separate H0a-gated task 056.008-T).
- Workspace-root resolution is containment-safe: resolved only from explicit --db-path/--config inputs or the launch cwd, never by walking to parent directories; an explicit --db-path/--config target is PREFERRED over the launch cwd for the SERVED DB SET. Note this alone does NOT restore registry-backed Generation/background-sync (those validate against the launch cwd) — curative connectivity for H0a is owned by 056.008-T (the launch-contract task).
- Containment refusal is DELEGATED to the shared graphtor_core::path::validate_path / is_reparse_point primitives (same guards as src/workspace/serve_discovery.rs), canonicalizing BOTH operands before comparison — no hand-rolled prefix check. An explicit refusal test enumerates absolute-above, ..-traversal, escaping symlink, junction/reparse-point, and a Windows short-name/case variant, each asserted refused.
- EVERY silent exit-2 'no databases found to serve' site (both discovery sites plus the primary-None guard) becomes a loud, actionable error.
- Existing fail-closed semantics preserved AND regression-asserted: malformed registry, missing explicit --config, pre-v4 schema gate, and duplicate-intake preflight each still exit pre-serve after the cwd-resolution change.
- A positive serve-ready startup info log is emitted on successful transport bind (Constitution V).
- fmt + clippy pedantic (-D warnings) + tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
CONDITIONAL on T0/T1 evidencing H0a (launch-cwd .graphtor discovery). Fix ONLY the single evidenced H0a runtime cause in cmd_serve (do not bundle unrelated variants, optional logging, or the managed-launch-config change). Resolve the workspace root in a containment-safe way — only from an explicit --db-path/--config input or the launch cwd itself, never by walking to parent directories, so resolution cannot escape or climb above the launch-cwd containment boundary (Principle III/IV) — refuse any candidate that lands outside/above the boundary, convert every silent exit-2 'no databases found to serve' into a loud, actionable error, and emit a positive serve-ready log. The explicit-target preference decouples the served DB set from a cwd the CLI may no longer supply, but does NOT by itself restore registry-backed Generation/background-sync (those are launch-cwd anchored — see 056.008-T). Green the T1 harness for the H0a branch. If T0 evidences H0b/H1/H0c instead, close this task as not-needed (satisfies 056.004-T). The managed .mcp.json launch contract (056.008-T), stale-lock liveness (H0b, 056.007-T), and the optional diagnosability sink (056.006-T) are separate conditional tasks.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Surfaces: src/main.rs::cmd_serve (~2446-2655) early-exit paths (auto-discovery is cwd.join(".graphtor")). This task is RUNTIME cmd_serve only; the managed .mcp.json launch-contract generation in src/workspace/mcp_config.rs is the separate H0a-gated task 056.008-T. Do NOT add an upward/parent-directory search — that would violate CLI containment (Principle IV). Add a positive serve-ready info log. Real lock symbols (for the conditional H0b task, not this one) are DatabaseLock::acquire / AdvisoryLock::acquire / handle_existing_lock in src/lock.rs — there is no `acquire_database_lock`. Diagnosability default is a documented stderr-redirect recipe; the opt-in file sink is the conditional 056.006-T task, and stale-lock liveness (H0b) is the conditional 056.007-T task.
<!-- END:implementation-notes -->