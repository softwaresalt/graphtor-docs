---
artifact_type: task
created_at: 2026-08-21T20:13:57.9801621Z
dependencies:
    - 056.002-T
id: 056.003-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Harden cmd_serve pre-serve diagnostics: loud exit-2 errors + serve-ready log (parity-safe)'
updated_at: 2026-08-22T07:59:11.7503327Z
---



<!-- BEGIN:acceptance-criteria -->
- NON-CONDITIONAL diagnostics only; curative connectivity remains in the evidence-selected branch.
- Cover four distinct pre-transport exit-2 sites: missing explicit `--config`, empty discovered paths, empty classified postures, and the primary-None guard. Each loud error names the actual launch cwd / authorized candidate root (not a falsely resolved project root) and gives bounded remediation: launch from/pin the project root, place a database under `.graphtor`, or supply an already-supported explicit target.
- Emit a structured `mcp_serve_ready` info event with `transport=stdio` and the launch cwd immediately before entering `rmcp::serve_server`, after all preflight gates pass. This means initialize-ready serve-loop entry, not a post-handshake or OS bind event.
- Preserve current containment and discovery signatures: `candidate_root = cwd`; no target-derived/split root, parent walk, or external-path fallback.
- Preserve malformed-registry, missing-explicit-config, pre-v4, and duplicate-intake fail-closed semantics.
- One table-driven red diagnostic matrix covers all four exit-2 messages plus a ready-event row. The ready row starts a valid fixture with stdin open but sends no initialize, observes the pre-`serve_server` event, then cleans up its owned child. Preserve the existing explicit-db/no-registry negative `config file` assertion.
- fmt + clippy pedantic + tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Non-conditional `cmd_serve` observability. Make all four pre-transport exit-2 sites loud and actionable, naming the actual launch cwd / authorized candidate root. Emit a structured initialize-ready event immediately before entering `rmcp::serve_server`, after preflight succeeds. Do not call it a transport bind or handshake-complete event, and do not change containment/discovery. H0a remains the managed launch-contract task; this task is diagnostics only.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Use one DRY formatter for the four remediation messages without changing their distinct guards. `cmd_serve` does not discover a canonical project root; it authorizes from `cwd`, so diagnostics must call that value the launch cwd/candidate root. Emit `mcp_serve_ready` immediately before the existing `serve_server` await, where the process is ready to read initialize. Test with a seeded serveable fixture, open stdin, no request, bounded capture, and owned-child cleanup. The optional sink is 056.006-T; lock liveness is 056.007-T.
<!-- END:implementation-notes -->