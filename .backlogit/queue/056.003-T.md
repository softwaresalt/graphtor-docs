---
artifact_type: task
created_at: 2026-08-21T20:13:57.9801621Z
dependencies:
    - 056.002-T
id: 056.003-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Harden cmd_serve diagnostics and emit preflight-complete event
updated_at: 2026-08-22T08:40:42.4336373Z
---



<!-- BEGIN:acceptance-criteria -->
- NON-CONDITIONAL diagnostics only; curative connectivity remains in the evidence-selected branch.
- Normalize all four pre-transport exit-2 messages through structured `tracing::error!` events so 056.006-T can capture them. Each names the launch cwd/authorized candidate root and bounded remediation.
- Process-level red tests cover the three reachable sites: missing explicit `--config`, empty discovered paths, and empty classified postures. Keep the structurally unreachable primary-None guard loud through the same formatter, but do not add an artificial control-flow injector solely to reach it; use a focused formatter/unit assertion if its text changes.
- Emit structured `mcp_serve_ready` with `transport=stdio` and launch cwd immediately before calling `rmcp::serve_server`, after preflight. Its contract is only `preflight_complete=true; about_to_call=serve_server`, not loop entry, transport readiness, or handshake completion.
- Preserve `candidate_root = cwd`, fail-closed gates, and all discovery signatures.
- The ready-event test uses a seeded fixture, open stdin, no initialize, bounded capture, and owned-child cleanup; it asserts the preflight event without treating it as handshake success.
- Update `docs/troubleshooting.md` and `docs/cli-reference/graphtor-docs.md` for the new per-site diagnostics and preflight event.
- fmt + clippy pedantic + tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Non-conditional cmd_serve observability. Convert the four exit-2 messages to structured actionable errors, with process tests for the three reachable guards and no artificial injector for the defensive primary-None invariant. Emit `mcp_serve_ready` only as preflight-complete/about-to-call evidence. Do not change containment or discovery.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Use one DRY formatter and `tracing::error!` for sink compatibility. The event is emitted before the existing `serve_server` await and must never claim that the loop has started. The optional sink is 056.006-T; lock liveness is 056.007-T.
<!-- END:implementation-notes -->