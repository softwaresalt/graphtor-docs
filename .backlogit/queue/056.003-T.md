---
artifact_type: task
created_at: 2026-08-21T20:13:57.9801621Z
dependencies:
    - 056.002-T
id: 056.003-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: 'Harden cmd_serve pre-serve diagnostics: loud exit-2 errors + serve-ready log (parity-safe)'
updated_at: 2026-08-22T02:05:00.0000000Z
---



<!-- BEGIN:acceptance-criteria -->
- NON-CONDITIONAL diagnostics/parity-safe scope (no longer H0a-gated). This task delivers runtime-owned observability improvements valuable on every causal branch, whose own test goes red before / green after its production change. It does NOT own curative H0a connectivity (that is the pinned-cwd managed launch contract 056.008-T plus existing-install delivery 056.009-T) and no longer claims it can green a no-target wrong-cwd managed launch.
- EVERY silent pre-transport exit-2 site becomes a loud, actionable error - there are FOUR distinct sites named by their semantic guard (not line numbers): (1) missing explicit --config (config_override points at a non-existent file), (2) served_paths empty (post-discover_served_databases union empty), (3) classified.postures empty (after classify_serve_postures + the phantom-default retain filter drops non-existent ReadOnly candidates - a SECOND, distinct 'no databases found to serve' site), and (4) primary None (post-open_serve_databases, the structurally-unreachable defence-in-depth guard). Each becomes a loud, actionable error that names the resolved project-root cwd and the remediation (drop a `.db` into `.graphtor`, pin the managed launch cwd, or pass an explicit --db-path/--config).
- A positive serve-ready startup info log is emitted on successful transport bind (Constitution V). This log line is a red→green anchor: absent today, present after the change.
- NO new containment surface and NO discovery-signature change (F1/F2/F3/N1): cmd_serve CONTINUES validating an explicit --db-path/--config against the authorized project-root cwd through the SHARED discover_served_databases / graphtor_core::path::validate_path / is_reparse_point primitives exactly as today (candidate_root = cwd, scan_root = cwd/.graphtor). No target-derived or split authorized root, and no parent-walk from a foreign launch cwd. A foreign launch cwd is corrected by pinning cwd to the canonical project root in 056.008-T, not by re-authorizing a target here.
- F4 parity preserved by construction: because no discovery signature changes, discover_served_databases, classify_serve_postures, discover_status_db_paths, and cmd_status stay in parity with no new test or divergence.
- Existing fail-closed semantics preserved AND regression-asserted: malformed registry, missing explicit --config, pre-v4 schema gate, and duplicate-intake preflight each still exit pre-serve after this change.
- Own observed-red tests from this task's production change (parity-safe): ONE TABLE-DRIVEN red diagnostic matrix covering the FOUR exit-2 cases above (one matrix row per semantic site - each asserting that site's new loud, actionable message naming the resolved project-root cwd; red today = terse/near-silent exit-2) PLUS one serve-ready-log scenario (red today = absent); all green after. Every semantic site is preserved as a distinct matrix row (do NOT drop or conflate a site). These tests assert the DIAGNOSTIC output (message + serve-ready log), NOT a successful initialize handshake - the raw no-target wrong-cwd initialize success is NOT this task's to green (it is owned by the H0a generated-contract test 056.008-T and, for the other branches, the 056.002-T harness). PRESERVE the existing tests/explicit_db_target_no_registry_test.rs negative 'config file' assertion (serve with an explicit --db-path and no registry must NOT emit 'config file' - it reaches the pre-v4 gate instead) while wording the new loud messages. Reuses the 056.002-T transport-harness spawn/capture scaffolding for out-of-process launch + stderr capture.
- fmt + clippy pedantic (-D warnings) + tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Runtime-owned, parity-safe diagnostics for cmd_serve — NON-CONDITIONAL (valuable on every branch; not H0a-gated). Convert every silent pre-transport exit-2 site (FOUR distinct semantic sites: (1) missing explicit --config, (2) served_paths empty, (3) classified.postures empty after the phantom-default retain filter - a SECOND, distinct 'no databases found to serve' site - and (4) the structurally-unreachable primary None guard) into a loud, actionable error that names the resolved project-root cwd and remediation, and emit a positive serve-ready info log on successful transport bind (Constitution V). Do NOT add a new containment surface or change discovery: cmd_serve keeps validating an explicit --db-path/--config against the authorized project-root cwd through the shared discover_served_databases / validate_path / is_reparse_point primitives (candidate_root = cwd, scan_root = cwd/.graphtor) exactly as today — no target-derived or split authorized root, and no parent-walk from a foreign launch cwd (F1/F2/F3/N1). Curative H0a connectivity is owned by the pinned-cwd managed launch contract 056.008-T (generated config pins the child cwd to the canonical project root) with existing-install delivery in 056.009-T; this task no longer claims to green a no-target wrong-cwd managed launch. Preserve and regression-assert every fail-closed gate. Diagnostics only — this task does NOT add the optional file-log sink (that is 056.006-T).
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Surfaces: src/main.rs::cmd_serve (~2446-2655). The FOUR silent exit-2 sites, named by semantic guard (verified via exact reads of cmd_serve, not line counts): (1) missing-explicit-config guard - config_override points at a non-existent file, eprintln! "error: config file ... not found" -> Ok(2); (2) served_paths.is_empty() after discover_served_databases -> "no databases found to serve" -> Ok(2); (3) classified.postures.is_empty() after classify_serve_postures AND the phantom-default retain filter that drops non-existent ReadOnly candidates -> a SECOND, distinct "no databases found to serve" -> Ok(2) (the earlier text conflated this with site 2); (4) the structurally-unreachable primary-None guard (stores.next() == None after open_serve_databases) -> Ok(2). Keep the loud-message formatting DRY (a single shared helper for the shared remediation text: drop a .db / pin cwd / pass --db-path|--config) without expanding unrelated scope. Use ONE table-driven diagnostic matrix (one row per exit-2 site + a serve-ready-log row) rather than four separate test fns, keeping all sites in one place, and PRESERVE the existing tests/explicit_db_target_no_registry_test.rs negative 'config file' assertion (an explicit --db-path with no registry must reach the pre-v4 gate, not emit 'config file') when wording the new messages. An explicit --db-path is ALREADY validated against the broader project-root cwd inside discover_served_databases (existing_candidates vs candidate_root = cwd; scan_root = cwd/.graphtor), so NO split-root/target-derived change is needed or wanted (F1/F2/F3/N1) — do not call workspace::paths::project_root / find_workspace_dir on an explicit target to re-authorize it. F4 parity: because no discovery signature changes, discover_served_databases (src/workspace/serve_discovery.rs:92-165), classify_serve_postures (263-325), discover_status_db_paths (src/main.rs:2664-2758), and cmd_status remain in parity with no new test. Add the positive serve-ready info log on the successful serve_server transport bind. This task is RUNTIME cmd_serve diagnostics only; the managed .mcp.json launch-contract generation in src/workspace/mcp_config.rs is 056.008-T (pinned cwd) and its delivery to existing installs is 056.009-T. Real lock symbols (for the conditional H0b task, not this one) are DatabaseLock::acquire / AdvisoryLock::acquire / handle_existing_lock in src/lock.rs. The serve/sync lock entry point IS the src/main.rs helper `acquire_database_lock` (~2803-2824), a thin wrapper that validates the path and delegates to those src/lock.rs primitives; do the H0b liveness change in the src/lock.rs primitives, not the wrapper. The opt-in file sink is 056.006-T; stale-lock liveness (H0b) is 056.007-T.
<!-- END:implementation-notes -->