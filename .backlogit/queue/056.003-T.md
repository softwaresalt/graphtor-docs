---
artifact_type: task
created_at: 2026-08-21T20:13:57.9801621Z
dependencies:
    - 056.002-T
id: 056.003-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Harden implicated pre-serve failure mode + add diagnostic log sink (green)
updated_at: 2026-08-21T20:56:44.2829204Z
---



<!-- BEGIN:acceptance-criteria -->
- The T1 harness passes: the server no longer exits before initialize for the reproduced benign launch.
- Only the SINGLE evidenced H0 sub-cause is implemented here (no bundled stale-lock or logging work).
- Workspace-root resolution is containment-safe: resolved only from explicit --db-path/--config inputs or the launch cwd, never by walking to parent directories; an explicit --db-path/--config target is PREFERRED over the launch cwd (this is what restores H0a connectivity).
- Containment refusal is DELEGATED to the shared graphtor_core::path::validate_path / is_reparse_point primitives (same guards as src/workspace/serve_discovery.rs), canonicalizing BOTH operands before comparison — no hand-rolled prefix check. An explicit refusal test enumerates absolute-above, ..-traversal, escaping symlink, junction/reparse-point, and a Windows short-name/case variant, each asserted refused.
- EVERY silent exit-2 'no databases found to serve' site (both discovery sites plus the primary-None guard) becomes a loud, actionable error.
- Existing fail-closed semantics preserved AND regression-asserted: malformed registry, missing explicit --config, pre-v4 schema gate, and duplicate-intake preflight each still exit pre-serve after the cwd-resolution change.
- H0a note: a loud early-exit alone does not clear OS error 232; connectivity is restored only via the preferred explicit target, else the fix is an operational launch-config/install recipe (recorded in T0/056.001-T).
- fmt + clippy pedantic (-D warnings) + tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Fix ONLY the single evidenced H0 sub-cause identified by T0/T1 (do not bundle unrelated variants or optional logging). When H0a (launch-cwd discovery) is evidenced: resolve the workspace root in a containment-safe way — only from an explicit --db-path/--config input or the launch cwd itself, never by walking to parent directories, so resolution cannot escape or climb above the launch-cwd containment boundary (Principle III/IV) — refuse any candidate that lands outside/above the boundary, and convert the silent exit-2 'no databases found to serve' into a loud, actionable error. Green the T1 harness. The stale-lock liveness variant (H0b) and the optional startup diagnosability sink are isolated into conditional tasks 056.006-T and 056.007-T.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Surfaces: src/main.rs::cmd_serve (~2446-2655) early-exit paths (auto-discovery is cwd.join(".graphtor")). Do NOT add an upward/parent-directory search — that would violate CLI containment (Principle IV). Add a positive serve-ready info log. Real lock symbols (for the conditional H0b task, not this one) are DatabaseLock::acquire / AdvisoryLock::acquire / handle_existing_lock in src/lock.rs — there is no `acquire_database_lock`. Diagnosability default is a documented stderr-redirect recipe; the opt-in file sink is the conditional 056.006-T task, and stale-lock liveness (H0b) is the conditional 056.007-T task.
<!-- END:implementation-notes -->