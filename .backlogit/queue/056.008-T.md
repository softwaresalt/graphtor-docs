---
artifact_type: task
created_at: 2026-08-21T21:58:08.4481362Z
dependencies:
    - 056.002-T
id: 056.008-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Managed .mcp.json launch-contract generation (H0a-only conditional)
updated_at: 2026-08-22T07:25:00.0000000Z
---

<!-- BEGIN:description -->
CONDITIONAL on T0 evidencing H0a (launch-cwd .graphtor discovery) AND T0's manual real-CLI probe proving that the target CLI honors its `cwd` field. Isolated out of 056.003-T so that task stays runtime-diagnostics-only and this one owns the install/config surface and the CURATIVE H0a lever. graphtor-docs owns managed .mcp.json generation (src/workspace/mcp_config.rs::managed_server_value / generate_mcp_config), which today emits only command + args ["serve"] + transport + the managed marker, so the CLI launches the child with no trusted workspace identity. Emit the minimal trusted launch identity: pin the child working directory (`cwd`) to the CANONICAL PROJECT ROOT. This single sufficient lever restores cwd-anchored registry discovery, DB auto-discovery, posture/Generation validation, background sync, and existing explicit-target validation without relaxing the runtime cwd boundary. Do not add generated `--db-path`, `--config`, or env-based target plumbing; those are unnecessary for the evidenced wrong-cwd regression and cannot substitute for an ignored `cwd`. Containment: validate the pinned cwd by EQUALITY to the canonicalized project root (it is not constrained to live inside .graphtor). No target-derived/split authorized root, no parent traversal, nothing outside the project root. Config-schema fidelity (F7): emit the T0-evidenced supported stdio field — the sibling backlogit/github/context7/tavily entries in .mcp.json use `type: "stdio"` while managed_server_value emits `transport: "stdio"` — and preserve legacy `transport` recognition; do not assert the field name is the root cause without evidence. Test-first proof: a generated-contract integration test creates the entry and directly executes its command + args + pinned cwd from an unrelated parent process, red before the generator change and green after. This proves the generated contract is executable; it does NOT prove the real Copilot CLI honors `cwd`, which T0 establishes separately. Delivering the new entry to already-installed workspaces is 056.009-T. If T0 shows the target CLI ignores/rejects `cwd`, route to H3 mode B (056.011-T) and close this H0a task as not-needed.
<!-- END:description -->

<!-- BEGIN:acceptance-criteria -->
- CONDITIONAL - H0a-only. If T0 evidences H0b, H1, or a fail-closed gate (H0c) instead of H0a, close as not-needed with a one-line rationale; closing as not-needed satisfies 056.004-T's dependency on it. Do NOT force speculative H0a implementation.
- Distinct install/config width: this changes src/workspace/mcp_config.rs (managed .mcp.json generation), NOT runtime cmd_serve (that is 056.003-T). ~2h single-width.
- Complete minimal launch-identity contract (verified against run/cmd_serve/load_source_config/serve_discovery::classify_serve_postures/spawn_background_sync): pin the child working directory (`cwd`) to the project root. Registry discovery (load_source_config -> cwd/.graphtor/config), posture/Generation-target validation (validate_path(.., root=cwd)), DB auto-discovery (cwd/.graphtor), and background sync (acquire_plan::plan(.., &cwd), data_root=cwd/.graphtor/data) are ALL cwd-anchored, so pinning cwd restores them together WITHOUT relaxing the runtime cwd boundary.
- Keep the generated contract minimal: do NOT add absolute `--db-path`, `--config`, or env target plumbing. The cwd pin is sufficient for H0a, existing explicit targets already validate correctly once cwd is the project root, and target arguments cannot substitute when a client ignores cwd.
- Launch-field support: T0's backup-first MANUAL real-CLI probe must already have proven that the target build honors `cwd`; documentation or a direct child spawn is not sufficient evidence. If the probe shows that the CLI ignores/rejects `cwd`, close this task as not-needed and route to H3 MODE B (056.011-T). No server-side external-path fallback.
- Containment (required): the pinned cwd is validated by EQUALITY to the canonicalized project root (it is NOT constrained to live inside .graphtor). No target-derived/split authorized root, no parent-directory traversal, and no generated target authorization.
- Config-schema fidelity (F7): emit the T0-evidenced supported transport field. The sibling backlogit/github/context7/tavily entries in .mcp.json use `type: "stdio"` while managed_server_value emits `transport: "stdio"`; if T0 confirms the Copilot CLI honors `type`, emit `type` (keeping or migrating legacy `transport` recognition). Preserve marker-based and exact-legacy-shape recognition (MANAGED_MARKER_KEY / is_exact_legacy_shape) so already-installed managed and pre-marker entries still refresh cleanly after the entry gains cwd/env/type keys. Do NOT claim the field name is the current root cause without T0 evidence.
- Test-first proof (this width): a generated-contract integration test that (1) generates the managed entry via generate_mcp_config for project P, (2) reads it back, and (3) DIRECTLY executes its command + args + pinned cwd from an unrelated parent cwd, asserting a successful initialize handshake and that P's databases are served. Red before the generator change, green after. This proves generator correctness and H0a connectivity after the separate T0 real-CLI capability probe; it is NOT evidence that Copilot honors the field.
- Delivery to existing installs: generating the entry only helps fresh installs / regeneration; the already-installed bug-reporter workspace is repaired by the separate migration task 056.009-T (refresh on cmd_upgrade/reinstall). Do not assume a binary upgrade rewrites .mcp.json.
- fmt + clippy pedantic (-D warnings) + tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Surfaces: src/workspace/mcp_config.rs::managed_server_value (~526-544) and generate_mcp_config (~138-227). The managed entry adds only the T0-evidenced stdio field and the canonical project-root `cwd`; do not add `--db-path`, `--config`, or env target plumbing. Verified cwd-anchoring: load_source_config default registry = cwd.join('.graphtor/config'); classify_serve_postures validates local.path and target via validate_path(.., root=cwd); serve auto-discovery = cwd.join('.graphtor'); spawn_background_sync uses acquire_plan::plan(.., &cwd) and data_root = cwd.join('.graphtor/data'). generate_mcp_config already fails closed on a symlinked/junction .mcp.json (is_reparse_point) and validates the managed binary path within project_root (PathViolation); validate the pinned cwd by equality to that canonical project root. Do not add parent-directory traversal or any target-derived/split authorized root. Config-schema (F7): managed_server_value today emits command + args ['serve'] + transport: 'stdio' + the x-graphtor-managed marker; is_exact_legacy_shape (~307-330) matches EXACTLY {command, args:['serve'], transport:'stdio'} (3 keys, no marker). Emitting the T0-evidenced field (e.g. type:'stdio' to match sibling entries) plus cwd keeps Marked recognition intact, while the pre-marker transport-only shape still migrates via is_exact_legacy_shape. Compare parsed serde_json::Value, never raw serialized bytes/key order.
<!-- END:implementation-notes -->