---
artifact_type: task
created_at: 2026-08-21T21:58:08.4481362Z
dependencies:
    - 056.017-T
id: 056.008-T
labels:
    - phase:remedy
    - cause:managed-config
    - selection:pending
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Managed .mcp.json launch contract (H0a or H3-B1 conditional)
updated_at: 2026-08-23T20:52:28.6791108Z
---

<!-- BEGIN:description -->
Conditional managed-launch generation for an H0a prerequisite or H3-B1 mechanism: generate the canonical project-root working-directory (`cwd`) contract in `managed_server_value`. Owns ONLY the cwd/working-directory field of `managed_server_value` plus focused config-generation tests. The `type`/`transport` discriminator reconciliation and its legacy-shape recognition were split out to the unconditional discriminator-generation task (056.026-T); this task no longer owns the discriminator.
<!-- END:description -->

<!-- BEGIN:acceptance-criteria -->
- Run only for an evidenced H0a prerequisite or H3-B1; otherwise move to `done` with `not-needed: managed cwd generation not selected`. The unconditional `type`/`transport` discriminator reconciliation is owned by 056.026-T, not this task.
- H0a activates when the exact-CLI treatment reaches healthy initialize or demonstrably advances beyond the foreign-cwd discovery failure to a later gate (an ordered prerequisite whose newly exposed cause is also selected); H3-B1 activates only after 056.019-T proves a different supported mechanism on the same exact CLI. If neither is selected, close with `not-needed: <rationale>`.
- Depend on 056.017-T (corrected DAG `056.017 -> 056.008`, parallel to `056.017 -> 056.024 -> 056.018`); consume 056.017-T typed outcomes. Generate ONLY the canonical project-root working-directory field/mechanism proven by T0/056.019 -- no target args/env plumbing and no stdio `type`/`transport` discriminator (that is 056.026-T).
- Validate the pinned `cwd` by equality to the canonicalized project root. Add no target-derived/split authorized root, target self-authorization, parent traversal, or external-path fallback. Preserve containment, marker ownership, exact legacy recognition, and typed collision/no-change behavior.
- Co-selection assembly rule: when both this cwd remedy and the discriminator remedy (056.026-T) are selected into one managed-config shipment, Stage orders 056.026-T before this task at assembly time (discriminator field reconciled first, then the cwd field composed) -- a shipment-assembly co-selection edge, not a standing backlog dependency.
- Write and observe red tests in this task, then green exactly three groups: managed-value cwd field, unrelated-parent generated-entry initialize with seeded data, and containment/legacy-marker preservation. This proves generated execution, not client capability or production delivery.
- Existing-install cwd/recovery delivery remains 056.009-T; T4 exclusively owns restored-production actual-client acceptance. Rust 1.75 and all gates pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Own only the cwd/working-directory field of `managed_server_value` and focused config-generation tests in `src/workspace/mcp_config.rs`. The `type`/`transport` discriminator reconciliation plus legacy-shape recognition moved to 056.026-T (unconditional). Corrected DAG: depends on 056.017-T (typed mutation API), parallel to 056.024-T/056.018-T recovery. 056.019-T owns H3-B selection, 056.009-T owns cwd/recovery existing-install delivery, and T4 owns delivered actual-client acceptance.
<!-- END:implementation-notes -->