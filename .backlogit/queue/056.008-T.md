---
artifact_type: task
created_at: 2026-08-21T21:58:08.4481362Z
dependencies:
    - 056.002-T
id: 056.008-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Managed .mcp.json launch-contract generation (H0a-only conditional)
updated_at: 2026-08-21T21:58:29.8206975Z
---

CONDITIONAL on T0 evidencing H0a (launch-cwd .graphtor discovery). Isolated out of 056.003-T so that task implements exactly one runtime cause and this one owns the install/config surface. graphtor-docs OWNS the managed .mcp.json generation (src/workspace/mcp_config.rs::managed_server_value / generate_mcp_config), which today emits only command + args ["serve"] + transport + the managed marker, so the CLI launches the child with no trusted workspace identity. Emit a trusted, containment-validated launch identity: PRIMARY lever pins the child working directory (cwd) to the project root (restores cwd-anchored registry discovery, DB auto-discovery, posture/Generation validation, and background sync together without relaxing the runtime cwd boundary); COMPLEMENT pins explicit --db-path (and --config when a registry exists) only when the served DB/registry lives outside that cwd. Explicit targets alone do not restore Generation. Preserve the genuinely-absent-registry zero-config case. Add an unrelated-cwd launch regression test. If T0 shows the CLI already supplies a usable cwd-local .graphtor or explicit target, or evidences H0b/H1/H0c, close as not-needed.

<!-- BEGIN:acceptance-criteria -->
- CONDITIONAL - H0a-only. If T0 evidences H0b, H1, or a fail-closed gate (H0c) instead of H0a, close as not-needed with a one-line rationale; closing as not-needed satisfies 056.004-T's dependency on it. Do NOT force speculative H0a implementation.
- Distinct install/config width: this changes src/workspace/mcp_config.rs (managed .mcp.json generation), NOT runtime cmd_serve (that is 056.003-T). ~2h single-width.
- Complete minimal launch-identity contract (verified against run/cmd_serve/load_source_config/serve_discovery::classify_serve_postures/spawn_background_sync): PRIMARY lever pins the child working directory (cwd) to the project root - registry discovery (load_source_config -> cwd/.graphtor/config), posture/Generation-target validation (validate_path(.., root=cwd)), DB auto-discovery (cwd/.graphtor), and background sync (acquire_plan::plan(.., &cwd), data_root=cwd/.graphtor/data) are ALL cwd-anchored, so pinning cwd restores registry-backed Generation/background-sync together WITHOUT relaxing the runtime cwd boundary.
- COMPLEMENT: pin absolute --db-path (the DB target) and, when a registry exists, --config, needed only when the served DB/registry legitimately lives outside the pinned cwd. Explicit targets ALONE do NOT restore Generation (posture/sync validate against cwd). Do NOT fabricate --config when the registry is genuinely absent (load_source_config errors on a missing explicit override; an absent default correctly falls through to zero-config).
- Evidence gate on launch mechanism: T0 records whether the CLI's MCP launch config honors a cwd/working-directory (and env) field. If yes, the cwd pin is the fix. If no, restoring registry-backed Generation under a foreign cwd would require relaxing the containment root (REFUSED); the remediation reduces to pinning --db-path for read-only DB serving plus an operational recipe (launch from project root), recorded in 056.001-T.
- Containment (required): every pinned cwd/--db-path/--config path is derived from the project root at generation time and validated within project-root .graphtor via the shared graphtor_core::path::validate_path / is_reparse_point primitives (both operands canonicalized). No parent-directory traversal.
- Unrelated-cwd launch regression test: a managed entry generated for project P must serve P's databases (and, when a registry exists, classify its real source targets Generation) when the child is spawned from an unrelated cwd.
- fmt + clippy pedantic (-D warnings) + tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Surfaces: src/workspace/mcp_config.rs::managed_server_value (~528-540) and generate_mcp_config. Both --db-path and --config are global clap flags (also env-backed: GRAPHTOR_DB_PATH / GRAPHTOR_SOURCES), and MCP stdio server entries can carry a cwd/env field, so the managed entry can pin cwd and/or the explicit targets. VERIFIED cwd-anchoring: load_source_config default registry = cwd.join('.graphtor/config'); classify_serve_postures validates local.path and target via validate_path(.., root=cwd); serve auto-discovery = cwd.join('.graphtor'); spawn_background_sync uses acquire_plan::plan(.., &cwd) and data_root = cwd.join('.graphtor/data'). generate_mcp_config already fails closed on a symlinked/junction .mcp.json (is_reparse_point) and validates the managed binary path within project_root (PathViolation) - reuse those same primitives for the pinned cwd/targets. Do NOT add parent-directory traversal. Managed entries are identified by the x-graphtor-managed marker, so adding cwd/env/args keys does not break marker-based recognition (only the exact legacy-shape migration path requires {command,args,transport}).
<!-- END:implementation-notes -->