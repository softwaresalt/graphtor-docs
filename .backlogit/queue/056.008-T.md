---
artifact_type: task
created_at: 2026-08-21T21:58:08.4481362Z
dependencies:
    - 056.002-T
id: 056.008-T
parent_id: 056-F
priority: high
references:
    - docs/exec-plans/2026-08-21-mcp-serve-initialize-handshake-regression-plan.md
status: queued
title: Managed .mcp.json launch-contract generation (H0a-only conditional)
updated_at: 2026-08-22T02:05:00.0000000Z
---

<!-- BEGIN:description -->
CONDITIONAL on T0 evidencing H0a (launch-cwd .graphtor discovery). Isolated out of 056.003-T so that task stays runtime-diagnostics-only and this one owns the install/config surface and the CURATIVE H0a lever. graphtor-docs OWNS the managed .mcp.json generation (src/workspace/mcp_config.rs::managed_server_value / generate_mcp_config), which today emits only command + args ["serve"] + transport + the managed marker, so the CLI launches the child with no trusted workspace identity. The Copilot CLI stdio MCP contract SUPPORTS cwd and env (verified against the Copilot CLI/SDK MCP config docs; locally the backlogit entry already uses env/${workspaceFolder}). Emit a trusted, containment-validated launch identity whose PRIMARY and sufficient lever pins the child working directory (cwd) to the CANONICAL PROJECT ROOT — this single lever restores cwd-anchored registry discovery, DB auto-discovery, posture/Generation validation, and background sync together AND makes the runtime's existing explicit-target validation resolve correctly (056.003 keeps validating --db-path/--config against candidate_root = cwd; once cwd is the project root those targets validate), all without relaxing the runtime cwd boundary. COMPLEMENT: pin explicit --db-path (and --config when a registry exists) only when the served DB/registry legitimately lives outside .graphtor but within the project root; explicit targets alone do not restore Generation. Containment: validate the pinned cwd by EQUALITY to the canonicalized project root (it is NOT constrained to live inside .graphtor); validate the pinned --db-path/--config as project-root-derived paths via the shared validate_path / is_reparse_point primitives (both operands canonicalized). No target-derived/split authorized root, no parent traversal, nothing outside the project root — a foreign launch cwd is corrected by the cwd pin here, never by re-authorizing a target in the runtime. Config-schema fidelity (F7): emit the T0-evidenced supported transport field — the sibling backlogit/github/context7/tavily entries in .mcp.json use `type: "stdio"` while managed_server_value emits `transport: "stdio"`, so if T0 confirms the Copilot CLI honors `type`, emit `type` and keep or migrate the legacy `transport` recognition; do NOT assert the field name is the current root cause without T0 evidence. Preserve the genuinely-absent-registry zero-config case. Test-first proof: a managed-launch integration test that generates the entry then EXECUTES the generated launch contract from an unrelated cwd (distinct from the raw 056.002-T transport harness), red before the generator change and green after. Generating the entry only helps fresh installs — delivering it to already-installed workspaces is the separate task 056.009-T. Depends on 056.002-T (the transport harness must exist first; consensus refuted the unnecessary-dependency claim). If T0 shows the CLI already supplies a usable cwd-local .graphtor or explicit target, or evidences H0b/H1/H0c, close as not-needed.
<!-- END:description -->

<!-- BEGIN:acceptance-criteria -->
- CONDITIONAL - H0a-only. If T0 evidences H0b, H1, or a fail-closed gate (H0c) instead of H0a, close as not-needed with a one-line rationale; closing as not-needed satisfies 056.004-T's dependency on it. Do NOT force speculative H0a implementation.
- Distinct install/config width: this changes src/workspace/mcp_config.rs (managed .mcp.json generation), NOT runtime cmd_serve (that is 056.003-T). ~2h single-width.
- Complete minimal launch-identity contract (verified against run/cmd_serve/load_source_config/serve_discovery::classify_serve_postures/spawn_background_sync): PRIMARY lever pins the child working directory (cwd) to the project root - registry discovery (load_source_config -> cwd/.graphtor/config), posture/Generation-target validation (validate_path(.., root=cwd)), DB auto-discovery (cwd/.graphtor), and background sync (acquire_plan::plan(.., &cwd), data_root=cwd/.graphtor/data) are ALL cwd-anchored, so pinning cwd restores registry-backed Generation/background-sync together WITHOUT relaxing the runtime cwd boundary.
- COMPLEMENT: pin absolute --db-path (the DB target) and, when a registry exists, --config, needed only when the served DB/registry legitimately lives outside .graphtor but within the project root. Explicit targets ALONE do NOT restore Generation (posture/sync validate against cwd; once cwd is pinned to the project root they validate correctly). Do NOT fabricate --config when the registry is genuinely absent (load_source_config errors on a missing explicit override; an absent default correctly falls through to zero-config).
- Launch-field support VERIFIED: the Copilot CLI stdio MCP config supports cwd and env (confirmed against the Copilot CLI/SDK MCP config docs; locally the backlogit entry sets env/${workspaceFolder}, and --db-path/--config are env-backed via GRAPHTOR_DB_PATH/GRAPHTOR_SOURCES), so the cwd pin (primary lever) is viable. T0 still confirms the honored fields for the specific CLI build. NO env/explicit-arg fallback when the CLI ignores cwd: if T0 evidences that the target CLI does NOT honor the managed cwd, then GRAPHTOR_DB_PATH/GRAPHTOR_SOURCES and explicit --db-path/--config CANNOT substitute — the runtime stays cwd-anchored by containment (cmd_serve validates every explicit target against candidate_root = cwd, which is the foreign launch cwd when cwd is ignored), so a target can never re-authorize itself from a foreign launch cwd (F1/F2/F3/N1). A CLI that lacks or ignores cwd is a client-compatibility condition — specifically H3 MODE B (056.011-T): this task's generated-contract launch test is the DISCRIMINATING EVIDENCE (H0a if the spawned child honors the pinned cwd and the handshake completes; H3 mode B if the CLI ignores/rejects cwd and the child early-exits from a foreign cwd), repaired by an evidence-backed client-compatibility adjustment (a supported CLI version or a client-honored working-directory mechanism) or recorded as an explicit operational unsupported-client path — NEVER a fake curative env/arg fallback and NEVER a server-side external-path fallback. The complement explicit --db-path/--config is valid ONLY because the pinned cwd equals the project root; it is not a substitute for an ignored cwd.
- Containment (required): the pinned cwd is validated by EQUALITY to the canonicalized project root (it is NOT constrained to live inside .graphtor); the pinned --db-path/--config are validated as project-root-derived paths (typically under .graphtor, permissibly elsewhere under the project root) via the shared graphtor_core::path::validate_path / is_reparse_point primitives (both operands canonicalized). No target-derived/split authorized root, no parent-directory traversal; authorize nothing outside the project root.
- Config-schema fidelity (F7): emit the T0-evidenced supported transport field. The sibling backlogit/github/context7/tavily entries in .mcp.json use `type: "stdio"` while managed_server_value emits `transport: "stdio"`; if T0 confirms the Copilot CLI honors `type`, emit `type` (keeping or migrating legacy `transport` recognition). Preserve marker-based and exact-legacy-shape recognition (MANAGED_MARKER_KEY / is_exact_legacy_shape) so already-installed managed and pre-marker entries still refresh cleanly after the entry gains cwd/env/type keys. Do NOT claim the field name is the current root cause without T0 evidence.
- Test-first proof (this width): a managed-launch integration test that (1) generates the managed entry via generate_mcp_config for project P, (2) reads it back, and (3) EXECUTES the generated launch contract (command + args + pinned cwd/env) by spawning the child from an UNRELATED cwd, asserting a successful initialize handshake and that P's databases are served (and, when a registry exists, its real source targets classify Generation). Red before the generator change, green after. Distinct from the raw T1 transport harness, which cannot exercise the generated contract (Copilot review P1: H0a proof coupling). This same generated-contract launch is the DISCRIMINATING EVIDENCE between H0a and H3 mode B (P1-1): if the spawned child honors the pinned cwd and the handshake completes, the cause was H0a and this task cures it; if the CLI IGNORES/REJECTS the pinned cwd and the child EARLY-EXITS from a foreign cwd, the condition is H3 mode B (056.011-T), NOT a server-side fallback here.
- Delivery to existing installs: generating the entry only helps fresh installs / regeneration; the already-installed bug-reporter workspace is repaired by the separate migration task 056.009-T (refresh on cmd_upgrade/reinstall). Do not assume a binary upgrade rewrites .mcp.json.
- fmt + clippy pedantic (-D warnings) + tests pass.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Surfaces: src/workspace/mcp_config.rs::managed_server_value (~526-544) and generate_mcp_config (~138-227). Both --db-path and --config are global clap flags (also env-backed: GRAPHTOR_DB_PATH / GRAPHTOR_SOURCES), and MCP stdio server entries can carry a cwd/env field, so the managed entry can pin cwd and/or the explicit targets. VERIFIED cwd-anchoring: load_source_config default registry = cwd.join('.graphtor/config'); classify_serve_postures validates local.path and target via validate_path(.., root=cwd); serve auto-discovery = cwd.join('.graphtor'); spawn_background_sync uses acquire_plan::plan(.., &cwd) and data_root = cwd.join('.graphtor/data'). generate_mcp_config already fails closed on a symlinked/junction .mcp.json (is_reparse_point) and validates the managed binary path within project_root (PathViolation) - reuse those same primitives for the pinned cwd/targets. Do NOT add parent-directory traversal, and do NOT introduce any target-derived/split authorized root — the runtime keeps validating explicit targets against candidate_root = cwd (056.003), so the cwd pin is the only lever this task adds (F1/F2/F3/N1). Config-schema (F7): managed_server_value today emits command + args ['serve'] + transport: 'stdio' + the x-graphtor-managed marker; is_exact_legacy_shape (~307-330) matches EXACTLY {command, args:['serve'], transport:'stdio'} (3 keys, no marker). Emitting the T0-evidenced field (e.g. type:'stdio' to match the sibling entries) plus cwd/env keeps Marked recognition intact (the marker is still present) and the pre-marker transport-only shape still migrates via is_exact_legacy_shape — keep that legacy recognizer even if new entries switch to `type`. Managed entries are identified by the x-graphtor-managed marker, so adding cwd/env/type/args keys does not break marker-based recognition (only the exact legacy-shape migration path requires the pre-marker {command,args,transport} shape). Rust guidance: when comparing an existing entry against the desired shape (the idempotent already-up-to-date Ok(None) case and is_exact_legacy_shape), compare PARSED serde_json::Value, never raw serialized bytes/key-order, so a semantically-identical entry with different key ordering is not spuriously rewritten; preserve is_exact_legacy_shape compatibility exactly.
<!-- END:implementation-notes -->