---
artifact_type: task
created_at: 2026-05-07T19:35:35.1604477-07:00
id: 033.002-T
labels:
    - db
    - tdd-green
    - bugfix
parent_id: 033-F
priority: medium
references:
    - src/db/chunks.rs
    - docs/exec-plans/2026-05-07-upsert-chunk-preserve-embedding-plan.md
status: queued
title: Implement join-put fix in upsert_chunk and update docs
updated_at: 2026-05-07T19:35:35.1604477-07:00
---

Green-phase TDD: Fix `upsert_chunk` in `src/db/chunks.rs` to preserve existing non-null embeddings using a join-put pattern.

Implementation approach (two-step Rust, per plan review P3-002 advisory):
1. Before the `:put`, call `get_vector(store, chunk_id)` to check for existing embedding
2. If `Some(emb)` is returned, pass the embedding as a `DataValue::List` parameter instead of `null`
3. If `None`, pass `null` as currently done
4. Update the Datalog script to use `$embedding` parameter instead of hardcoded `null`

Also update the module doc comment (lines 9-10) to reflect new behaviour:
- Remove: "always sets embedding to null"
- Replace with: "preserves any existing non-null embedding"

Update the `upsert_chunk` function doc comment to describe the preservation behaviour.

Exit criteria:
- `cargo test db_chunks_test::upsert_chunk_preserves_existing_embedding` PASSES
- All existing `db_chunks_test` tests still pass
- All existing `db_vectors_test` tests still pass
- `cargo clippy --all-targets -- -D warnings -D clippy::pedantic` clean

Reference: docs/exec-plans/2026-05-07-upsert-chunk-preserve-embedding-plan.md (Units 2+3)